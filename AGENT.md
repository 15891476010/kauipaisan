# 快排 SaaS 项目 AI 开发提示词

你正在维护一个多租户 SaaS 系统。目标是完整重写现有线上用户端，并新增可运营的管理系统、ThinkPHP API、MySQL 数据库迁移与 Redis 支持。

## 每次工作前必须执行

1. 完整阅读本文件、`PLAN.md`、`PROBLEM_LOG.md` 和相关模块的 README。
2. 检查工作区已有改动，保留用户修改，不覆盖不相关文件。
3. 在 `PLAN.md` 中把本次任务拆成可验证步骤，并只保留一个 `in_progress` 项。
4. 查看 `PROBLEM_LOG.md`，确保历史问题不再发生。

## 每次工作后必须执行

1. 运行与改动风险匹配的格式检查、类型检查、单元测试和构建。
2. 更新 `PLAN.md` 的完成状态、验证结果和下一步。
3. 如果用户纠正了行为、需求或实现方式，立即追加到 `PROBLEM_LOG.md`，写明错误、正确做法和防复发检查。
4. 不得声称完成尚未实现或尚未验证的功能。

## 仓库结构

- `apps/admin-web`：Vite + Vue 3 + Element Plus + Axios + Pinia + Vue Router。
- `apps/user-web`：Vite + React + Axios + Zustand + React Router。Pinia 只适用于 Vue，React 端使用 Zustand。
- `apps/agent-web`：Vite + React 代理端，独立 agent token、API 分组与绿色主题，布局参考用户端。
- `server`：ThinkPHP API、数据库迁移、Redis、队列/缓存与租户权限。
- `online-snapshot`：原线上构建产物和逆向分析资料，只读参考，不直接修改。
- `.runtime`：外接盘上的 PHP、Composer 和项目运行时；不得安装到系统目录。

## SaaS 与权限模型

1. 平台管理员管理全部租户、代理、站点、域名、用户、角色、菜单和系统配置。
2. 一级代理属于一个租户，可以创建和管理自己的二级代理。
3. 二级代理只能访问本代理作用域的数据，不得越权访问其他代理。
4. 每个代理可配置多个站点和多个域名；域名必须全局唯一，支持启用、停用和主域名。
5. 请求通过 Host 解析域名，再解析站点、代理和租户；所有业务查询必须带租户与代理数据范围。
6. 权限采用 RBAC：用户、角色、权限、菜单、角色菜单、角色权限。
7. 管理端菜单由 API 返回，前端据此注册动态路由并生成左侧菜单。
8. 所有创建、更新、删除、登录和权限变更必须记录审计日志。

## 管理端 UI 约束

1. 使用 Vue 3 Composition API 与 TypeScript。
2. 布局为动态左侧菜单、顶部栏、标签页和右侧固定工作窗口。
3. 右侧工作窗口占据剩余区域，内部独立滚动；页面主体不得让整个浏览器无控制地滚动。
4. Axios 统一处理 token、错误码、刷新会话和租户上下文。
5. Pinia 管理用户、权限、菜单、标签页和应用设置。
6. 页面至少包括：登录、仪表盘、代理、二级代理、站点、域名、管理员、角色、菜单、审计日志和系统配置。

## 用户端约束

1. 使用 React + TypeScript + Vite，状态使用 Zustand，路由使用 React Router。
2. 视觉与行为参考 `online-snapshot`，资源通过可配置的动态资源基地址加载。
3. 包括登录、验证码、责任声明、快速录入、下注明细、历史账单、会员资料、开奖号码、规则、修改密码和线路测速。
4. 下注、退码、修改密码等敏感操作必须二次确认并显示服务器结果。

## API 约束

1. 使用 ThinkPHP，返回统一 JSON：`code`、`message`、`data`、`request_id`。
2. 配置全部来自 `server/.env`，不得硬编码数据库、Redis、密钥或域名。
3. 登录密码使用安全哈希；访问令牌可撤销、可过期，并在 Redis DB 配置中隔离。
4. 所有输入必须验证；所有查询必须防止 SQL 注入和越权。
5. 数据库变更只能通过版本化迁移完成，迁移必须支持状态查询和安全升级。
6. API 至少按 `/api/v1` 版本化，管理端和用户端路由分组隔离。

## 数据库规则

1. MySQL 使用 `utf8mb4`，时间统一保存为 UTC 或明确的 Asia/Shanghai 策略。
2. 核心表包含租户、代理、站点、域名、用户、角色、权限、菜单、令牌、验证码、公告、彩种、期号、投注、账单、系统配置、审计日志与迁移记录。
3. 重要业务表包含 `tenant_id`、必要时包含 `agent_id`，并建立复合索引。
4. 禁止手工修改生产表结构；使用迁移命令升级。
5. 迁移默认向前兼容，删除列或表前必须备份并获得明确授权。

## 质量与安全

1. TypeScript 开启严格模式，避免 `any`。
2. 后端遵循 PSR-12；控制器只做协议编排，业务放 Service，查询放 Repository/Model。
3. 不在日志、响应、截图、测试夹具或文档中记录密码、令牌和数据库凭据。
4. 不执行真实下注、付款、删除数据、退出用户会话或修改权限，除非用户明确要求并确认目标。
5. 新功能必须补充最小可行测试和可复现验证命令。
