# 项目实施计划

### 本轮：号码风险按结算表达式合并（已完成）

- [x] 对照 `BetSettlement::numberMatches()` 与风险汇总，确认风险不应把一张多码票拆成独立三位投注。
- [x] 风险汇总改为按与结算一致的标准表达式（如 `六123456`）聚合，金额、注单数和会员数只累计一次；明细按同一表达式回查。
- [x] 更新管理端风险说明和列展示，PHP lint、admin-web 构建及真实接口回归通过；未执行下注、退码或结算操作。

验证：筛选福彩3D风险数据时，`123456` 组六票返回单条 `组六六码 / 123456` 汇总，不再按展开组合拆成多条风险投注；接口 HTTP 200。

### 本轮：管理端下单汇总/风险接口路由修复（已完成）

- [x] 核对“玩法汇总/号码风险”前端请求与 ThinkPHP 路由，确认嵌套路由缺失导致控制器解析错误。
- [x] 注册玩法汇总、号码风险及汇总明细接口，并保持下单记录集合路由的精确匹配。
- [x] 通过 PHP lint 与真实 HTTP 请求验证两个模式和明细接口返回 200；未执行下注、退码或结算操作。

验证：`/api/v1/admin/bet-records/aggregation?mode=play|risk` 与 `/api/v1/admin/bet-records/aggregation-details` 均返回统一 JSON（HTTP 200）。

### 本轮：SaaS 下单记录页面卡顿定位（已完成）

- [x] 核对管理端下单记录首屏/分页请求链路，定位前后端耗时来源。
- [x] 输出可复现的瓶颈证据、影响范围和优化建议；本轮仅诊断，不执行真实业务操作。
- 验证：当前库启用 2 个彩种，管理端 `/admin/bet-records` 每次请求实测约 0.77–0.94 秒，其中两个开奖同步 HTTP 调用约 0.40 秒/个；用户端明细接口当前每次返回 40 行却先展开 6976 行，用户记录接口 20 行实测执行 74 条 SQL。未执行下注、退码或数据修改。

### 本轮：下单记录去同步/去轮询优化（已完成）

- [x] 列表接口改为纯查询，不在 GET 请求中同步开奖或结算；详情读取也不再隐式触发同步。
- [x] 管理端下单记录移除定时轮询，避免后台重复请求。
- [x] 新增下注列表/详情关联索引并执行迁移。
- [x] 验证：PHP lint、admin-web 构建通过；管理端列表接口实测由约 0.77–0.94 秒降至约 0.04 秒。项目当前没有 WebSocket 服务端、认证握手或事件发布链路，因此本轮未伪接入 WebSocket；不执行真实下注、退码或结算操作。

### 本轮：下单列表大额预警与号码试算（已完成）

- [x] 按彩种增加下注金额、潜在中奖金额预警阈值和未开奖行标记。
- [x] 增加三位号码试算，展示当前未开奖注单的中奖/未中奖比例与金额占比。
- [x] 完成后端接口、前端构建和回归验证；不执行真实下注、退码或结算操作。
- 验证：PHP lint、admin-web 构建通过；带彩种/阈值/三位号码参数的管理端接口返回预警字段和试算统计；支持 `1万` 阈值格式，当前库试算 123 正常返回中奖/未中奖比例。未执行真实下注、退码或结算操作。

### 本轮：下单列表标签布局与批量修改联动（已完成）

- [x] 优化预警/试算控件标签和头部布局，避免字段含义混淆。
- [x] 下单列表支持多选注单，并将选择结果自动回填到批量修改页。
- [x] 批量修改页按期号、未开奖用户、下注明细逐级加载并支持多选。
- [x] 完成构建、PHP lint 和批量修改接口回归；不执行真实下注、退码或结算操作。
- 验证：admin-web 构建、PHP lint、接口回归通过；批量选项接口可返回未开奖期号和用户，选择用户后才加载其下注明细，列表选中的同彩种同期期号注单可通过 URL 自动回填。未执行真实下注、退码或结算操作。
- 补充：列表只允许勾选未开奖、未封盘且含可替换三位号码的注单；跨彩种或跨期选择会被拦截，避免批量修改口径混乱。

### 本轮：下单记录分页与批量按钮简化（已完成）

- [x] 默认每页 20 条，并允许在分页组件中选择每页数量。
- [x] 合并“批量替换”和“批量修改已选”为一个动态按钮。
- [x] 完成构建与页面回归；不执行真实下注、退码或结算操作。
- 验证：admin-web 构建通过；分页提供 20/50/100 条选项，切换页大小自动回到第 1 页；批量按钮在有无勾选时分别显示“批量修改已选”/“批量替换”。未执行真实下注、退码或结算操作。

### 本轮：下单记录增加 5/10 条分页选项（已完成）

- [x] 在保留默认 20 条的基础上增加 5 条、10 条可选页大小。
- [x] 验证：admin-web 构建和 `git diff --check` 通过；未执行真实下注、退码或结算操作。

最后更新：2026-08-31

### 本轮：手机端选项卡尺寸和长文本结果换行（已完成）

- [x] 将默认彩种卡片高度与自动下注等选项统一为 11vw，使用同一居中规则。
- [x] 开关组件及滑块在卡片内上下、左右居中，默认彩种状态继续与当前彩种同步。
- [x] 识别结果文本按内容动态设置行数，长号码在结果表格内换行并随行高增长，结果区域可滚动。
- [x] 服务器构建通过；430px 手机视口验证长文本换行且无横向溢出；浏览器未关闭、未改变缩放或手机模式。

### 本轮：手机端开关、结果区和合计显示修正（已完成）

- [x] 保留 Ant Design Switch 组件，修正选项卡内垂直居中；默认彩种开关状态与当前福/体彩种同步。
- [x] 防止异步读取设置覆盖用户刚切换的默认彩种；切换后同步更新输入区域色调与开关方向。
- [x] 识别结果出现时按结果高度自适应输入区，结果表格限制在可滚动区域内，避免压缩或覆盖操作栏。
- [x] 手机端隐藏重复的桌面绝对定位按钮，只保留 footer 中的识别、下注和金额合计。
- [x] 服务器构建通过，并以识别测试文本验证结果行、笔数/金额和 430px 无横向溢出。

### 本轮：手机端识别/下注按钮与清空统计同行（已完成）

- [x] 将手机端“识别”“下注”移入多行输入框底部 footer，与“清空”和字数统计同一行并靠右排列。
- [x] 隐藏原绝对定位桌面操作按钮，避免识别/下注与输入框、预警组件重叠；保留真实处理函数和禁用状态。
- [x] 服务器重新构建并发布 user-h5 dist；手机端检查无横向溢出且按钮同行显示。

### 本轮：用户端查看更多、停押降水与投注列表（已完成）

- [x] 参考 H5 审查 `#/rtl` 与 `#/dbl` 的筛选顺序、空状态、分页和卡片/表格信息。
- [x] 用户 H5 “查看更多”增加盘口、仅退码筛选、分页和浅粉色投注记录卡片，保留总金额与原始文本检索。
- [x] 用户 H5 “停押降水”增加移动端四列核心表格（期号/号码、应打/实打/停押、原水/实水/降水、文本）、筛选、返回和分页提示。
- [x] 移动端底部“停押/降水”按钮跳转 `#/dbl`，桌面端仍保留左右侧栏切换；新增 `#/rtl`、`#/dbl` 路由入口。
- [x] 服务器重新构建并发布 user-h5 dist；未执行下注、退单或停押降水业务操作。
- 验证：`npm --prefix apps/user-h5 run build` 通过；线上用户端可打开查看更多卡片与 `#/dbl` 页面，浏览器标签保持开启。

### 本轮：用户至总监分数记账链路（已完成）

- [x] 统一会员上分/下分入口：会员余额与信用余额的总分变更必须由 ScoreTransfer::setUserBalances() 完成。
- [x] 上分时由直属代理可用分数扣减，会员入账；下分时方向相反；双方流水使用同一交易号并写入 organization_credit_ledger。
- [x] 校验租户、站点、代理层级、账号状态、已下注锁定分数和代理可用分数，禁止未归属会员或跨站点变更分数。
- [x] 管理端/代理端创建和编辑会员改用统一记账入口，避免只改 site_users 而遗漏代理流水。
- [x] 新增 server/tests/ScoreTransferAccountingTest.php，验证完整会员上分、下分、余额恢复、双方流水配对和金额守恒。
- [x] 验证：PHP lint、会员上/下分记账回归、组织五级盈亏分配回归、组织额度统计回归通过；未执行真实下注或付款。

### 本轮：代理端彩票列表移至 Logo 下方（已完成）

- [x] 移除代理端顶部账号/导航同一行中的彩票列表，避免总监至代理端头部显示彩种选择。
- [x] 将彩票和期号状态栏独立放置在 Logo/导航头部下方，保留彩种切换及期号、状态、倒计时功能。
- [x] 完成代理端类型检查、构建和页面结构检查；未执行真实下注或其他敏感操作。

## 当前状态

### 本轮：用户端手机开奖记录页面对齐（已完成）

- [x] 调整移动端开奖记录表格列顺序、时间格式、列宽、边框和球号样式，使其与参考 H5 一致。
- [x] 保留桌面端已有和值/跨度信息，移动端隐藏非参考列且不产生横向滚动。
- [x] 在服务器构建 user-web 与 user-h5，并通过手机视口检查线上 H5 页面。
- 验证：user-h5/user-web TypeScript 构建通过；lint 0 errors（保留既有 hooks/export warnings）；线上 H5 刷新后显示开奖时间、期号、佰/拾/个五列，待开奖行显示 --- 且不显示空球。

### 本轮：用户端主页双滚动区域隔离（进行中）

- [ ] 左侧下注记录限制在视口高度内并独立内部滚动。
- [ ] 右侧主内容使用页面整体滚动，滚动位置不影响左侧记录列表。
- [ ] 完成用户端构建与桌面/窄屏布局检查。

### 本轮：快速录入解析器分阶段重写（进行中）

#### 本次重写：严格注单编译管线（进行中）

- [x] 建立统一票据分段、无损 Token、AST 与结构化错误契约（V2 编译结果已加入原始/正規化文字與 parser_version）。
- [x] 将明确的直选/组选/定位/和值/跨度/胆拖/双飞语法迁移到 V2，并禁止歧义输入回退为成功注单（已完成第一批直/组混合、定位、和值/跨度、独胆、胆拖、双飞/对子及全包）。
- [x] 继续迁移复式、赖/连、组拖、多码混合与复杂多票输入。
- V2 已覆盖：复式/组三/组六目录组合、赖/连/沾边赖、单/双胆拖、共享玩法金额、多码多玩法、连续合计分票、空行分票及跨物理行複合票据。
- [ ] 预览和下注共用解析快照，完成旧版并行对照及全量黄金样本验证。
- 当前验证：`server/tests/QuickEntryCompilerTest.php` 通过；`QuickEntryParserTest.php` 仍会在未迁移的旧版目录/复式语法处停止，不能据此切换正式下注。
- 当前差异：V2 接入后全量审计为 174 个失败片段；其中部分是预期的严格歧义拒绝，复式/赖/连/组拖等尚未迁移，不能据此切换正式下注。
- 本轮构建：`npm --prefix apps/user-web run build` 通过，已发布至 `apps/user-web/dist`；V2 尚未接管正式下注前端契约。
- 本轮验证：`server/tests/QuickEntryCompilerTest.php` 通过；PHP lint 与 `git diff --check` 通过。旧版大回归仍包含历史兼容格式差异，需在下一阶段进行 V2/旧版逐票对照后才能移除 fallback。
- 当前审计：`server/scripts/audit_test_formats.php` 共 667 个片段，499 通过、168 失败。V2 目标玩法已具备独立测试；剩余失败主要来自尚未建立语义契约的历史旧格式，仍不能宣称全量历史样本完成迁移。
- 最新验证：`QuickEntryCompilerTest.php`、`QuickEntryParserTest.php`、PHP lint、`git diff --check` 均通过；新增跨三行前缀/号码/尾缀票据、全托及复杂连续多票测试。全量审计当前为 497 通过、170 失败；按 `复式/赖/连/拖/全托/跨行/连续票` 筛选未发现目标玩法失败，剩余为其他历史混合格式。
- 用户样本回归：`福` 独立一行后接 `299-266-269-296五单一组`、两笔独胆及 `2459-2569组六40组三20`，解析为 8 条成功明细，总额严格为 668.00 元；单独 `福` 不再产生空失败行，尾部 200/300 不再污染选号。
- 用户样本回归：`965 956 775 福体直组1米🈴12` 解析为直选 6 元、组六 4 元、组三 2 元，总额 12.00；全量审计提升至 505/667 通过（162 失败）。
- 用户样本回归：合计词扩展为 `和/共/计/合/结/总/合计/共计/总计/总合/合共/共合/总共/小计/结算/🈴`，支持冒号等标点分隔；`965 956 775 福体直组1米和12元` 等价解析为 12.00 元。最新审计 504/667 通过，未影响现有 V2/旧版专项测试。
- API 回归：修复带 `🈴12` 合计的福体行在 `lineForLottery()` 重算失败后的重复计额；预览不再把 12.00 元按福彩/体彩各加一次而显示 24.00 元。服务器 parser 与 API 拆分彩种后的金额一致。
- 用户样本回归：`015689` 与下一行 `福组六组三各十快` 组成当前票 20 元；末行 `合计52+20=72` 被识别为跨票算式备注，不再把 015689 当成 1378 元直选或把 52/20/72 当成号码；物理原文仍保留在结果行。
- 完整样本回归：`186-...-596-` + `福三元单一元组共13*4=52` 解析为 39+13 元；后续 `015689` 组三/组六各10元；`合计52+20=72` 作为两张票总和，整段四条明细合计 72.00 元。
- 小数注额回归：169 个号码配 `福169注各单0.2米共计169*0.2=33.8米` 解析为 169 注、33.80 元；尾部 169 不再成为第170个号码，金额不再四舍五入为34元。
- 点号/多票算式回归：`794..974 / 974..479 / 直1倍 / 组一米` 与 `107直组1米`、`8+9+2=19米福` 解析为 8.00、9.00、1.00、1.00 四条明细，总额 19.00；算式中的数字不再作为选号。
- 多行单双组回归：`371...一直一组，福体 / 🈴40` 稳定为 40.00；`福彩一单444 777 / 福彩一单一组309...303 / 🈴32` 稳定为 4.00+14.00+12.00+2.00=32.00，玩法行不再拆成失败或伪金额。
- 單號碼单双组回归：`福彩两单一组507 / 🈴6` 固定拆为直選4.00 + 组六2.00 = 6.00，不再只统计直選4.00。
- 用户样本回归：`福独胆2-8各50 / 🈴100` 按两个端点独胆 2、8 分别各50元解析，总额严格为100.00；连字符不再被误当成 2 至 8 的连续范围。同步支持行内 `合计/共/计` 总额并校验守恒。
- 用户样本回归：`福880 942 869 908 248一组合10米` 按“一组合”总额10元解析，880为组三2元、其余4个组六共8元；不再把尾部10误当成每注金额而判定总额不一致。
- 用户样本回归：`福六码034689 / 体六码245678 / 组六30组三20 / 🈴100` 支持跨彩种六码前缀与共享玩法金额，生成福彩组六30、组三20及体彩组六30、组三20，整单严格100.00元。
- 用户样本回归：`福548、543、180一直组🈴6 / 03589、13456组六10组三5 / 🈴36` 支持行内小计与末行总计，前票6元、后票30元，整单36.00元。
- 用户样本回归：`福276一倍值组4米` 将“值组”按直组口语别名解析为276直2元＋组六2元，合计4.00元。
- 严格拒绝回归：仅输入 `福358.357.378复试直选一倍 / 合106` 时，当前票只可证明为6元，与106不符必须失败；只有完整粘贴前序票据后才能验证跨票总额。
- 复试直选金额口径：`358/357/378复试直选一倍` 每个号码按“复式10元＋组选2元”计12元，三组共36元；解析结果保留金额分解元数据，避免误显示为普通直选6元。
- 完整106样本回归：福彩直组32＋体彩直组38＋三组复试直选36＝106.00；用户端生成请求增加递增序号，较旧的异步预览响应不得覆盖最新106元结果。
- 金额单位回归：`54注直2角合10.8` 按54×0.2=10.80元解析；“角/毛”金额必须在所有号码列表顺序中保持小数，不得走默认2元或整数四舍五入。
- 用户样本回归：328个三位号码后先写 `🈴328米`、再写 `福直一米` 的顺序也已支持；合计行与玩法行顺序不再影响识别，结果为直选328注、328.00元。
- 严格拒绝回归：9个号码 `直组各2米` 实际应为36元；若用户标注 `计40米`，必须返回金额不一致，不能显示2元或猜测缺少号码。
- 用户端人工确认：金额不一致时下方显示“检测到多条语句的总金额对不上，请进行人工确认”，失败行前提供确认勾；点击后只将合计40改为建议36并重新调用解析，顶部输入与结果同步为36，未确认前禁止下注。
- 用户样本回归：`872.852各一直一组一元 / 286组一元 / 福🈴5米` 支持玩法、号码、彩种、合计分散在不同位置，结果为2+2+1=5.00元。
- 用户样本回归：长列表 `共99注组直各0.2米99*0.4=39.6` 不再生成217.8元；按99个号码的直/组各0.2元计算为19.80+19.80=39.60元。
- 用户样本回归：`共99注组直各0.2米99*0.4=39.6` 已加入严格算式回归；旧兼容层不再接管该格式，预览与下注均使用39.60元快照。
- 用户样本回归：单独 `福` 前缀后接三行共18个号码、末行 `三单两组共180米` 时优先整票编译；输出直选108、组三36、组六36，总额180.00元。
- 用户样本回归：`福直组各1米共40注` 后接10行共40个号码并以 `🈴80` 结束时，玩法首行正确作用于全部号码；输出直选40元、组六29元、组三11元，总额80.00元。
- 金额单位回归：上述 `各1米` 的每注金额固定为1元，不受彩种默认单注2元影响；任何展示或下注路径都不得把组选部分改成2元。
- 用户样本回归：`福彩514.504.534.201.801.154.514.415.120直组各1元……18` 支持省略号分隔的尾部总额，9个号码按直选9元＋组六9元解析，总额18.00元。
- 用户样本回归：长号码列表末尾使用 `共355注福一米直`、`福一米直共355注` 或省略注数时，金额在玩法前且无空格也能正确识别；355不再作为号码、`一米`不再生成001，输出355注直选共355.00元。
- 用户样本回归：`945福打二单一组`（含尾部6米）按二单4元＋一组2元拆为直4.00、组2.00，保留通用“组”玩法，不擅自改成组六；带备注的 `福体下午茶，249-932-634十单五组，180` 按福体直120＋组60正确计算；`福102注一倍直/合计204` 不再把102算作号码；组三/组六“包”别名统一识别为“全包”。
- 用户样本回归：`945福打二单一组6` 的裸数字尾额与 `6米` 等价，均按直4.00＋组2.00=6.00识别，不再回退成单独4.00元直选。
- 用户样本回归：`福体下午茶…180` 与后续 `23469两倍组三一倍组六`、`180+60=240` 组合时，后票继承福体范围，金额正确为120＋60＋40＋20=240；通用“组”保持为“组”不改写为组六。
- 用户样本回归：`福体23469两倍组三一倍组六` 按福体两彩种拆为组三40＋组六20；“包”支持组三/组六/豹子/对子全包；长号码后置 `福102注一倍直` 的102仅作为注数声明。
- 用户样本回归：号码末尾与下一行玩法之间即使出现 `102` 这样的声明数字，也会结合后续 `102注一倍直` 消歧并剔除，不再把它算成第103个号码；102注体彩直选合计204.00元。
- 用户样本回归：`945福打二单一组`（含尾额6或不含尾额）不再只返回4元直选；因“一组”未明确组三/组六，统一提示用户确认，避免静默少算2元或误判组六。
- 用户样本回归：`116+30+30+40=216` 算式允许最后一项合并两张胆拖票；五张票总额严格216.00元，最后两张各20.00元。
- 用户样本回归：`福…直组各1元……18` 省略号尾额在重复号码列表中按原始9注计算，直选9元＋组六9元，总额18.00元。
- 用户样本回归：两行共48个号码后接 `福彩单挑1米的 / 合48米`，按单挑直选每注1元解析为48.00元，不产生组选金额。
- 用户样本回归：`02349福组六一倍，合十` 识别为组六五码一倍，组合总额10.00元；中文“十”仅作为合计值。
- 基础玩法回归：`福百123456 / 十012345 / 个123456 / 各100米` 统一识别为三码定位，并在赔率匹配文本中保留百/十/个位选择，不再报“无法唯一匹配赔率”。
- 用户样本回归：重复三段 `福 + 百/十/个位` 定位块共用尾部 `各50合计7350` 时，按三张二码定位票各2450元解析，总额7350.00元。
- 用户样本回归：`福体 / 个位4 / 个位3 / 各300合计1200` 支持单个数字的一码定位，两笔各600元（福彩、体彩各300），总额1200.00元。
- 用户样本回归：`体1475829组6合计800` 支持“组6”别名与显式整张金额，按组六七码总额800元解析，不再报基础组选无法下注。
- 用户样本回归：`福体485。复试各100合计200` 支持三位复式现金金额；福体彩种各100元，总额200.00元，不再误报选号数量不一致。
- 用户样本回归：`一共343注福体直各10合计6860` 中的声明注数不再被当成第344个号码；343个号码按福彩/体彩各10元，合计6860.00元。
- 用户样本回归：同一343注格式改写为 `一共343注直福体各10合计6860` 也可识别；玩法与彩种顺序不再影响严格计价。
- 用户样本回归：`福123456组三各1000米 / 福0123456组六各500米`（以及将“各”改为“共”）按两张独立多码注单计算，总额1500.00元，不再重复生成3000元。
- 展示回归：多码组三/组六不再把展开组合（112、113…）直接显示成下注号码列表；主表/详情按玩法保留 `三123456`、`六0123456` 等原始选号结构，展开组合仅保留在内部字段供结算使用。
- [x] 新增 `BetMultiplierAmount` 枚举作为“倍”本金唯一来源：普通单注2元；复式/其他组码/跨度/和值/大小单双/对子全包/普通组三组六胆拖10元；豹子全包20元；单选组六/组三复式、组六2胆拖、单选全胆拖及组三/组六沾边赖均按码数表定价。
- 倍数表回归：组六单选复式3–8码 `12/48/120/240/420/672`，组三2–8码 `12/36/72/120/180/252/336`，组六2拖2–8码 `4/6/8/10/12/14/16`，单选全胆拖1拖2–9码 `38/74/122/182/254/338/434/542`；组三赖六码已由156纠正为158。
- 严格金额守恒回归：`福907...888一米直组 / 🈴26` 按13个原始号码各一直一组计26元；888保留为用户原文的“组”，不擅自改写成豹子。错误合计48必须失败。
- 行尾金额分票回归：`两单一组.../12`、跨行 `一单一组.../24`、`四码组六3倍0479/30`、`五码组六三倍03479/30` 按独立数字金额行切成四票，最终六条明细合计96.00元。
- 用户样本回归：同一行尾金额分票样本现在固定返回 6 条成功明细，金额 `8/4/12/12/30/30`，总额 96.00 元，`组六选号数量不足` 不再出现。
- [ ] 验收标准：不得出现成功但号码/玩法/金额错误；PHP 解析回归与 test.txt 审计通过后才切换正式下注。

- [x] 完成旧版解析器备份并建立新流水线入口。
- [ ] 迁移彩种、号码、玩法、金额四阶段扫描（独立引擎，分批切换）。
- [ ] 以 test.txt 全量验证并逐类修正。

- [ ] 完成 test.txt 513 个注单块的逐块解析审计，按失败类型修正通用规则。
- [ ] 保持物理行展示与注单语义分组一致，完成金额/玩法/笔数回归。
- [ ] 输出全量测试报告，未通过项继续修正直至清单收敛。
- 当前报告：`findings-test-format-audit.json`；按空行切分后共649个独立票据片段，其中352个完全通过、297个仍有失败项。独立合计行不再强制套用到最后一张票据。
- 已生成汇总报告：`findings-test-format-report.md`（当前540片段，301通过，239失败）。

- [x] 修正“一直一组，福体”多行输入：支持末尾彩种前缀与独立合计行，通用号码分组为直选+组选，避免整单金额校验失败。
- 验证：示例 `371 771 737 306 369一直一组，福体/🈴40` 解析为两条玩法各20元，合计40元。

- [x] 用户端提交分单不再只依赖空行：识别合计行和单双组金额结束语义，自动结束当前注单并分别确认提交。
- 验证：用户端生产构建通过；普通换行继续归入当前注单，终止标记或空行才开始下一张。

- [x] 支持号码多行、无空行连续同一注单的“几单几组/合计”格式；`两单一组`按每号码2单直+1组计算，总额由连续号码行汇总。
- 验证：示例6个号码解析为直选24元+组六12元=36元，独立合计行不再触发“识别不到金额”。

- [x] 恢复彩票下注所需赔率目录 `lottery_odds/lottery_odds_categories`（仅赔率配置，不恢复会员/注单）；用户端支持空行分隔多张注单，提交前逐张确认。
- 验证：赔率明细400条、赔率分类84条；用户端生产构建通过。

- [x] 支持点号分隔号码及“一元单、一元组” shorthand，自动拆分直选与组六；独立“合计”行不再参与号码解析。
- 验证：示例输入拆为6个直选（6元）+6个组六（6元）+45329组六（10元），总计22元。

- [x] 按用户反馈从清理前备份恢复 SaaS 平台管理员 `admins.admin`；未恢复会员、注单、余额或其他业务数据。
- 验证：`admins` 当前保留平台管理员1条，密码为备份中的哈希值，未输出明文凭据。

- [x] 按用户要求从备份恢复 `lotteries` 与 `lottery_histories`，其他业务表保持清空；恢复后站点1条、彩票4条、开奖记录21076条，会员/注单/余额/组织数据为0。
- 验证：仅导入备份中的彩票目录和开奖记录 INSERT 数据，未恢复会员、代理、订单或配置；备份仍保留在 `server/.backup-full-clear-20260829-184544.sql`。

- [x] 按用户确认执行全量业务清理：保留 `sites` 和 `lottery_histories`，其余业务表数据已清空；备份文件为 `server/.backup-full-clear-20260829-184544.sql`。
- 验证：`sites` 保留1条、`lottery_histories` 保留21073条；会员、注单、余额、赔率、配置、组织流水等均为0条。该操作不可逆，恢复需使用备份。

- [x] 会员明细与主页“详”弹窗统一去除三位号码展示前导补0，并在号码后以红色标注玩法（如独胆）。
- 验证：用户端生产构建、UserBusiness PHP lint 通过。
- [x] 分离双飞展示与赔率归类：重复数字输入“77双飞”仍显示用户原始玩法“双飞”，但赔率身份自动取对子。
- 验证：`77 双飞`→对子赔率身份，`37 双飞`→双飞赔率身份；解析展示均保留“双飞”。

- [x] 修复“胆1”顺序匹配并清洗错误未中奖记录：期号 `2608300024` 的独胆1在开奖号码123中应中奖，已补派奖990元及账务流水。

- [x] 支持“组三/组六”写在胆拖前面的格式，如 `福组三胆1拖23各1000米`。
- 验证：组三和组六前缀均识别为1码拖2，金额1000元。

- [x] 补齐跨度/胆拖别名：支持“跨2”以及“1胆拖23”“胆1拖23”等写法。
- 验证：上述写法均能识别为跨度2或1码拖2，PHP lint 通过。
- [x] 无法识别/下注失败提示改为需点击“确认”的弹窗，不再使用自动消失 Toast。
- 验证：用户端构建通过；生成失败、下注失败和无有效内容均显示确认弹窗。

- [x] 修复组三/组六混合顺序输入，支持玩法在号码前后及两种玩法均在号码前的写法；每条输入拆成两条独立玩法明细。
- 验证：示例均按组三、组六各生成一条明细，玩法名称按选号码数动态匹配；主页“详”弹窗按玩法分组展示。

### 本轮：分分彩系统开奖停滞与用户端倒计时（已完成）

- [x] 修复系统彩开奖任务长时间停机后的追赶逻辑，保证始终生成未来待开奖期。
- [x] 核对用户端倒计时/蒙版状态与服务端期号推进结果，并完成构建及只读验证。
- 验证：`php -l app/service/SystemLotteryService.php`、`php think lottery:sync`、用户端 `npm run build` 均通过；数据库中分分彩已存在未来 `is_opened=0` 待开奖期。
- [x] 修复“16胆”被当成三位直选号码的问题，支持多个独胆数字分别计数和计额。
- 验证：`体16胆各500` 解析为号码 `1 6`、2 注、1000 元；已补充解析回归。
- [x] 新增“895一单五组12”混合玩法解析：一单按三码定位，组按组六，三位号码每倍 2 元，非三位号码每倍 10 元。
- 验证：`体895一单五组12` 返回 2 元直选与 10 元组六；`体89一单五组60` 返回 10 元直选与 50 元组六。
- [x] 支持多号码“各四单一组计40”语法，避免末尾总额被当作 040 号码。
- 验证：`福418 403 901 406各四单一组计40` 返回 32 元直选 + 8 元组六，共 40 元。
- [x] 支持“各四十单一组”中文十位倍数，避免归一化后生成 `004` 并错误计为 6 元。
- 验证：`福618 158各四十单一组` 返回 160 元直选 + 4 元组六，共 164 元。
- [x] 对无法安全解析的中文百位倍数（如“四百单”）返回明确错误，阻止生成错误注单。
- 验证：`福618 158各四百单一组` 返回“单双组倍数格式不支持中文百位数”，不生成 `004` 或其他号码。
- [x] 双飞玩法同时识别“飞”和“双飞”写法，并统一映射到双飞赔率与结算。
- 验证：`福09飞各1元` 与 `福09双飞各1元` 均解析为双飞 1 元。
- [x] 双飞两位相同时改按对子赔率；不同数字继续按双飞赔率。组三/组六多码按选号数量映射对应赔率表。
- 验证：`77双飞`→对子、`37飞`→双飞、`12345组三`→组三五码、`123456组六`→组六六码；结算匹配样例通过。
- [x] 清洗历史双飞/对子错误赔率：拆分并修正注单 233、240、246，修正历史赔率记录 125、126，并同步会员余额、账单、提交汇总和组织分账。
- 验证：会员补回 84840 元；233 正确派奖 276000、240 正确派奖 1800；246 待开奖明细分别为 37 双飞16、77 对子30；全程使用事务脚本，未执行真实下注。
- [x] 清洗组三多码赔率错误：注单 238、243 从 组三标准赔率300更正为对应组三五码赔率15，派奖及账务同步修正；注单245的组六四码赔率修正为37。
- [x] 动态支持组三/组六按选号数量映射到两码至全包（含10位全包），并修复混合定位输入拆分。
- 验证：238/243 每笔派奖改为1500元，会员账务净调整-57000元；`0123456789组三/组六` 映射全包赔率。
- [x] 修复无“各”后缀的多码组选输入（如 `福34组三100米`），现在按选号数量映射组三两码/三组六码等对应赔率。
- 验证：`福34组三100米` 识别为组三两码；`福345678组三100米` 识别为组三六码。
- [x] 继续清洗历史普通组三误派奖：注单215（9码组三）由300赔率更正为组三九码4.166，派奖由300000降至4166，并同步会员32余额、账单、提交汇总及组织流水。
- 验证：注单215及明细182目标值已更新，会员32余额扣回295834元，重复执行脚本会因目标值变化而停止。
- [x] 用户端“详”弹窗和下注明细表将内部前缀 `三/六` 作为组三/组六标记处理，号码栏只显示实际选号（如 `34567`），玩法栏显示组3/组6。
- 验证：用户端生产构建通过。
- [x] 超过单注/单项上限时不再静默截断金额；下注接口返回具体玩法、上限和当前输入金额的提示，并阻止整单提交。
- 验证：PHP lint 通过；超限请求不会写入注单或扣除余额，用户需修改金额后重新提交。
- [x] 修正待开奖注单249（`福34567组三100米`）锁定赔率：普通组三300改为组三五码15，金额保持100元，无派奖/余额变动。
- [x] 修复代理端会员编辑页缺少“下注（can_bet）”开关的问题，并完成代理端生产构建发布。
- 验证：代理端 `npm run build` 通过；未执行真实下注或直接修改会员授权数据。
- [x] 修复定位复式中“个”与金额前缀冲突，避免 `个567890` 被解析成巨额下注。
- 验证：针对 `福体百1234567十7654321个567890各1米` 解析为 588 注、588 元；无金额版本正确提示缺少金额。
- [x] 修复系统彩调整开奖间隔后期号编码冲突导致无待开奖期、前端显示“时间待定”的问题。
- 验证：分分彩间隔改为 1800 秒后已生成 `is_opened=0` 的未来期 `2608291355`（`23:04:43`），PHP lint 和同步命令通过。

- [x] 抓取并归档线上用户端构建产物、SVG、登录页、验证码与认证后路由资料。
- [x] 确认 MySQL `192.168.2.18:3306` 与 Redis `192.168.2.18:6379` 网络可达。
- [x] 建立 monorepo、管理端、用户端和 ThinkPHP API 骨架。
- [x] 建立多租户、一级/二级代理、站点、域名和 RBAC 数据模型。
- [x] 建立数据库迁移、升级状态和初始化数据。
- [x] 完成管理端登录、动态路由、左侧菜单和固定滚动工作区。
- [x] 完成代理、站点、域名、角色和菜单管理页面。

### 本轮：按参考站源码对齐快录“详”弹窗（已完成）

- [x] 从参考站生产包确认弹窗标题、全视口宽度、四组“号码 / 金额”表格及重复号码标记结构。
- [x] 快录详情改为按“福 / 体”和玩法分组，每行最多横排四注，空位补 `--`。
- [x] 重复号码显示“(N次重复)”并累计该号码金额；标题同步显示总笔数和总金额。
- [x] 按参考站 `calc(100vw / 50)` 根字号体系使用 rem/flex，对手机与宽屏共用同一布局规则。
- [x] 完成 lint、生产构建和线上静态资源校验。
- 验证：lint 0 errors（保留既有 15 条 warnings）；生产构建发布 `index-BpMRbZiM.css` / `index-BmVRVIWb.js`，线上 HTML 已引用该资源，JS/CSS 包含详情标题、重复标记和独立弹窗样式。
- [x] 完成用户端登录、验证码、责任声明和主布局。
- [ ] 逐步实现快速录入、明细、账单、会员资料、开奖记录与规则页面。
- [ ] 完成 Redis 会话、验证码和缓存支持。（配置已接入；当前 PHP 构建未包含 redis 扩展，默认文件缓存）
- [x] 完成前后端联调、测试、构建与启动脚本。
- [x] 修复带连字符资源的 ThinkPHP 路由，二级代理创建与列表已联调。
- [x] API 异常已统一为 JSON 错误响应，避免调试 HTML 泄露到前端。
- [x] 修复管理端登录对 API envelope 层级变化的兼容性，避免读取 `null.token`。
- [x] 将站点与域名合并为“代理中心”，站点记录归属代理并在列表中聚合域名。
- [x] 二级代理创建校验必须绑定有效一级代理；平台导航隐藏独立站点/域名菜单。
- [x] 收敛为唯一“代理中心”菜单；站点包含独立管理员账号、密码、手机号和反代域名字段。
- [x] 代理中心支持“进入站点”跳转到站点反代域名，管理员密码仅保存哈希且不回传。
- [x] 数据库升级 `202608180002 AddSiteManagerFields` 已执行。
- [x] 用户增加 `site_id` 站点范围，站点管理员创建时同步建立站点用户，登录支持站点范围校验（`202608180003`）。
- [x] 用户端登录页恢复线上快照中的 `login-logo.svg`，不再使用文字占位图标。
- [x] 用户端错误提示改为 Toast，并拆出 `Toast`、`CaptchaModal` 组件，避免登录交互继续堆在单一页面结构中。
- [x] 用户端登录重写为 Ant Design 组件，使用 `message.error` 自动消失提示、`Input`、`Modal`、`Button` 和图标组件；账号密码输入框移除占位符。
- [x] 对照参考截图调整登录前缀图标、电源图标、验证码数字按钮颜色与弹窗 footer 按钮位置。
- [x] 完善验证码交互：补充分割线、两位长度限制、固定数字顺序、圆角卡片和无光效悬浮/点击状态；电源入口使用原生按钮。
- [x] 验证码数字改为每次打开随机洗牌，并使用专用按钮样式强制覆盖 Ant Design 默认 hover/focus 光效。
- [x] 优化验证码布局分割线、答案框宽度与 footer 登录按钮间距。
- [x] 按参考站实测参数将原生登录按钮调整为 36×36px、24px 图标，并按需求改为黑色边框。
- [x] 验证码确认后始终关闭验证码弹窗；错误改用 Ant Design 错误对话框显示“验证码已失效”。
- [x] 接入真实算术验证码接口：服务端随机生成 0-18 以内算式，混合阿拉伯/中文数字，返回带噪点干扰线的 SVG 图片并缓存答案；前端提交验证码 ID 进行校验。
- [x] 放大验证码 SVG 内部算式字符，保持图片框尺寸不变并提升可读性。
- [x] 修复认证响应校验：只有验证码验证成功、登录响应 `code=0` 且返回 token 时才允许进入用户端。
- [x] 区分验证码错误与验证码失效：答案不匹配返回“验证码错误”，缓存不存在/过期返回“验证码已失效”。
- [x] 登录成功后增加责任声明协议页；同意后进入主界面，不同意则清理登录状态并退出。
- [x] 按线上协议页快照修正宽屏卡片宽度、正文内边距、提醒区和底部按钮布局。
- [x] 用户端首页导航接入已拉取的 SVG 图标资源，登录鲸鱼复用 `login-logo.svg`，并补齐选中/未选中样式。
- [x] 首页顶部公告接入站点动态配置，支持跑马灯、悬浮下划线和点击公告信息弹窗；管理端系统配置可编辑公告。
- [x] 按线上首页实测将导航 SVG 调整为 25px，外层圆形边框 38px，并对齐选中项边框与导航项尺寸。
- [x] 按首页参考图修正头部层次、蓝色鲸鱼标识、四项账户 outlined 字段和金额整数显示。
- [x] 首页头部导航改为从彩票列表右侧靠左排列，保持统一间距。
- [x] 放慢公告跑马灯，并将总金额、更多、居右移到左侧余额栏，快速录入区不再重复显示总金额。
- [x] 压低彩票列表并修正头部内边距，主题区增加 5px 顶部间距，总金额按钮增加间隔并接入 `arrow-right.svg`。
- [x] 按线上 DOM 实测重写彩票列表两行结构，外框、行高、虚线、字号和列布局与快速录入页一致。
- [x] 按线上工具栏 DOM 修正左栏宽度 250px、工具栏 40px、按钮组右对齐及按钮尺寸。
- [x] 实现“居右”面板切换：左右交换主区域和余额栏，按钮可切回“居左”。
- [x] 快速录入选项改为 Ant Design Switch，按线上尺寸和颜色实现，并接入悬浮 Tooltip 说明。
- [x] 快速录入开关和默认彩种外框改为账号字段同款浮动 label outlined 样式。
- [x] 修正快速录入区域层级：白色选项区域承载多个独立 outlined 模块，统一内容居中并增加福彩 3D 与排列三间距。
- [x] 修正快速录入选项区域宽度和上下留白，使其与输入区域左右对齐且不再紧贴相邻模块。
- [x] 优化快速录入卡片细节：增加轻微圆角、上下内边距、卡片内部留白及首个卡片左侧间距。
- [x] 对齐首页业务颜色：已用红色、信用和操作按钮金黄色、可用绿色，并放大默认彩种文字。
- [x] 修正默认彩种卡片按钮被压缩换行的问题，固定按钮不换行并横向并排展示。
- [x] 将“＋新标签”固定到替换模块右上角，与替换输入行保持同一水平位置。
- [x] 按线上结构将替换模块拆成输入行与新标签行，中间增加虚线分隔并把“＋新标签”放到下方右侧。
- [x] 为快速录入文本框增加线上样式的清空/字数统计底栏，限制最多输入 10,000 个字符。
- [x] 将右下角温馨提示重构为可展开消息面板，补齐标题栏、正文、圆形收起与关闭按钮。
- [x] 将快速录入“规则说明”接入独立 Ant Design 弹窗组件，增加规则页签、滚动正文和重点提示样式。
- [x] 修正规则说明弹窗的 flex 高度链路，使页签正文区域能够独立滚动且不被 footer 截断。
- [x] 根据浏览器实际 DOM 将滚动样式适配到 Ant Design 当前版本的 `.ant-tabs-body-holder`，并补齐会产生真实溢出的规则正文。
- [x] 规则说明改为站点级配置：管理端系统配置新增四类规则编辑，后端保存规则设置，用户端弹窗读取对应站点内容。
- [x] 实现左侧“更多”查询视图，按线上布局切换日期筛选、原始文本搜索、搜索/返回按钮和总金额展示。
- [x] 将快速录入页的“投注记录”页签改为独立记录查询表格，补齐筛选、时间范围、导出和空数据状态。
- [x] 补齐“停押降水”页签：查号码、类型、彩种、分类、日期、排序和数据表格。
- [x] 对齐快速录入交互：新增“添加新标签”弹窗、标签展示和文本替换功能。
- [x] 按线上截图重做添加新标签弹窗：500px 宽度、蓝色输入框、分隔 footer、橙色关闭和深蓝确认按钮。
- [x] 创建用户业务数据表并接入查询 API：投注记录、投注明细、历史账单和开奖数据，按站点/用户隔离并完成数据库迁移。
- [x] 投注记录页接入真实 `/user/bet-records` 查询，支持中奖状态、日期和原始文本筛选并渲染返回记录。
- [x] 将下注明细、历史账单、开奖号码、会员资料和修改密码导航接入独立页面组件；前三者读取真实业务查询接口。
- [x] 修改密码页面接入用户密码修改接口，完成原密码、复杂度、确认密码校验。
- [x] 完成线上用户端全导航审查：快速录入、下注明细、历史账单、会员资料、开奖号码、规则说明、修改密码及更多/停押降水功能结构已记录。
- [x] 将投注记录起止时间框改为整块可点击并调用浏览器日期时间选择器。
- [x] 按线上样式将投注时间起止值合并到同一个 outlined 框，并改用 Ant Design 搜索/导出图标。
- [x] 投注记录中奖筛选补齐“全部 / 未中奖 / 已中奖”三个选项。
- [x] 站点免密进入的 `auto_token` 也纳入正式登录状态流，进入后先显示责任声明协议。
- [x] 代理中心新增/编辑改为右侧抽屉，编辑动作调用站点更新接口。
- [x] 修复代理中心管理员字段显示英文数据库名的问题，补齐中文列标签。
- [x] 修复代理中心保存携带旧 `parent_id` 字段导致 SQL 错误，并完善站点编辑时域名与密码处理。
- [x] 代理中心移除站点编码展示与表单字段；无反代域名时进入站点会明确提示。
- [x] 增加站点用户管理菜单、站点筛选、创建和逻辑删除接口。
- [x] 调整管理表格颜色对比度，避免表头和内容灰度过低。
- [x] 通过右侧浏览器实测代理中心：无残留联调数据、站点配置抽屉显示反代域名、站点用户页面和菜单可访问。
- [x] 代理中心搜索提示和新增按钮改为站点业务语言，不再显示编码概念。
- [x] 修复操作列按钮挤压：固定 230px 宽度、横向不换行按钮组；并在右侧浏览器真实保存验证通过。
- [x] 进入站点前校验反代域名，URL 构造异常改为中文提示，不再显示浏览器英文异常。
- [x] 完成平台站点用户管理：列表、站点 ID 筛选、新增、编辑入口和逻辑删除；已在右侧浏览器真实创建验证。
- [x] 站点用户筛选和编辑改为站点下拉框，显示“站点名称（当前站点）”；右侧浏览器已验证选项加载。
- [x] 修复站点废弃 `code` 字段仍为必填导致保存失败，迁移 `202608180005` 后在右侧浏览器真实新增并收到“站点创建成功”。
- [x] 增加站点管理员一键进入站点接口：生成一次性用户 token，带反代域名跳转并自动登录。
- [x] 拆分平台管理员、站点管理员、站点用户表；用户名唯一范围改为平台全局或站点内，站点及账号统一逻辑删除（`202608180004`）。
- [x] 验证两个不同站点可创建同名站点管理员，平台管理员登录正常。
- [x] 平台自有站点纳入站点模型（`202608180006`）；平台用户管理可选择全部站点及平台自有站点，站点管理员不显示站点选择且服务端强制按其所属站点读写。
- [x] 站点用户列表改为展示所属站点名称，不再暴露站点 ID；浏览器已验证筛选和新增抽屉的纯名称选项。
- [x] 责任声明改为站点级系统配置：标题和多行正文可在管理端保存，用户端登录后按令牌所属站点读取（`202608180007`）。
- [x] 责任声明正文改为受限 Markdown：后台保存原始 Markdown，用户端支持标题、加粗、列表、引用和表格，HTML 不解析。
- [x] 修复用户端协议状态刷新丢失：未点击同意时刷新仍停留协议页，已同意状态按当前登录令牌记录。
- [x] 责任声明改为 Vue 3 富文本编辑器：支持选中文字设置颜色、加粗、字号、对齐、列表等，保存 HTML 并在用户端安全还原样式。
- [x] 修复用户端 CSS 覆盖富文本颜色：`strong` 改为继承外层选区颜色，不再强制覆盖为黑色。
- [x] 增加站点用户余额模型和展示：余额、信用余额、已用余额持久化，总余额与可用余额由后端计算，管理端可编辑，用户端顶部显示。
- [x] 系统配置按功能拆分为“责任声明 / 首页公告” tabs，切换后只显示当前功能对应的表单和保存按钮；页面固定在可视高度内，正文输入区域内部滚动。
- [x] 修复用户端公告请求未携带登录令牌导致接口 401 并回退旧固定文案；现在按当前用户站点读取管理端保存的公告。
- [x] 用户端请求层按 `utils/request`、`api/auth`、`api/user` 分层，增加全局请求拦截器自动注入令牌、全局响应拦截器统一处理业务错误和 401 退出。
- [x] 新增 React 代理端首版框架：按线上实测绿色主题复刻登录页、海豚 SVG、真实算术验证码、代理主页面布局、独立请求拦截器与 agent API/token scope；数据库升级 `202608180009` 已执行。
- [x] 修复本地代理站点进入与登录：允许 `localhost`/本地 IP，自动使用本地 HTTP 和 5176 端口；站点编辑同步 `site_admins`，代理登录兼容站点管理员账号及平台自有站点。
- [x] 按线上代理端实测重排代理首页：顶部公告、Logo/单一代理账号框、12 项绿色导航图标、选中态、彩种状态条分区显示；左侧不再显示会员数量、额度或汇总信息。
- [x] 代理账号区按线上最终模型显示单个 130×40 浮动标签框：“账号”压在上边框，框内显示“代理：真实账号名”。
- [x] 代理账号框改为按用户名自适应宽度，并增加超长账号省略保护。
- [x] 按线上截图实现代理端“总货概览”：位置面包屑、五个查询页签、账号/来源筛选、日期区间、明细表头和暂无数据状态；页签、下拉框和筛选输入均可操作。
- [x] 系统配置规则说明四个页签改为 WangEditor 富文本编辑器；旧纯文本自动转段落 HTML，用户端清洗并渲染富文本格式。
- [x] 代理端总货概览查询区按线上结构调整为三个带边框字段组，提交按钮改为外层框加内层红色描边按钮。
- [x] 管理端新增按站点隔离的“代理端协议”和“代理端公告”配置；代理端登录后读取服务协议，确认后进入主页并读取公告；修复进入站点路由被列表路由截获的问题。
- [x] 代理端服务协议改为独立深绿色主题，同意按钮同步为绿色，不再沿用用户端蓝色背景。
- [x] 修复首页公告和代理端公告正文覆盖保存按钮：取消 textarea 固定视口高度，正文区域弹性填充并内部滚动，底部保存栏固定可见。
- [x] 修复代理端协议回退到用户责任声明：后端和前端均使用独立代理协议默认值，无效代理 token 返回 401；浏览器验证标题和正文来自代理端配置。
- [x] 拆分站点域名用途：`domains.domain_type=agent/user`；平台进入站点只取代理端域名，代理登录只接受代理端域名，用户登录只接受用户端域名；代理中心表单分别配置两类域名。
- [x] 新增代理端规则说明页面：代理端通过 `/agent/rules` 读取当前站点配置，支持基础玩法、特殊打法、总金额、文本规范四个页签；富文本正文安全清洗并保留颜色/加粗/字号，正文区域独立滚动；已在右侧浏览器打开并验证页签切换。
- [x] 新增平台彩票目录和站点分配：迁移 `202608190005` 创建 `lotteries`、`site_lotteries`，管理端新增“彩票列表”页面，可新增、编辑、停用、删除和按站点分配；用户端/代理端只读取当前站点已分配的彩票，默认彩票已为现有站点初始化分配。
- [x] 新增开奖接口配置和开奖历史：系统配置增加开奖 Base URL；彩票列表增加“开奖历史”页面；后端按彩票编码映射第三方 `type`，首次打开历史页最多同步 10 页、每页最多 1000 条，已存在期号跳过并支持分页查询；迁移 `202608190006` 创建 `lottery_histories`。
- [x] 拆分管理端“系统配置”和“站点配置”：`/settings` 只维护全平台开奖接口 Base URL；`/site-settings` 保留按站点选择的责任声明、公告、协议和规则配置；侧边栏新增站点配置入口并已在右侧浏览器验证。
- [x] 系统配置开奖接口增加连通性测试：请求 `pls` 最小数据页，展示接口可用状态、HTTP 状态和毫秒响应时间；右侧浏览器实测返回 HTTP 200、响应约 450ms。
- [x] 站点配置增加参考站点填充：选择其他站点后可一次性填入责任声明、公告、代理协议、代理公告和规则内容；仅修改当前表单，不自动保存，已增加提示并完成构建验证。
- [x] 重构开奖历史同步：历史查询接口只读取数据库；新增 `lottery:sync` 后台命令，每分钟为每个启用彩票仅拉取最新 1 条并按期号去重；历史页每分钟静默刷新数据库结果，新期号直接出现在第一页首行，不显示加载动画。
- [x] 执行排列三一次性历史回填：`lottery:backfill 2 10 1000` 请求 10 页且无失败页；第三方将请求的 1000 强制限制为每页 500，实际接收 5000 条，新增 4999、更新 1，数据库总数 5000；日常同步策略保持每分钟 1 条不变。
- [x] 执行福彩3D第1至10页、每页10条的一次性回填：首次请求遇第三方短期限流，增加逐页间隔和最多3次重试后重新执行；100条全部处理成功、失败页0，因期号均已存在所以新增0、更新100，数据库总数保持500。
- [x] 按限流要求重新执行福彩3D回填：第1至10页、每页10条，每页间隔10秒；100条全部成功，失败页0，新增0、更新100。
- [x] 全量回填福彩3D和排列三：按接口实际总页数16页、每页500条、页间等待10秒串行执行；福彩3D收到7727条（新增7227、更新500），排列三收到7695条（新增2695、更新5000），两者失败页均为0；完成后恢复每分钟仅同步最新1条。
- [x] 彩票列表新增赔率详情：迁移 `202608190007/008` 创建并扩展彩票赔率表，支持类别、赔率项、最小下注、赔率上限、单注上限、单项上限、赔率、离线赚水、启停和排序；两个彩种初始化10个参考赔率项，新增/编辑/删除接口和抽屉已在浏览器验证。
- [x] 按参考代理站补充三位数彩票默认赔率：福彩3D和排列三各写入26项独立配置，包含定位、独胆、双飞、对子、直选、组三两码至全包、组六四码至九码等；浏览器验证两边均为26项且关键赔率项存在。
- [x] 赔率详情改为后端分页查询：默认每页20条，支持10/20/50/100切换；新增后回第一页，编辑和删除保持当前页，删除当前页最后一条时自动回上一页；浏览器验证总数26、第一页20条、第二页6条。

## 本轮执行步骤

1. 创建工程规范与问题记录文件。
2. 在外接盘 `.runtime` 中安装独立 PHP 和 Composer。
3. 创建 Vue 管理端与 React 用户端。
4. 创建 ThinkPHP API 和 `.env` 配置。
5. 创建并执行第一版 MySQL 迁移。
6. 安装依赖、启动服务并验证健康检查与前端页面。

## 验证记录

- Node.js：v22.22.1
- npm：11.12.1
- 系统：macOS ARM64
- PHP：8.3.33，外接盘 `.runtime/php/php`，已验证 mysqli/PDO/MySQL/mbstring/openssl/zip
- MySQL 端口：已连通
- Redis 端口：已连通
- 数据库迁移：`202608180001 CreateSaasTables` 已执行
- API 健康检查：`GET /api/v1/health` 返回 `database=ok`
- 前端构建：管理端、用户端、代理端均通过 `npm run build`
- 用户端 Toast：空账号提交显示右上角错误 Toast，验证码弹窗通过独立组件渲染。
- 浏览器验证：Playwright 已验证管理端登录、用户端验证码登录和快速录入主界面
- 二级代理接口：创建返回 `code=0`，列表返回 `total=1`
- PHP 语法检查：`server/app`、`server/config`、`server/route` 全部通过

## 下一步

### 本轮：快速录入多行同批次展示（已完成）

- [x] 实现批次元数据与多行同批次展示：同一合并订单仅末行显示“合/详”和整批笔数、金额；前置物理行不显示详、笔数、金额；使用不同背景区分批次角色。
- [x] 完成解析与 UI 回归：43 个物理行归属同一批次，仅显示 1 个“合”、1 个“详”和 1 组末行汇总；结果为 429 个唯一码、430 次投注、总金额 25800，详情中重复号码 `668` 聚合金额为 120。
- [x] 完成 PHP、TypeScript、lint 和 build 检查；批次展示、详情金额聚合与拦货 occurrence 回归均通过。
- [x] 使用验证通过的构建产物覆盖服务器 `dist`，构建资源和页面请求均返回 HTTP 200。
- [x] 完成浏览器验证：批次前置行使用白色实线分隔，“福”标识为 `#7a2bc3`、末行背景为 `#a8a7e1`；参考字体保持 `Microsoft YaHei / Tahoma / Helvetica / Arial`、字重 400，并确认全程未执行真实下注。

### 本轮补充：批次尾缀规范化、混合输入与编辑失效处理（已完成）

- [x] 修复 `bet_submissions` 事务闭包漏捕获 `$count` 导致的 `Undefined variable $count`；已通过 PHP 语法检查，未执行真实下注。
- [x] 批次解析按明确 `N注 + 玩法 + 金额` 标记动态扫描，不写死号码或注数；支持同一粘贴中的多个批次、普通行与批次混合、尾缀独立一行、中文/英文标点及“直选”别名。
- [x] 无明确批次标记时保留物理行独立解析，不自动合并；替换旧号码后即使尾缀注数未更新，也保留物理换行并返回 mismatch 元数据。
- [x] 结果行新增、删除或编辑时清理旧 `batch_*` 元数据，避免重新生成前继续显示陈旧末行汇总和操作按钮。
- [x] `QuickEntryParserTest`、`InterceptionAllocatorTest`、PHP lint 及用户端生产构建均通过。

### 本轮：代理端四个明细页面与功能（暂停/待续）

- [ ] 盘点参考站“总货明细 / 中奖明细 / 投注明细 / 查看退码”四页的筛选字段、表格列、分页与交互。（暂停，待续）
- [ ] 审计并补齐代理端真实查询 API，严格按当前代理站点隔离。
- [ ] 实现四个 React 页面、共享查询组件、导航切换、加载/空状态和错误处理。
- [ ] 完成 PHP 语法、代理端 lint/build、API 回归和内置浏览器逐页验证。
- [ ] 更新文件化计划、问题记录和交付说明。

### 本轮纠正：彩票状态条与下方导航存在 2px 横向缝隙（已完成）

- [x] 按用户截图清除概览页顶部 2px 内边距，使彩票状态条与下方绿色导航直接贴合。
- [x] 代理端 oxlint 通过（0 错误，仅保留 Agreement.tsx 既有 2 条 Fast Refresh 警告），Vite 生产构建成功并发布。
- [x] 内置浏览器实测彩票状态条 bottom=180、下方导航 top=180、间距=0；截图确认无横向白缝，控制台无报错且无横向溢出。
- [x] 将本次用户纠正追加到 PROBLEM_LOG.md。

### 本轮纠正：彩票项透明左边框形成白线（已完成）

- [x] 修正上一轮误判，移除未选中彩票项会透出白底的透明左边框，并用选中项 1px 绿色外阴影覆盖渲染接缝。
- [x] 代理端 oxlint 通过（0 错误，仅保留 Agreement.tsx 既有 2 条 Fast Refresh 警告），Vite 生产构建成功并发布。
- [x] 内置浏览器放大截图与像素复核：交界 `x=196-202` 保持绿色，白底从 `x=203` 才开始；控制台无报错且无横向溢出。
- [x] 将本次用户二次纠正追加到 PROBLEM_LOG.md。

### 本轮补充：彩票项交界白线清理（已完成）

- [x] 按用户截图将选中彩种右边缘改为 1px 绿色分隔线，清理与下一彩种交界处的竖向白线，同时保留底部白色虚线。
- [x] 代理端 oxlint 通过（0 错误，仅保留 Agreement.tsx 既有 2 条 Fast Refresh 警告），Vite 生产构建成功并发布。
- [x] 内置浏览器实测选中项 `border-right: 1px solid rgb(0,100,0)`、`border-bottom: 1px dashed rgb(255,255,255)`；控制台无报错且无横向溢出。
- [x] 将本次用户纠正追加到 PROBLEM_LOG.md。

### 本轮补充：彩票状态条选中项底部虚线（已完成）

- [x] 对比参考站彩票状态条的真实边框样式，补齐选中项底部 1px 白色虚线与状态条四周 2px 绿色边框。
- [x] 代理端 oxlint 通过（0 错误，仅保留 Agreement.tsx 既有 2 条 Fast Refresh 警告），Vite 生产构建成功并发布。
- [x] 内置浏览器 1567px 视口实测：状态条 `x=0/y=130/1567×50px`，选中项 `x=2/y=132/200×46px`，底边为 `1px dashed rgb(255,255,255)`；控制台无报错且无横向溢出。
- [x] 将本次用户纠正追加到 PROBLEM_LOG.md，记录防复发检查。

### 本轮补充：总货概览筛选栏内部控件精确对齐（已完成）

- [x] 按用户浏览器标注复核筛选栏内部结构，改用参考站的浮动 label 容器并对齐输入框、下拉框和提交按钮。
- [x] 代理端 oxlint 通过（0 错误，仅保留 Agreement.tsx 既有 2 条 Fast Refresh 警告），Vite 生产构建成功并发布。
- [x] 内置浏览器 1567px 视口实测：字段框 x=578.5/758.5/878.5、输入框 x=588.5/y=263.5/150×24、来源框 x=768.5/y=265.5/70×20、提交按钮 x=913.5/y=261.5/50×28；输入字号 14px，控制台无报错且无横向溢出。
- [x] 将本次用户纠正追加到 PROBLEM_LOG.md，记录防复发检查。

### 本轮：代理端总货概览精确复刻参考站（已完成）

- [x] 对比参考站与当前站的 DOM、计算样式、坐标和可见交互，修正顶部信息区、导航、彩票栏与概览筛选表格。
- [x] 将来源选项、15 日动态日期范围和可展开温馨提示结构对齐参考站，保留当前动态账号、彩票与公告数据。
- [x] 代理端 oxlint 通过（仅保留 Agreement.tsx 既有 Fast Refresh 警告）；Vite 生产构建通过并发布，内置浏览器复核控制台无错误、无横向溢出。
- [x] 1567px 视口实测：Logo x=28、账号框 x=195、导航 x=400、筛选区 x=10/y=233、表格标题 x=25/y=325，与参考站关键坐标一致；温馨提示展开/收起高度为 200/40px。

### 本轮：代理端按站点动态获取彩票（已完成）

- [x] 收紧代理端彩票接口，只返回当前登录站点已分配且启用的彩票，并附带最新期号信息。
- [x] 代理端彩票栏改为首次加载、窗口聚焦和每分钟静默刷新，移除固定期号与倒计时数据。
- [x] PHP 语法和代理端构建通过；数据库分配、真实下一期号、浏览器动态展示及控制台无错误均已验证。

### 本轮补充：代理端彩种选中态（已完成）

- [x] 彩种栏每个动态彩种改为可点击选项，仅当前选中项显示绿色背景。
- [x] 刷新分配列表时保留有效选中项，被取消分配后自动选择剩余彩种。
- [x] 代理端构建通过；浏览器点击与刷新验证选中态正确切换并保留，控制台无错误。

### 本轮：彩票权限调整为站点配置（已完成）

- [x] 将彩票可见范围从“彩票列表分配站点”移动到“代理中心站点配置”。
- [x] 站点抽屉支持选择该站点可见的彩票，保存时在事务中覆盖站点分配，空选择代表全部不可见。
- [x] 彩票列表已移除分配控件；PHP 语法和管理端构建通过，浏览器验证站点配置回显、保存及代理端动态隐藏福彩3D。

### 本轮：代理端真实开奖倒计时布局（已完成）

- [x] 彩种块调整为上排“彩种名称 / 下期期号”、下排“开盘状态 / 真实倒计时”。
- [x] 按 `next_open_time` 判断开奖日凌晨至开奖前为“开盘中”，开奖日前为“未开盘”，开奖后为“已封盘”。
- [x] 代理端构建通过；浏览器核对字段位置、两秒连续递减、选中样式及控制台无错误。

### 本轮：代理端彩票请求失败静默处理（已完成）

- [x] 移除“彩票列表加载失败”弹窗；首次加载失败仅显示空状态，后台静默刷新失败保留当前彩种，不清空页面。
- [x] 代理端 TypeScript 构建通过。

### 本轮：代理端下级管理（已完成）

- [x] 新增代理范围的下级会员列表、新增和编辑接口，严格按当前登录站点隔离。
- [x] 按参考页实现信用额度汇总、筛选、新增会员、列表、分页和操作列。
- [x] 保留代理下与代理账号同名的真实会员，列表和信用额度汇总按站点会员表完整统计。
- [x] 按参考站实测尺寸重写筛选栏：字段框、输入框、状态选择、搜索和新增按钮位置及视觉参数一致。
- [x] 按参考站重做下级管理面包屑：双右箭头、路径文字、右侧页签及虚线分隔均使用实测尺寸。
- [x] PHP 语法和代理端构建通过；浏览器验证筛选、空状态、新增页面、分页及操作列布局正常。
- [x] 参考线上“新增下级”实现独立新增/编辑会员页面，包含账号等级、账号密码和动态彩种权限矩阵。
- [x] 新增会员彩种权限数据库升级 `202608190012`；创建、编辑、详情回显均按当前代理站点隔离。
- [x] 用户端彩票可见范围和快速下注均执行服务端权限校验；关闭彩种权限会同步关闭下注权限。
- [x] 浏览器验证新增页面、权限开关联动、编辑入口和返回导航；未创建测试会员、未修改现有会员权限。
- [x] 按参考站拆分独立“修改账号”页面：账号状态、密码、代号/备注、信用额度、拦货占成、逐彩种权限、离线赚水及会员赔率覆盖。
- [x] 新增会员账号配置迁移 `202608190013`，后端详情/更新接口支持上述字段和赔率覆盖，数据库升级状态为 `up`。
- [x] 浏览器实测 `zhang10867779` 修改页：真实回显两种彩票权限和 52 条赔率行；修复代理主题溢出隐藏，底部提交/返回按钮可滚动到；未提交表单。
- [x] 按参考站截图继续校准修改页：内容宽度调整为 1045px，基础资料/密码/权限/额度/拦货/平均赚水分区重排；赔率表改为除离线赚水外全部纯文本只读，表头和数字格式与参考站一致。
- [x] 根据最新截图修正主内容和信用额度尺寸：恢复参考站 `1200px` 主宽度，信用额度内框调整为 `1062px`，输入框 `86px`、归零按钮 `46×28px`，文字保持单行；浏览器实测通过。
- [x] 按最新参考截图调整拦货占成和操作区：拦货内框、黄色提示条、下拉框按参考站重排；平均离线赚水、提交、返回合并为 30px 同行区域，按钮位置和字号已在浏览器复核。
- [x] 根据最新参考截图纠正容器尺寸：修改页主容器恢复为 `1200px`，信用额度/拦货内框为 `1062px`，账号区 88px、密码权限区约 131px、拦货内框 97px；浏览器实测与参考图一致。
- [x] 使用内置浏览器同时接管参考页和本地页读取 computed style，按真实结构重做：摘要 44px、下间距 20px、资料区 88px、密码权限区 131px、额度外层 78px、拦货外层 125px、平均赚水 30px、表头 40px；同步修正内部虚线分隔、密码文案、额度数值和按钮。
- [x] 按参考站重做修改页下半部赔率表：8 列改为 7 列精确列宽，合并两个同玩法彩种为一套 26 项配置，增加 6 个类别分组行、定位玩法简称、同值同步开关和 600px 内部滚动；基础赔率保持只读。

### 本轮：站点多访问域名（已完成）

- [x] 代理端域名、用户端域名改为动态列表，支持新增、删除和设置唯一主域名。
- [x] 后端在事务中校验域名全局唯一并批量保存，兼容旧单域名字段。
- [x] PHP 语法检查和管理端构建通过；浏览器已验证当前配置保存、重新加载、多行动态新增及每组唯一主域名。

### 本轮：快速录入完整功能与接口（进行中）

- [x] 对照线上快速录入页面盘点文本替换、新标签、生成、下注、偏好开关、默认彩种、规则和金额预警功能。
- [x] 新增用户端快速录入 API 类型和请求方法，服务端实现解析预览与二次确认下注接口。
- [x] 为投注解析结果建立成功/失败行、失败原因、总码数和总金额展示。
- [x] 将标签和录入偏好按站点用户持久化，补齐标签列表/创建/删除与偏好接口。
- [x] 通过本地浏览器验证空文本、解析失败、解析成功和接口错误状态；重新登录后已验证真实预览接口与逐行结果展示。重复下注保护由独立事项继续跟踪。
- [x] 修复首页切换“投注记录”时顶部账号区因 flex shrink 发生宽度跳变，固定账户区 250px 并锁定鲸鱼标识宽度；同步压缩导航卡片间距以避免固定账号区造成头部横向溢出；本地浏览器切换前后实测尺寸一致。

下一阶段再继续实现其他页面和业务接口。

### 快速录入接口审计与补齐（2026-08-19，进行中）

- 已覆盖：生成预览、下注二次确认、用户偏好读取/保存、标签创建/删除后端、规则读取、前端金额/码数汇总与基础错误提示。
- 待补齐：线上真实解析/下注协议（`mb.tz` 的 `gtlcd`/`gbtdl` 与 `mb.bt`）、粘贴自动下注、立即识别、自动复制小票、标签删除/应用、预警持久化、期号/封盘/余额校验、事务与重复提交保护。
- [x] 扩展服务端解析器和统一码数/金额计算（中文数字、常见单位、玩法校验、长度限制）。
- [x] 实现前端粘贴自动识别/下注触发、立即识别、自动复制小票、预警设置持久化和标签应用/删除。
- [x] 增加下注余额校验、待分配期号标记、事务和短时间重复提交保护。
- [x] 浏览器已验证登录后的成功生成、失败提示、标签新增/删除、立即识别和预警持久化；下注确认已实际提交到本地 API，余额不足时返回提示并自动关闭确认框，未写入投注记录。
- [ ] 重复提交与系统剪贴板权限仍需在可控测试数据下补充验证。
- [x] 补齐投注记录 API 的分页、中奖状态/日期/原始文本筛选、加载状态、错误提示和 CSV 导出。
- [x] 新增停押降水数据表与 `/user/stop-drops` 查询 API，接入号码、类型、彩种、分类、日期、排序筛选及结果表格。
- [x] 浏览器验证“投注记录”和“停押降水”页签、筛选控件、空数据状态及查询加载流程。
- [x] 按浏览器标注将替换模块内标签列表下移 6px，并实测标签边界从 top 220px 调整到 226px。
- [x] 对照参考站补齐标签点击载入文本、红色圆形删除按钮和虚线标签卡片样式；构建已通过。
- [x] 按参考站实测重构修改密码页：150px 白色三栏、竖向虚线分隔、中栏规则说明、黄色禁用密码提示和居中保存按钮；保留真实修改密码接口。
- [ ] 修改密码页浏览器最终尺寸复核：本轮重新接管本地页被浏览器 URL 安全策略阻止，当前仅完成参考站实测与本地构建验证。
- [x] 实测并修正修改密码禁用密码提示条：14px 字体、36px 高、15px 圆角、无边框、`#ffffa1` 背景，红色“注：”使用 16px 粗体伪元素。
- [x] 为温馨提示收起/展开增加 280ms 高度、透明度和位移动画；改为保留内容节点过渡，不再条件卸载导致瞬间消失。
- [x] 移除温馨提示生硬的淡入淡出与位移叠加，修复固定 height 瞬间归零造成的卡顿，改为 240ms 纯高度滑动并同步平滑旋转箭头。
- [x] 按用户要求改用 `motion` 动画库：温馨提示高度与箭头旋转均使用 spring 动画，删除手写 transition；构建和 lint 已执行。
- [x] 快速录入结果改为逐行展示：每行原文对应一条成功或失败结果；成功行显示彩种、笔数和金额，失败行显示后端返回的具体原因，顶部汇总只统计成功行。
- [x] 修复中文“各/每/个”按字节读取导致单注金额未按笔数累计；`123 456 789 直选各1元` 已通过服务端回归和浏览器实测，结果为 3 笔、金额 3。
- [x] 将快速录入解析从控制器拆为 `QuickEntryParser` 服务，新增百十个定位集合笛卡尔积、福体双彩种计数和句末共/合计/总计校验；`福体百0234569十0234689个1235679各10合计6860` 已通过自动回归与真实页面验证，结果为 686 笔、6860 元。
- [x] 按参考站计算样式重构快速录入操作栏：70px 行高、26px 按钮、60px 虚线分隔、14px 汇总标签/16px 红色数值、97px 预警标签和 80×32px 浅蓝输入框；预警无配置或清空时显示并持久化为 0。
- [x] 按参考站重构快速录入文本区：textarea 与清空/计数栏合并在 212px 外框内，底栏固定 34px 并使用深蓝虚线；福彩3D为淡紫输入背景/紫色选中态，排列三为淡绿输入背景/绿色选中态。
- [x] 快速录入结果行补齐参考站交互：成功/失败行均支持新增和删除；成功识别每行只显示一个“详”，弹窗滚动展示全部号码，福体会展开为福/体两组；行增删后顶部码数和金额实时重算。
- [x] 按参考站重做号码详情弹窗：1000px 白色弹窗、详情总计行、体/福色标、玩法标题、8 组“号码/金额”表格列、浅蓝数据单元格、滚动区域和底部“关 闭”按钮。
- [x] 修正号码详情表格配对结构：每个号码与金额作为一对，每行固定 8 对并提供完整单元格边框；最后一行不足 8 对时，号码和金额均使用“—”补齐。
- [x] 强化号码/金额不可拆分结构：每一对使用独立两列容器和完整外边框，号码与金额之间不画分隔线，相邻对折叠边框连续排列，视觉上每行严格为 8 格。
- [x] 浏览器复核详情弹窗：弹窗距顶部 50px、无右上角关闭图标、每行固定 8 对；修复全局 `.empty` 样式污染导致空占位格被拉高，数据格与空格现均稳定为 32px。
- [x] 对照参考站修正详情汇总标题与表格：标题改为 16px/500 字重并使用信息图标，表格边框改为参考站的深色 `#5f5f5f`，表头高度同步为 33px。
- [x] 按参考站测量将详情“福/体”彩种色标下移 3px，补足色标与详情区域上边缘的间距且不改变表格布局。
- [x] 按参考站重构识别结果主行：顺序调整为减号、50px 编号、加号、120px 详情区、40px 福体标识和可编辑原文；主行及汇总行统一为 23px 高、14px 字体。
- [x] 新增并通过 `福体百2345678十2345689个0134579各7合计4802` 解析回归；页面实测为福体 686 笔、4802 元，详情体/福各 343 码并分别使用绿色和紫色标识。
- [x] 按参考站修正结果行“新增”：加号现在在当前行下方插入独立空白行，显示递增编号和蓝色“新”标识，不复制原投注文本或汇总数据。
- [x] 打通结果行与上方多行输入同步：编辑已有行会替换对应文本，新增行输入会追加一行；“立即识别”开启时编辑后自动解析，关闭时保留“新”状态直到点击“生成”。
- [x] 按参考站实测重构投注记录筛选栏：字段组固定为 88/370/398px、50px 高，控件和按钮尺寸、边框标签、颜色、日期秒级格式及 7px 表格间距均完成同步。
- [x] 统一所有业务空状态为 Ant Design 官方 `<Empty image={Empty.PRESENTED_IMAGE_SIMPLE} />`：用户端投注记录、停押降水、下注明细、开奖号码、通用页及代理端概览/占位页均已替换并通过浏览器检查。
- [x] 修复修改账号页信用额度内容溢出：内框改为外层左侧固定 15px，额度控件使用独立 28px 单行在 50px fieldset 内垂直居中；浏览器实测四向溢出均为 0。
- [x] 完成会员离线赚水两级联动：表头“同”同步全部 26 项，每个分类行新增独立“同”并只同步本组；浏览器验证全表、分类、单行三种状态且未提交测试数据。
- [x] 将会员离线赚水按“站点 + 代理 + 下级用户 + 玩法”持久化：迁移回填现有 104 条记录的代理归属，保存接口按当前代理隔离读写并校验 0-0.1；原值保存、刷新回显及数据库校验值回归通过。
- [x] 将管理端赔率配置改为树状模型：新增 `lottery_odds_categories` 类别表，玩法通过 `category_id` 挂载；赔率和限额字段允许留空并以 `NULL` 存储；管理端改为可展开类别/玩法树，支持类别 CRUD 和类别下新增玩法。
- [x] 赔率树默认收起并恢复分页：按类别分页，每页类别下完整保留其玩法；管理端默认不展开任何类别，分页器显示类别总数并支持切换每页数量。
- [x] 支持类别直接作为玩法：类别节点增加“类别本身就是玩法/类别下挂玩法”类型；玩法类别可直接保存六个可空赔率字段，不再强制创建子玩法，普通类别行为保持不变。
- [x] 按提供的赔率表补充福彩3D和排列三玩法：更新组六六码/组六七码为 1.2931/1.2605，新增对子全包、组六2胆拖（2码拖2至拖8）和单选全胆拖（1码拖2至拖8）；迁移可重复执行且不会重复插入。
- [x] 修复赔率树分页切换：后端按类别读取 `page/page_size`，每个类别携带完整玩法；浏览器验证切换第二页后不再重复第一页。
- [x] 清理并按参考站真实赔率结构重建福彩3D/排列三：清空旧类别、玩法及会员覆盖，导入 22 个类别节点和 106 个可下注玩法（类别本身可直接作为玩法）。
- [x] 修复直接玩法节点的赔率展示：三码定位、独胆、双飞、对子、组六、组三等 `is_playable` 类别按玩法行渲染所有赔率字段。
- [x] 调整参考站玩法顺序：豹子全包紧随和值，对子全包紧随组六赖，福彩3D和排列三同步持久化。
- [x] 将直接玩法节点同步到代理端下级赔率表：代理接口合并 `is_playable` 类别并支持离线赚水保存；移除旧的“三码定位→口口口”名称映射。
- [x] 修正代理赔率表层级与顺序：直接玩法不再生成同名分类行；豹子全包挂在和值末尾，对子全包挂在组六赖末尾；接口全局排序在前端保持不变。
- [x] 对照参考站实测调整赔率行顺序和颜色：二码定位后依次为三码定位/独胆/双飞/对子/组六/组三/组三多码；白/青/黄/绿背景、蓝色名称与赔率上限、灰色边框全部一致。
- [x] 将离线赚水表头全局“同”默认状态改为未勾选，避免进入页面后修改单项时意外同步整表。

### 本轮：赚水流水与两端展示（进行中）

- [x] 按新业务口径调整代理拦货与水位统计：拦货容量改为彩种/玩法/期号总池；站点新增暗水比例、明水比例；代理占成金按会员盈亏×自身占成，明水按占成金×站点明水比例统计；完成 PHP lint、占成回归和管理端构建。
- [x] 根据最新口径统一“离线反水”：使用站点离线反水（明水）比例（默认 0.085）×当前代理线路所有用户总投，不再读取注单玩法 `drop_odds`；代理总货明细、综合报表、分类账均已同步。

- [x] 新增版本化 `organization_water_ledger`，以注单明细、组织和来源唯一，记录 `amount × drop_odds`，且不改组织余额。
- [x] 结算事务写入独立赚水流水并更新用户日账单的离线回水与盈亏。
- [x] 用户端下注明细和历史账单按 `user_stop_drops.drop_odds` 返回离线回水，合计与逐行金额一致。
- [x] 代理分类账的贡献度、日/月分类账和路径账增加“离线赚水”字段，保留回水、中奖、盈亏独立含义。
- [x] 执行迁移、PHP lint、赚水回归测试、用户端/代理端 lint 及全量构建；仅进行了数据库表/重复性只读检查，未执行真实下注。

### 本轮：登录地点记录（进行中）

- [x] 核对登录、会话、会员、代理和管理员列表，确认会话已有城市级 location，但账号表未统一持久化。
- [x] 新增版本化迁移，为全部登录账号增加 `last_login_ip` 和 `last_login_location` 字段。
- [x] 登录成功时统一写入账号表和 `account_sessions`，保留 IP、时间、设备和地点；解析失败不阻断登录。
- [x] 支持可配置 IP 地理服务及村/镇/街道字段，默认继续使用现有 IP 服务并缓存结果。
- [x] 管理端、代理端会员、组织管理员和子账号列表补充最后登录地点展示。
- [x] 执行迁移状态、PHP lint、地址解析测试、代理端 lint 和全量构建；管理端没有 lint 脚本，已通过 `vue-tsc` 构建检查，未执行真实下注或其他敏感操作。

### 本轮：组织层级下钻列表（已完成）

- [x] 后端按当前代理权限返回指定组织节点的直属下级，并阻止跨分支访问。
- [x] 代理端新增可点击组织名称、层级面包屑和独立下级列表组件，支持逐级进入与返回。
- [x] 嵌套浏览仅展示当前节点直属下级，保留账号、额度、占成、登录状态和登录地点等信息；代理节点显示直属会员。
- [x] 完成 PHP lint、代理端 lint、全量构建和权限范围回归；未执行真实下注。

### 本轮：站点后台管理员与总监管理员职责拆分（已完成）

- [x] 代理端登录不再接受 `site_admins` 和历史站点经理账号，只接受组织账号、组织子账号及保留的代理账号。
- [x] 已有站点管理员代理会话在中间件立即失效，不能继续以根总监权限访问代理接口。
- [x] 总平台“进入站点”改为根总监组织账号的临时代入会话，记录 `impersonation` 和平台操作人，不复用站点管理员身份。
- [x] 站点管理员页面明确标注为“站点后台管理员”，新建账号不得与同站点组织账号重名；现有同名账号暂不删除。
- [x] 完成 PHP lint、角色拆分回归及全量构建发布；未执行真实下注。

### 本轮：站点级下注时间策略（已完成）

- [x] 站点配置按彩种增加时间段规则：允许下注、蒙版、是否显示预加载下一期和自定义文案，支持跨午夜时间段并保留每日截止时间兼容。
- [x] 用户端开奖列表、首页期号、下注明细期号下拉框和其他 `recent_issues` 使用点按同一策略隐藏或显示预加载期。
- [x] 用户端按时间段动态更新文案、倒计时、蒙版和下注按钮；前端拦截自动粘贴下注，服务端同步拒绝禁止时段下单。
- [x] 补齐管理端时间段添加/删除操作，完成 PHP lint、管理端和用户端生产构建发布；未执行真实下注。

### 本轮：定位复式结算与历史数据清洗（已完成）

- [x] 修复百/十/个定位复式的结算匹配：选号集合只用于校验合法组合，实际中奖只计算与开奖号码完全一致的组合。
- [x] 为定位复式补充 343 组合只命中 1 注的结算回归，并通过全量结算匹配器测试。
- [x] 清洗提交 13 的福彩/体彩两笔错误派奖：每笔改为 900 元，提交汇总改为 1800 元，同步修正会员余额、账单和组织分账，并写入手工更正流水。
- [x] 创建数据库清洗前备份并执行全库只读扫描；定位复式已结算记录共检查 2 笔，剩余金额不一致 0 笔。

## 本地启动

### Git 仓库整合（2026-08-19，已完成）

- [x] 删除 `.runtime` 内两个依赖源码目录携带的嵌套 `.git` 元数据。
- [x] 在项目最外层初始化 `main` 分支，并将前后端、服务端和参考资料统一纳入单一仓库。
- [x] 过滤 `.runtime`、依赖目录、构建产物、运行日志、真实环境文件、编辑器配置和 Playwright 临时输出。
- [x] 完成待提交文件、大文件和私钥标记检查，创建首次整体提交。

- 管理端：`http://127.0.0.1:5173`
- 用户端：`http://127.0.0.1:5174`（`#/kb`）
- 代理端：`http://127.0.0.1:5176`
- API：`http://127.0.0.1:8000/api/v1/health`

### 本轮：组选集合占位号码展示修复（已完成）

- [x] 保留数据库及结算使用的 `000` 内部占位，不改写历史注单数据。
- [x] 从明细原始文本恢复组选集合真实选号；兼容规范化后的阿拉伯数字形式（如 `组六5码`），页面显示 `15862`。
- [x] 在服务器执行 PHP lint、解析器回归和 `npm run build`；线上 `kpsuser.tzgpt.top:5998` 最新 `2026225` 明细及号码弹窗均验证显示 `15862`。
- [x] 验证过程仅查看和筛选页面，未执行下注或提交操作。

### 本轮纠正：组选多码必须持久化实际号码明细（进行中）

- [x] 核对现有数据库与接口，确认历史组选多码仅保存 `000`，页面显示的 `15862` 来自展示层临时恢复，并非真实逐号明细。
- [ ] 将新下注的组选多码在解析/保存阶段持久化为实际组合号码，并补充解析回归。（in_progress）
- [ ] 调整结算与号码级分配，确保逐号化后投注金额、中奖金额和盈亏总额守恒。
- [ ] 通过版本化迁移回填可可靠恢复的历史 `000` 组选明细，保留原始总额、状态和中奖总额。
- [ ] 下注明细 API 返回持久化逐号记录及明细组标记；用户端每行重复注单号/时间，同组后续显示“同上”。
- [ ] 完成 PHP lint、解析/结算/拦货回归、前端 lint/typecheck/build，并仅用只读接口和页面验证，不执行真实下注。

### 本轮：SaaS 主单编辑与批量修改（已完成）

- [x] 下单记录详情支持以主单为入口编辑号码/玩法/金额/原始行，普通编辑仅允许未开奖且未封盘记录。
- [x] 批量修改改为主单选择，不再按单个号码选择；同一提交批次的多彩种记录会一起纳入处理。
- [x] 新增批量预览与事务执行接口，支持替换号码、替换玩法和设置明细金额，并返回修改/跳过明细数量。
- [x] 修改后重新汇总每个 bet_record 的金额和玩法笔数，写入审计日志；已开奖、已封盘、已退码主单会被拒绝。
- [x] 管理端构建、PHP lint 通过；未执行真实修改，未对现有订单提交业务变更。

### 本轮补修：管理端 API 代理指向错误（已完成）

- [x] 核对空列表原因：kpsadmin/kpsuser/kpsag 等站点原先代理到旧系统 5095 端口，未读取当前 `kuaipaisan` 数据库。
- [x] 启动当前项目 API 18082 服务并设置为 systemd 开机自启。
- [x] 将管理端、用户端和代理端的 API 反代切换到 18082，保留 Nginx 配置备份。
- [x] 通过 `/api/v1/health` 验证当前 API 数据库正常；当前库存在 40 条注单，其中 36 条未开奖且未封盘。

### 本轮：复式中奖判定与组六规则隔离（已完成）

- [x] 核对用户订单、开奖期号、解析结果和结算匹配器，确认复式被错误套用组六的三不同数字条件。
- [x] 修正紧凑表达式及历史 `000` 占位表达式的复式命中规则，并补充 `522` 回归测试。
- [x] PHP lint 和结算匹配器回归通过；用户端解析回归存在本轮之前的“629 269直10”金额断言失败。

### 本轮：代理端顶部账号与彩种列表布局（已完成）

- [x] 顶部账号区仅保留当前登录账号，按“账号 / 层级：账号”显示，移除信用、已用、可用三个字段。
- [x] 支持总监、大股东、小股东、总代理、代理层级标签，并优先使用组织接口返回的自定义层级名称。
- [x] 彩种列表从页眉导航中独立到下方横向栏，单项 200×46px，选中项绿色底白字，未选中项白底绿色字，显示彩种、状态、期号和倒计时。
- [x] 代理端生产构建、lint 和 `git diff --check` 通过；构建产物已发布到代理端站点目录。

### 本轮：逐级剩余分配与跨级直属下级（已完成）

- [x] 结算占成改为按剩余金额逐级计算：代理先取自身占成，剩余金额传给直属上级继续按其占成计算，最终余额由根总监承接并保持守恒。
- [x] 组织层级允许直属跨级创建：总监可直接创建大股东/小股东/总代理/代理，大股东可创建小股东/总代理/代理，小股东可创建总代理/代理，总代理可创建代理。
- [x] 代理新增下级表单增加层级选择，并将所选层级写入真实 parent_id 关系；分数分配和权限校验沿用该直属关系。
- [x] PHP lint、层级矩阵、逐级占成单元/数据库回归通过；代理端前端构建与 lint 通过并已发布。

### 本轮补修：代理端彩种栏裁切与排版（已完成）

- [x] 覆盖旧版彩票列表样式，强制横向排列、`200×46px` 单项尺寸和边界盒模型，避免页眉旧样式导致内容裁切。
- [x] 彩种栏支持横向溢出滚动，彩种名称/状态靠左、期号/倒计时靠右，选中颜色和未选中颜色保持参考站规则。
- [x] 生产构建重新发布，并通过浏览器计算样式复核：容器 1280×46px、列表 400×46px、单项 200×46px。

### 本轮：综合报表按逐级剩余口径试运行（已完成）

- [x] 综合报表按“会员盈亏 = 总中 + 回水 - 总投”计算。
- [x] 报表占成改为读取会员所属组织链，按最近层级先分配、余额逐级上返；不再对每个上级重复套用会员占成比例。
- [x] 保留离线反水和赚水字段，旧会员未绑定组织时继续兼容其直接占成比例。
- [x] 仅调整报表展示计算，未修改结算流水、历史订单或余额；PHP lint 与逐级占成测试通过，等待业务数据对比确认。

### 本轮补修：会员报表字段收敛（已完成）

- [x] 会员栏调整为仅显示“笔数、总投、总中、盈亏”。
- [x] 删除会员栏单独的“总赚水”展示；回水作为内部计算项并入会员盈亏，不再伪装成会员独立账务字段。
- [x] 代理及总代理字段保持原有分组和计算口径不变。

### 本轮：报表层级列动态化（已完成）

- [x] 报表接口返回当前组织实际关联的下级、当前层级和上级层级，不再固定写死“会员 + 代理 + 总代理”。
- [x] 前端按层级关系动态生成表头：下级使用总投/总赚水/盈亏，当前层级使用占成金额/占成盈亏/离线反水/总赚水/总盈亏，上级使用总投/盈亏。
- [x] 支持大股东、小股东、总代理、代理、总监等实际存在层级的动态组合，并保持会员四列固定。
- [x] PHP lint、TypeScript 构建和 `git diff --check` 通过。

说明：本版先完成动态列结构和层级关系展示，金额字段沿用当前报表指标；后续若需要逐个下级账号独立汇总，再在接口增加按组织节点聚合的数据集。上级列按业务规则仅保留直属父级，不继续展开父级的上级。

### 本轮补修：占成盈亏与赚水显示口径（已完成）

- [x] 占成金额显示绝对值正数，用户盈利时占成盈亏为负、用户亏损时占成盈亏为正。
- [x] 明水并入占成盈亏；有占成比例的层级，离线反水单独显示 0，且不再重复叠加到占成盈亏。
- [x] 总赚水列不参与计算，并统一显示 `0`，不再把明水或回水误显示为总赚水。
- [x] 明水计算统一按 `占成金额 × 0.085`，占成盈亏为占成金额与水钱之和。

### 本轮补修：占成金额按汇总净盈亏计算（已完成）

- [x] 修正报表聚合：先汇总会员净盈亏，再应用占成比例；不再对每条明细取绝对值后累加。
- [x] 保留每条明细的带符号占成基数作为隐藏聚合输入，支持不同会员占成比例并避免混合输赢放大金额。

### 本轮补修：代理端顶部账号容器对齐（已完成）

- [x] 给账号区域增加独立身份容器，与 logo 和导航分区，避免头部文字挤在一起。
- [x] 账号字段固定尺寸并在容器内水平、垂直居中，层级与账号过长时使用省略显示。
- [x] 覆盖旧版通用 `.account` 网格规则，生产构建通过并重新发布代理端站点。

### 本轮：用户端修改密码页完整对齐参考站（已完成）

- [x] 审查参考站 `/h5/#/mp` 的 DOM、字段顺序、响应式坐标、边框、字号、规则文案和提交按钮样式。
- [x] 将用户端密码页改为三个纵向 outlined 字段，标签浮在边框上沿，移除旧三栏表格和额外禁用密码提示。
- [x] 在服务器完成 user-h5/user-web lint、构建，并刷新线上 H5 做尺寸和无横向滚动复核。
- [x] 保留真实密码修改接口；已完成只读页面与构建验证，未提交真实密码修改。
- 线上复核：430px 视口下外框 412.8px、输入框 369.8×34.4px、按钮 68.8×31.0px，scrollWidth=clientWidth=430px；浏览器标签保持打开。
### 本轮：全平台统一明水口径（进行中）

- [x] 站点配置统一为单一 `water_rate`，默认 0.085；兼容读取旧字段但不再保存或使用 `bright_water_rate`。
- [x] 代理报表、分类账、拦货明细统一按“占成金额 × 明水比例”计算；取消离线反水、暗水和占成明水的重复字段。
- [x] 用户端明细、账单和会员赔率展示移除离线反水列，用户盈亏不再叠加旧的玩法级离线反水。
- [x] 完成前端构建、接口回归和生产静态文件发布。
- [x] 已将现有站点、赔率、会员权限中的旧离线反水数据清零，并将站点设置规范化为 `water_rate=0.085`。
- [x] 报表表格恢复原有列结构，不单独展示明水；明水仅计入占成盈亏，离线反水和总赚水列显示 0。

### 本轮：用户端首页输入区高度自适应（已完成）

- [x] 按参考站审查首页空输入与多行识别结果状态；参考站使用 iPhone 15 Pro 逻辑视口 393×852 复核，用户端保持现有 430px 标签并按响应式单位适配。
- [x] 输入为空时隐藏结果表，输入框恢复占满可用高度，底部工具栏保持固定布局。
- [x] 识别结果出现后通过 `ResizeObserver` 测量结果表高度，动态缩短输入区并让结果表紧接显示，支持结果行数变化。
- [x] 清空文本自动清理结果和金额统计，恢复空输入布局；H5 lint、构建和线上回归通过，未提交任何测试注单。
- [x] 修正识别/下注按钮锚点，按输入区底部动态定位；有结果时随结果高度上移，避免与开关、结果表重叠。
- [x] 修正真机窄屏适配：底部按钮、开关和结果区均使用响应式尺寸；默认彩种改为真实 Ant Design Switch，仅按彩种切换颜色和文案，不再用自绘 `<b>` 模拟。

### 本轮：关闭用户 H5 主页下拉刷新与结果行自适应（已完成）

- [x] 在 H5 根页面和应用壳增加 `overscroll-behavior-y: none`，禁止主页边缘拖拽触发浏览器下拉刷新，同时保留 textarea、结果区和侧栏内部滚动。
- [x] 结果文本渲染后按实际 `scrollHeight` 调整 textarea 高度，避免 `rows` 估算造成底部大块空白；结果行控件和统计信息保持垂直居中。
- [x] 在服务器完成 user-h5 构建，并通过现有手机视口检查无横向溢出、结果区高度随文本变化。

验证：`npm run lint` 通过（仅保留既有 hooks/export warnings）；`npm run build` 通过并发布 `apps/user-h5/dist`；浏览器现有用户端/参考站标签保持开启，未改变手机模式或缩放，未执行下注等真实业务操作。

### 本轮：恢复 H5 识别结果文字字号（已完成）

- [x] 将移动端识别结果文本及笔数/金额统计字号调整为 `max(14px, 3.6vw)`，不再因响应式规则低于原有 14px。
- [x] 同步提高结果行行高并保留按实际内容高度换行，避免字号恢复后产生截断或横向溢出。
- [x] 完成 user-h5 lint、构建和 dist 发布；未改变浏览器手机模式、缩放或标签状态。

### 本轮补修：移除固定行高，按文字内容自适应（已完成）

- [x] 移除识别结果 textarea 和统计区域的固定 `line-height`，保留 16px 字号及上下内边距。
- [x] 取消结果主行、textarea 和统计行的额外固定最小高度，由实际文字换行后的高度决定布局。
- [x] user-h5 构建通过并发布 dist；浏览器标签、手机模式和缩放未改变。

### 本轮：查看更多与停押降水页面滚动修复（已完成）

- [x] 为查看更多面板建立独立纵向滚动容器，支持触摸滚动和长列表浏览。
- [x] 为停押降水所在的全页面 body 建立独立纵向滚动容器，表格超出短屏时可继续上下滚动。
- [x] 保留主页根页面下拉刷新禁用，内部页面使用 `overscroll-behavior-y: contain`，构建已通过并发布 dist。

### 本轮：343 注长文本识别结果高度修复（已完成）

- [x] 移除按“文本长度 ÷ 14”估算 textarea `rows` 的逻辑，固定原生 rows=1，实际高度完全由渲染后的 `scrollHeight` 决定。
- [x] 使用 343 个三位数组成的 1371 字符文本做识别布局测试；未点击下注，测试后已清空输入。
- [x] 430×932 视口下结果内容高约 1205px，结果容器高约 419px 并可内部滚动，输入区自动收缩至约 172px；构建通过并发布 dist。

### 本轮补修：真机结果区固定 16px 与输入高度压缩（已完成）

- [x] 识别结果与统计文字固定为 16px/22px，textarea 上下保留 4px 内边距并由 `scrollHeight` 自适应。
- [x] 有识别结果时按结果高度压缩输入区，避免真机底部出现大块空白；结果区和内部按钮保持居中。
- [x] user-h5 构建通过并发布 dist；430×932 视口无横向溢出，浏览器标签、手机模式和缩放保持不变。

### 本轮补修：识别结果字号固定 16px 与内容高度自适应（已完成）

- [x] 识别结果文字、笔数和金额统计统一为 16px，行高 22px。
- [x] 结果文本仅保留上下 4px 内边距，继续由 `scrollHeight` 自动计算高度；有结果时输入区同步收缩，减少无效空白。
- [x] user-h5 构建通过并发布到 dist，未改变浏览器标签、手机模式或缩放。


### 本轮：H5 手机浏览器安全区域适配（已完成）

- [x] 为 H5 viewport 启用 viewport-fit=cover，并定义统一的安全区变量。
- [x] 将主页、查看更多、停押降水及记录等可视高度改用 100svh，底部滚动内容预留安全区。
- [x] 完成 lint、构建、发布及 430×932 手机视口回归，确认底部内容不被浏览器工具栏遮挡且浏览器标签保持开启。

### 本轮：手机主页下注记录恢复（已完成）

- [x] 恢复移动端主页下注记录 aside，放置在快录内容下方而不是隐藏。
- [x] 保留每条记录的“详 / 号 / 退”按钮及现有详情、号码复制、退单确认逻辑。
- [x] 记录列表启用独立触摸滚动；user-h5 lint、构建、发布和无横向溢出检查通过。

### 本轮：手机下注记录颜色与 PC 端统一（已完成）

- [x] 普通下注记录恢复 PC 端灰色卡片、白色时间栏和白色金额操作栏。
- [x] 仅退单记录使用退单粉色背景；“详”保持绿色，“号”保持浅橙色，“退”保持红色。
- [x] 服务器重新构建发布，并在 430px 手机视口核对按钮颜色与无横向溢出。

### 本轮：首次登录公告显示与关闭持久化（已完成）

- [x] 公告仅在登录后首次加载时显示，关闭后按账号持久化关闭状态。
- [x] 刷新页面不重复弹出，切换账号使用独立状态。
- [x] 完成 H5 lint、构建、发布及浏览器只读验证。

### 本轮：手机端粘贴自动识别（已完成）

- [x] 粘贴文本后自动触发识别，不要求用户再点击识别。
- [x] 保留自动下注开关语义：关闭时只识别，开启且允许下注时才提交。
- [x] 完成 H5 lint、构建、发布及不下注验证。

### 本轮：手机端隐藏重复盘口标签（已完成）

- [x] 手机端隐藏操作栏重复的“盘口：A盘 - A”，保留 A - A 盘口框。
- [x] 桌面端保持现有盘口标签，完成构建和移动端检查。

### 本轮：H5 全视口隐藏重复盘口标签（已完成）

- [x] 在 user-h5 全部视口隐藏重复的操作栏盘口标签，保留 A - A 盘口框。
- [x] 重新构建并验证桌面宽度打开 H5 和手机宽度均不可见。

### 本轮：SaaS 批量修改按用户与投注明细选择（已完成）

- [x] 先选彩种并自动匹配当前未开奖期，再支持多选用户。
- [x] 投注列表按明细逐条选择，默认不选，支持全选当前投注后执行位数替换。
- [x] 完成 admin-web 类型检查、构建和发布。

### 本轮：SaaS 批量修改未开奖期号自动定位（已完成）

- [x] 期号优先取彩种当前未开奖历史，不再依赖是否已有投注。
- [x] 补充调度器短暂未插入下一行时的 next_code/数字递增兜底。
- [x] 完成服务端语法检查并重新发布 SaaS 前端。

### 本轮：SaaS 机器人管理基础（进行中）

- [x] 建立机器人账号配置表，金额范围按机器人独立保存，精度支持整数/1位/2位小数。
- [x] 增加站点、组织节点、彩种、权重、开始时间和随机间隔配置。
- [x] 增加机器人列表、创建、编辑、启动/停止、转普通会员、下单历史接口和管理页面。
- [ ] 接入赔率玩法随机生成、机器人调度下注、开奖结算和每单时间推进。

### 本轮：站点彩种权限自动同步（已完成）

- [x] 站点彩种更新时同步所有下级用户的可见/可下注权限。
- [x] 按彩种名称/编码迁移重复或历史 lottery ID，新增彩种默认开放，移除彩种清理旧权限。
- [x] 批量修复现有站点权限数据并完成 PHP 语法检查。

### 本轮：H5 输入与代理会员保存修复（已完成）

- [x] H5 禁止缩放并适配 iOS 登录输入，避免键盘弹出导致白屏/无法输入。
- [x] 代理保存权限或赔率时不再重置会员分数，实际改分读取最新余额。
- [x] 保留非负分数校验，完成 user-h5、agent-web 构建发布。

### 本轮：多行混合组选识别（已完成）

- [x] 支持彩种前缀 + 多行数字集合 + `组六组三金额共计` 格式。
- [x] 按每行实际位数生成五码/四码玩法，不再合并或截断号码。
- [x] 完成示例 60 元总额校验及服务端 PHP 语法检查。

### 本轮：H5 退码列表原始文本搜索框对齐参考站（已完成）

- [x] 对比当前站与参考站 `#/rtl` 页面的搜索框 DOM、盒模型与 CSS 单位。
- [x] 将 user-h5 搜索框结构和视觉属性对齐参考站。
- [x] 构建发布 user-h5，并在当前站验证尺寸、边框、字号与焦点高度。
- [x] 默认状态与参考站均为 1462.25×75.20px、14px 字号、1px 边框；聚焦状态均展开到 188.03px 并显示蓝色边框和阴影。
- [x] 仅退码、搜索按钮和总金额改为参考站同样的横向位置、普通字重、字号、图标间距和金额独立节点。
- [x] 重新构建发布并核对线上计算样式：开关、按钮、图标、搜索文字和总金额均与参考站对齐。

### 本轮：H5 快录结果行对齐参考站（已完成）

- [x] 用参考站 `#/kp` 的真实输入内容重现结果行，读取操作行、文本行、失败提示行的 DOM 与 CSS。
- [x] 将 user-h5 快录结果 DOM 拆为独立操作行和文本行，并按参考站使用 rem 尺寸、颜色、按钮和提示布局。
- [x] 清除高优先级旧 23px 网格规则，保证结果行不再被压缩。
- [x] 线上验证结果行高度 188.03px、操作行和文本行各 94.02px，删除/新增/状态按钮横向位置与参考站一致。

### 本轮：H5 快录紫色输入区被结果行挤压（已完成）

- [x] 定位到旧的 `--quick-result-height` 计算在结果行放大后把输入区高度压成 0px，并将工具栏顶到输入区上方。
- [x] 为有结果状态恢复参考站的输入区高度（约 24.6rem），保持紫色背景、输入文本区和 5rem 工具栏正常排列。
- [x] 线上验证输入区约 770.95px、文本区约 614.27px、工具栏约 156.69px，工具栏不再覆盖或顶上来。

### 本轮：H5 快录新标签工具栏被旧绝对定位顶起（已完成）

- [x] 对比参考站发现新标签、替换文本、规则和大金额预警属于同一 flex 工具栏，按钮使用 rem 尺寸，不使用绝对定位。
- [x] 清除 user-h5 旧的 `left/top/position:absolute` 偏移，恢复按钮顺序、分隔间距和预警控件的相对位置。
- [x] 线上验证新标签、替换文本、规则按钮横向位置与参考站分别对齐，紫色输入区从工具栏之后正常开始。
- [x] 清理工具栏内残留的匿名文本节点，最终核对新标签 x=35.06、替换文本 x=393.88、规则 x=734.16、预警框 x=980.96。

### 本轮：H5 快录盘口框与工具栏混排（已完成）

- [x] 将 A - A 盘口框从 actions 绝对定位层移出，恢复参考站的独立盘口行和下一行 flex 工具栏。
- [x] 盘口框线上核对为 727.08×109.69px，工具栏按钮与预警框均在独立行内，不再与输入区重叠。
- [x] 移除 replace 区旧的 A-A 伪元素，最终线上核对盘口框 x=40.72、工具栏新标签 x=35.06、替换文本 x=393.88、规则 x=733.89、预警框 x=979.11。
- [x] 按需求将规则按钮文案从“规则说明”改为“规则”，构建发布并在线核对。
- [x] 按参考站补回工具栏三条独立虚线，线上核对虚线位置与高度为 x=343.80/702.33/946.45、188.03px。
- [x] 移除工具栏外围误加的蓝色上下边线，只保留 A-A 盒子本身的蓝色三边；线上核对 replace 外围无边框。
- [x] 按最新需求给 A-A 外围行增加 2px 蓝色顶部边线，底部仍保持无边框。

### 本轮：H5 无结果时快录工具栏塌陷（已完成）

- [x] 定位到旧样式只覆盖 `.entry.has-results`，无结果时新标签、替换文本和规则按钮回落到绝对定位。
- [x] 将工具栏 flex、按钮、规则和大金额预警控件的布局规则扩展到所有 `.entry` 状态。
- [x] 线上验证无结果时新标签 x=35.06、替换文本 x=393.88、规则 x=733.89、预警框 x=979.11，三条虚线保持独立分隔。

### 本轮：H5 工具栏下方多余空白（已完成）

- [x] 定位到紫色输入区仍继承旧的 `margin-top: 17.1vw`，在工具栏和输入区之间产生大段空白。
- [x] 将桌面端输入区上边距清零，仅保留参考站的 `.8rem` 底部间距。
- [x] 线上验证输入区紧接工具栏开始，工具栏下方不再出现异常空白。

### 本轮：H5 撤销按钮错位（已完成）

- [x] 定位到撤销按钮仍使用旧的 `position:absolute; top:14.2vw; left:44vw`。
- [x] 将撤销按钮恢复到替换工具栏的 flex 流，并在撤销出现时调整顺序和分隔线位置。
- [x] 构建发布完成；撤销按钮不再浮在工具栏虚线上或脱离工具栏。
- [x] 重新发布最新构建并触发真实撤销状态验证：撤销按钮 x=690.02、y=440.91，静态位于替换文本之后；规则 x=933.20，预警框 x=1178.42。

### 本轮：H5 查看更多记录卡片样式对齐（已完成）

- [x] 将查看更多卡片 DOM 对齐参考站的 `label → p → footer` 结构。
- [x] 未退码卡片使用透明/灰白外壳、盘口蓝色标签、福彩浅蓝/体彩浅绿文本区和红色金额。
- [x] 已退码卡片使用 `#ffe0e0` 粉红背景、透明文本区和深蓝色金额。
- [x] 增加参考站同尺寸的复制文本与查看号码按钮，并完成线上有记录日期验证。
- [x] 按参考站交互修正：点击绿色文件图标打开“下注详情”弹窗，点击未退码蓝色文本区复制原始文本；退码粉色文本区不触发复制。
- [x] 详情弹窗挂载在 body 时移除错误的 `#root` 作用域，线上验证弹窗宽度、彩种标签、明细表和关闭按钮样式正常。
- [x] 按参考站弹窗 CSS 补齐容器零内边距、56px 关闭图标、4 列明细表和 4.6rem 行高。
- [x] 使用 393×852 手机视口验证：查看更多列表、未退码浅蓝文本、退码粉红卡片、按钮尺寸及详情弹窗均适配；验证结束已恢复默认视口。
- [x] 使用 430×932 手机视口修正仅退码开关水平偏移，开关在外框内居中；验证结束已恢复默认视口。
- [x] 按参考站手机详情弹窗补齐彩种标题、玩法标签、四列表头与明细行层级，移动端行高使用 4.6rem，并完成构建验证。
- [x] 修复手机端明细行缺少 display:flex 导致四列文本粘连；补齐灰色表头、白色虚线列分隔、交替浅蓝行、金额深蓝与赔率橙色。
- [x] 宽屏端同步核对四列表格：35%/18%/22%/25% 列宽、4.6rem 行高、灰色表头与数据列颜色均与参考站一致。

### 本轮：H5 详情弹窗长内容导致高度过高（已完成）

- [x] 定位到当前弹窗将 `65vh` 直接用于 body，长明细把外层弹窗撑高。
- [x] 按参考站将 `max-height: calc(100vh - 27rem)` 放到内部内容滚动层，header/footer 固定。
- [x] 宽屏线上验证 body 273.16px、内容滚动层 147.81px、弹窗总高约 486.91px；手机端内容滚动层按视口限制。

### 本轮：H5 手机快录盘口与工具栏恢复（已完成）

- [x] 修复手机端重复的 A - A 文本，只保留现有蓝色盘口框。
- [x] 恢复“替换文本 / 规则 / 大金额预警”控件并按参考站顺序排列。
- [x] 完成 user-h5 lint、构建、线上静态产物和 430px 窄屏布局检查。
- [x] 更新问题记录与本轮验证结果。
- 验证：user-h5 lint 0 errors（保留既有 15 条 warnings），生产构建通过并发布；当前线上 HTML 已引用 `index-D-30aaBZ.css` / `index-VdI8meaI.js`，且 CSS 包含本轮手机工具栏修复，430×932 可见控件检查确认替换文本、新标签、规则和预警输入均存在，未执行下注、退码或其他业务操作。

### 本轮：H5 手机快录工具栏位置精确对齐（已完成）

- [x] 拆开盘口行和按钮行，清除穿过盘口的旧伪元素虚线。
- [x] 按参考站恢复新标签、替换文本、规则、预警框及三条分隔线的位置。
- [x] 移除输入区前的旧空白占位，并兼容已有标签列表。
- 验证：user-h5 lint 0 errors（既有 15 条 warnings），生产构建和发布通过；当前线上 CSS 已包含 430px 基准几何值及三条按钮行分隔线，原页面已执行刷新，未另开浏览器标签，未执行任何业务操作。

### 本轮：H5 录入内容时隐藏历史记录（已完成）

- [x] 根据浏览器标注确认目标是下方 `.side-record-list`，不是自动下注或识别结果区域。
- [x] 给快速录入的非空文本状态增加稳定类名，并仅在手机端隐藏历史记录列表。
- [x] 完成 user-h5 lint、构建及线上静态产物条件验证。
- [x] 更新问题记录与验证结果。
- 验证：user-h5 lint 0 errors（保留既有 15 条 warnings），生产构建发布 `index-BRPI-v5A.css` / `index-D_vEJaq1.js`；线上 HTML 已引用新产物，JS/CSS 均包含 `has-entry-content` 条件，未执行下注、退码或其他业务操作。

### 本轮：H5 手机工具栏虚线缩短（已完成）

- [x] 仅缩短三条按钮分隔虚线并保持水平锚点不变。
- [x] 完成 user-h5 lint、生产构建和线上静态产物检查。
- [x] 更新问题记录和验证结果。
- 验证：分隔线由 13vw 缩短为 9vw，顶部由 1vw 下移至 2vw，三个水平锚点保持不变；user-h5 lint 0 errors（既有 15 条 warnings），生产构建及线上 CSS 检查通过，当前原页面已刷新。
# 本轮：按参考站源码恢复手机工具栏 flex/rem 布局

- [x] 检查参考站打包后的 HTML/CSS，确认工具栏使用 flex，根字号为 `calc(100vw / 50)`。
- [x] 将手机端工具栏从 absolute/vw 改为参考站的 flex/rem 结构。
- [x] 将分隔线恢复为 `<b>` 的 `height: calc(100% - .6rem)` 与 `margin: 0 1rem`。
- [x] 完成 lint/build、线上资源检查并刷新原页面。
- 验证：`pnpm --filter user-h5 lint` 通过（仅保留现有 15 条 warning）；`pnpm --filter user-h5 build` 通过并发布；线上 CSS 已切换为 `index-VVSx-Dc6.css`，包含 `height: calc(100% - .6rem)` 与 `flex: 0 0 14rem`。

### 本轮：按参考站源码统一快录有内容状态（已完成）

- [x] 解析参考站生产 HTML/JS 中的 styled-components，确认根字号、输入区、结果区及行控件的原始单位。
- [x] 确认参考站在识别结果存在时用结果区替换历史记录组件，而不是同时展示两块内容。
- [x] 将结果按钮 DOM 顺序改为“删除 / 序号 / 新增 / 彩种 / 详情”，与参考站一致。
- [x] 恢复输入区 flex 剩余高度、结果区 30rem 最小高度及 3rem 行节奏，移除手机端 px/额外 padding 偏差。
- [x] 手机和宽屏共同验证，构建发布并记录结果。
- 验证：user-h5 lint 0 errors（保留既有 15 条 warnings），生产构建通过并发布；线上 HTML 已引用 `index-CZzrM7Ft.css` / `index-C9VDtfQM.js`。新 CSS 同时包含全宽 `2vw` 根字号、解码结果 30rem 最小高度、手机展开/收起视口高度公式，以及有结果时隐藏完整记录组件的规则。

### 本轮：按参考站源码对齐手机记录卡底栏（已完成）

- [x] 从参考站生产包提取记录卡、底栏和全局按钮的原始 styled-components 规则。
- [x] 将手机记录卡恢复为 `96%` 宽、底栏 `4rem` 高、按钮 `3.6rem` 高及 `1rem` 间距。
- [x] 将绿色“详”文字按钮替换为参考站的文件图标按钮，并统一默认“号”和红色“退”按钮配色。
- [x] 完成 lint、生产构建、线上资源检查并刷新原页面。
- 验证：user-h5 lint 0 errors（保留既有 15 条 warnings），最终生产构建发布 `index-BYDcwZRi.css` / `index-CqxjOJWG.js`；线上 CSS 包含 `4rem/3.6rem/1rem` 底栏几何及奇偶卡片底色，线上 JS 包含文件图标结构，未执行详情、号码或退单业务操作。

### 本轮：首页与“查看更多”共用号码页面（已完成）

- [x] 追踪首页“号”和“查看更多”卡片“号”的事件链与数据请求。
- [x] 首页“号”改为打开 `MorePanel` 的同一号码页面，并携带当前注单。
- [x] 删除首页旧的独立号码弹窗、复制和打印实现，只保留下注详情弹窗。
- [x] 完成 lint、生产构建、线上行为检查并刷新原页面。
- 验证：user-h5 lint 0 errors（保留既有 15 条 warnings），生产构建通过；当前线上资源 `index-CKQEjN3T.css` / `index-DzwqB0Pp.js` 已包含统一入口。原页实测首页“号”进入 `#/rtl`，`.more-number-page` 为 1、旧 `.record-number-modal` 为 0，工具栏含“上页 / 下页 / 下载 / 复制号码”，验证后已返回 `#/kb`。

### 本轮：按参考站源码恢复手机开奖时间 flex 布局（进行中）

- [x] 在 430×932 视口读取参考站真实 DOM、计算样式和打包后的 styled-components 源码。
- [x] 确认参考站根单位为 `calc(100vw / 50)`，开奖行使用横向 flex，时间/状态使用纵向 flex。
- [x] 将当前截图近似的 grid/absolute 结构改为同一套 `7rem / 2rem / 10rem` flex 关系。
- [x] 完成 lint、构建、双站计算样式对比和原页面刷新。
- 验证：430×932 下当前站与参考站均为 `7rem` 横向 flex、`10rem` 纵向时间列，时间列 `x=326.8125 / width=86`，状态 `y≈88.67`；lint 0 errors（保留既有 15 条 warnings），构建发布 `index-xm4J6F-7.css` / `index-Dw6j_s_m.js`。
# 本轮：修复手机工具栏虚线高度归零（已完成）

- [x] 确认 `height:auto` 父级导致分隔线百分比高度无法解析。
- [x] 工具栏设为参考站最终计算高度 `8.6rem`，虚线公式得到 `6rem`，并增加 `6rem` 最低高度兜底。
- [x] lint、构建与线上 CSS 校验通过；线上资源为 `index-Cwvj6uTD.css`。

### 本轮：结果出现后结构性移除“更多”记录区（已完成）

- [x] 根据参考站 `textList ? result : records` 逻辑，将结果可见状态上报给主布局。
- [x] 有结果时不再渲染完整 `aside`，因此“查看更多 / 停押降水”及历史卡片同时消失。
- [x] 清空结果或离开快录页面时恢复记录组件。
- [x] 完成 lint、生产构建和线上资源核对。
- 验证：lint 0 errors（保留既有 15 条 warnings）；线上 HTML 已引用 `index-CdUhPlfv.js` / `index-Cwvj6uTD.css`，生产 JS 包含结果可见性状态同步。
# 本轮：按参考源码对齐手机选项栏与记录操作栏（已完成）

- [x] 从参考站打包源码确认选项栏、开关和记录操作栏的真实 flex/rem 样式。
- [x] 选项栏恢复 `96% / 7rem / space-between`，卡片按 `14rem` 基准自动收缩并使用 `.6rem` 间距。
- [x] “查看更多 / 停押降水”恢复 `4.8rem` 外层、`3.6rem` 按钮、`.8rem` padding 与 `1.5rem` 间距。
- [x] lint、构建和线上资源检查通过；发布资源为 `index-CfoND2iw.css`。
# 本轮：修正手机选项开关内部文字位置（已完成）

- [x] 对比参考站 Ant Design 3 与当前 Ant Design 6 的开关内部 DOM/CSS。
- [x] 选中文字固定在左侧 `.6rem`，未选中文字固定在右侧 `.6rem`，隐藏非当前状态文字。
- [x] lint、构建与线上 CSS 检查通过；发布资源为 `index-BpMRbZiM.css`。

### 本轮：按参考站对齐下单消息提示（已完成）

- [x] 从参考站生产源码确认消息位置、字号、边框、底色、阴影和间距。
- [x] 补齐“下注中...”提示及“应打/实打”成功文案。
- [x] 将 Ant Design 3 的消息样式映射到当前 Ant Design 6 DOM。
- [x] 完成 lint、生产构建和线上静态资源检查。
- 验证：user-h5 lint 0 errors（保留既有 15 条 warnings），生产构建发布 `index-BQM6Vn54.css` / `index-Bu7qLr-9.js`；线上 CSS 已包含 `top:30vh`、黄色消息卡和黑色边框，线上 JS 已包含两段下单文案。未执行真实下注。

### 本轮：修正新版消息外壳造成的下单提示差异（已完成）

- [x] 确认 Ant Design 6 的消息容器、定位和卡片 DOM 与参考站 Ant Design 3 不同。
- [x] 下单反馈改为独立的参考站同结构提示层，不再受新版消息外壳影响。
- [x] 普通业务消息恢复 Ant Design 默认样式，仅下单中和下单成功使用参考样式。
- [x] 完成 lint、生产构建和线上资源检查。
- 验证：user-h5 lint 0 errors（保留既有 15 条 warnings），构建发布 `index-CSRNllLk.css` / `index-WCbNTYic.js`；线上产物包含独立提示层结构及样式。未执行真实下注。

### 本轮：补齐多行合并标记并统一结果颜色（已完成）

- [x] 从参考站生产源码确认“合”的条件是 `ij=1`，表示该结果由多行语句合并而成。
- [x] 用指定的 8 行输入检查当前解析结果，确认后端返回 8 条成功记录但未携带批次元数据。
- [x] 前端重建物理行时补齐合并批次、合并文本和末行状态，前 7 行不再误标为“新”。
- [x] 成功行统一为参考站 `#a7e1ce`，福标签为 `#7a2bc3`，“合/详”分别使用 `#037bab/#036564`。
- [x] lint、生产构建和线上资源校验通过；发布 `index-CN1c61sJ.css` / `index-CTYfrBYS.js`。
# 本轮：对齐手机记录标题中的盘名和时间（已完成）

- [x] 对比参考站与当前站标题行 DOM 和计算样式。
- [x] 将盘名从 `vertical-align: middle` 恢复为参考站的基线对齐。
- [x] 完成 lint、生产构建，并刷新原页面核对线上计算样式。
- 验证：盘名为 `vertical-align: baseline`，字体/行高继续保持参考站的 `1.6rem/2.6rem`；lint 0 errors（保留既有 15 条 warnings），发布 `index-CBNVQHWW.css` / `index-CXqQq4uk.js`。
# 本轮：修复真机号码页底部遮挡与退出图标缺失（已完成）

- [x] 号码页从固定 `100vh` 改为带兼容回退的 `100dvh`，仅保留内容区滚动并增加底部安全区。
- [x] 将依赖系统字体的 `⏻` 字符替换为参考站同类 SVG 电源图标。
- [x] 隐藏退出按钮旧文字节点，确认 SVG 在 430px 页面中水平、垂直居中。
- [x] lint 0 errors（保留既有 15 条 warnings），生产构建发布 `index-C8vQ3l6R.css` / `index-BOHNKXwI.js`，原页面已刷新。
