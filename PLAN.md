# 项目实施计划

最后更新：2026-08-20

## 当前状态

- [x] 抓取并归档线上用户端构建产物、SVG、登录页、验证码与认证后路由资料。
- [x] 确认 MySQL `192.168.2.18:3306` 与 Redis `192.168.2.18:6379` 网络可达。
- [x] 建立 monorepo、管理端、用户端和 ThinkPHP API 骨架。
- [x] 建立多租户、一级/二级代理、站点、域名和 RBAC 数据模型。
- [x] 建立数据库迁移、升级状态和初始化数据。
- [x] 完成管理端登录、动态路由、左侧菜单和固定滚动工作区。
- [x] 完成代理、站点、域名、角色和菜单管理页面。
- [x] 完成用户端登录、验证码、责任声明和主布局。
- [ ] 逐步实现快速录入、明细、账单、会员资料、开奖记录与规则页面。
- [ ] 完成 Redis 会话、验证码和缓存支持。（配置已接入；当前 PHP 构建未包含 redis 扩展，默认文件缓存）
- [x] 完成前后端联调、测试、构建与启动脚本。
- [x] 修复带连字符资源的 ThinkPHP 路由，二级代理创建与列表已联调。
- [x] API 异常已统一为 JSON 错误响应，避免调试 HTML 泄露到前端。
- [x] 修复管理端登录对 API envelope 层级变化的兼容性，避免读取 `null.token`。
- [x] 将站点与域名合并为“代理中心”，站点记录归属代理并在列表中聚合域名。
- [x] 二级代理创建校验必须绑定有效一级代理；平台导航隐藏独立站点/域名菜单。
- [x] 收敛为唯一“代理中心”菜单；站点包含独立管理员账号、密码、手机号和反代域名字段。
- [x] 代理中心支持“进入站点”跳转到站点反代域名，管理员密码仅保存哈希且不回传。
- [x] 数据库升级 `202608180002 AddSiteManagerFields` 已执行。
- [x] 用户增加 `site_id` 站点范围，站点管理员创建时同步建立站点用户，登录支持站点范围校验（`202608180003`）。
- [x] 用户端登录页恢复线上快照中的 `login-logo.svg`，不再使用文字占位图标。
- [x] 用户端错误提示改为 Toast，并拆出 `Toast`、`CaptchaModal` 组件，避免登录交互继续堆在单一页面结构中。
- [x] 用户端登录重写为 Ant Design 组件，使用 `message.error` 自动消失提示、`Input`、`Modal`、`Button` 和图标组件；账号密码输入框移除占位符。
- [x] 对照参考截图调整登录前缀图标、电源图标、验证码数字按钮颜色与弹窗 footer 按钮位置。
- [x] 完善验证码交互：补充分割线、两位长度限制、固定数字顺序、圆角卡片和无光效悬浮/点击状态；电源入口使用原生按钮。
- [x] 验证码数字改为每次打开随机洗牌，并使用专用按钮样式强制覆盖 Ant Design 默认 hover/focus 光效。
- [x] 优化验证码布局分割线、答案框宽度与 footer 登录按钮间距。
- [x] 按参考站实测参数将原生登录按钮调整为 36×36px、24px 图标，并按需求改为黑色边框。
- [x] 验证码确认后始终关闭验证码弹窗；错误改用 Ant Design 错误对话框显示“验证码已失效”。
- [x] 接入真实算术验证码接口：服务端随机生成 0-18 以内算式，混合阿拉伯/中文数字，返回带噪点干扰线的 SVG 图片并缓存答案；前端提交验证码 ID 进行校验。
- [x] 放大验证码 SVG 内部算式字符，保持图片框尺寸不变并提升可读性。
- [x] 修复认证响应校验：只有验证码验证成功、登录响应 `code=0` 且返回 token 时才允许进入用户端。
- [x] 区分验证码错误与验证码失效：答案不匹配返回“验证码错误”，缓存不存在/过期返回“验证码已失效”。
- [x] 登录成功后增加责任声明协议页；同意后进入主界面，不同意则清理登录状态并退出。
- [x] 按线上协议页快照修正宽屏卡片宽度、正文内边距、提醒区和底部按钮布局。
- [x] 用户端首页导航接入已拉取的 SVG 图标资源，登录鲸鱼复用 `login-logo.svg`，并补齐选中/未选中样式。
- [x] 首页顶部公告接入站点动态配置，支持跑马灯、悬浮下划线和点击公告信息弹窗；管理端系统配置可编辑公告。
- [x] 按线上首页实测将导航 SVG 调整为 25px，外层圆形边框 38px，并对齐选中项边框与导航项尺寸。
- [x] 按首页参考图修正头部层次、蓝色鲸鱼标识、四项账户 outlined 字段和金额整数显示。
- [x] 首页头部导航改为从彩票列表右侧靠左排列，保持统一间距。
- [x] 放慢公告跑马灯，并将总金额、更多、居右移到左侧余额栏，快速录入区不再重复显示总金额。
- [x] 压低彩票列表并修正头部内边距，主题区增加 5px 顶部间距，总金额按钮增加间隔并接入 `arrow-right.svg`。
- [x] 按线上 DOM 实测重写彩票列表两行结构，外框、行高、虚线、字号和列布局与快速录入页一致。
- [x] 按线上工具栏 DOM 修正左栏宽度 250px、工具栏 40px、按钮组右对齐及按钮尺寸。
- [x] 实现“居右”面板切换：左右交换主区域和余额栏，按钮可切回“居左”。
- [x] 快速录入选项改为 Ant Design Switch，按线上尺寸和颜色实现，并接入悬浮 Tooltip 说明。
- [x] 快速录入开关和默认彩种外框改为账号字段同款浮动 label outlined 样式。
- [x] 修正快速录入区域层级：白色选项区域承载多个独立 outlined 模块，统一内容居中并增加福彩 3D 与排列三间距。
- [x] 修正快速录入选项区域宽度和上下留白，使其与输入区域左右对齐且不再紧贴相邻模块。
- [x] 优化快速录入卡片细节：增加轻微圆角、上下内边距、卡片内部留白及首个卡片左侧间距。
- [x] 对齐首页业务颜色：已用红色、信用和操作按钮金黄色、可用绿色，并放大默认彩种文字。
- [x] 修正默认彩种卡片按钮被压缩换行的问题，固定按钮不换行并横向并排展示。
- [x] 将“＋新标签”固定到替换模块右上角，与替换输入行保持同一水平位置。
- [x] 按线上结构将替换模块拆成输入行与新标签行，中间增加虚线分隔并把“＋新标签”放到下方右侧。
- [x] 为快速录入文本框增加线上样式的清空/字数统计底栏，限制最多输入 10,000 个字符。
- [x] 将右下角温馨提示重构为可展开消息面板，补齐标题栏、正文、圆形收起与关闭按钮。
- [x] 将快速录入“规则说明”接入独立 Ant Design 弹窗组件，增加规则页签、滚动正文和重点提示样式。
- [x] 修正规则说明弹窗的 flex 高度链路，使页签正文区域能够独立滚动且不被 footer 截断。
- [x] 根据浏览器实际 DOM 将滚动样式适配到 Ant Design 当前版本的 `.ant-tabs-body-holder`，并补齐会产生真实溢出的规则正文。
- [x] 规则说明改为站点级配置：管理端系统配置新增四类规则编辑，后端保存规则设置，用户端弹窗读取对应站点内容。
- [x] 实现左侧“更多”查询视图，按线上布局切换日期筛选、原始文本搜索、搜索/返回按钮和总金额展示。
- [x] 将快速录入页的“投注记录”页签改为独立记录查询表格，补齐筛选、时间范围、导出和空数据状态。
- [x] 补齐“停押降水”页签：查号码、类型、彩种、分类、日期、排序和数据表格。
- [x] 对齐快速录入交互：新增“添加新标签”弹窗、标签展示和文本替换功能。
- [x] 按线上截图重做添加新标签弹窗：500px 宽度、蓝色输入框、分隔 footer、橙色关闭和深蓝确认按钮。
- [x] 创建用户业务数据表并接入查询 API：投注记录、投注明细、历史账单和开奖数据，按站点/用户隔离并完成数据库迁移。
- [x] 投注记录页接入真实 `/user/bet-records` 查询，支持中奖状态、日期和原始文本筛选并渲染返回记录。
- [x] 将下注明细、历史账单、开奖号码、会员资料和修改密码导航接入独立页面组件；前三者读取真实业务查询接口。
- [x] 修改密码页面接入用户密码修改接口，完成原密码、复杂度、确认密码校验。
- [x] 完成线上用户端全导航审查：快速录入、下注明细、历史账单、会员资料、开奖号码、规则说明、修改密码及更多/停押降水功能结构已记录。
- [x] 将投注记录起止时间框改为整块可点击并调用浏览器日期时间选择器。
- [x] 按线上样式将投注时间起止值合并到同一个 outlined 框，并改用 Ant Design 搜索/导出图标。
- [x] 投注记录中奖筛选补齐“全部 / 未中奖 / 已中奖”三个选项。
- [x] 站点免密进入的 `auto_token` 也纳入正式登录状态流，进入后先显示责任声明协议。
- [x] 代理中心新增/编辑改为右侧抽屉，编辑动作调用站点更新接口。
- [x] 修复代理中心管理员字段显示英文数据库名的问题，补齐中文列标签。
- [x] 修复代理中心保存携带旧 `parent_id` 字段导致 SQL 错误，并完善站点编辑时域名与密码处理。
- [x] 代理中心移除站点编码展示与表单字段；无反代域名时进入站点会明确提示。
- [x] 增加站点用户管理菜单、站点筛选、创建和逻辑删除接口。
- [x] 调整管理表格颜色对比度，避免表头和内容灰度过低。
- [x] 通过右侧浏览器实测代理中心：无残留联调数据、站点配置抽屉显示反代域名、站点用户页面和菜单可访问。
- [x] 代理中心搜索提示和新增按钮改为站点业务语言，不再显示编码概念。
- [x] 修复操作列按钮挤压：固定 230px 宽度、横向不换行按钮组；并在右侧浏览器真实保存验证通过。
- [x] 进入站点前校验反代域名，URL 构造异常改为中文提示，不再显示浏览器英文异常。
- [x] 完成平台站点用户管理：列表、站点 ID 筛选、新增、编辑入口和逻辑删除；已在右侧浏览器真实创建验证。
- [x] 站点用户筛选和编辑改为站点下拉框，显示“站点名称（当前站点）”；右侧浏览器已验证选项加载。
- [x] 修复站点废弃 `code` 字段仍为必填导致保存失败，迁移 `202608180005` 后在右侧浏览器真实新增并收到“站点创建成功”。
- [x] 增加站点管理员一键进入站点接口：生成一次性用户 token，带反代域名跳转并自动登录。
- [x] 拆分平台管理员、站点管理员、站点用户表；用户名唯一范围改为平台全局或站点内，站点及账号统一逻辑删除（`202608180004`）。
- [x] 验证两个不同站点可创建同名站点管理员，平台管理员登录正常。
- [x] 平台自有站点纳入站点模型（`202608180006`）；平台用户管理可选择全部站点及平台自有站点，站点管理员不显示站点选择且服务端强制按其所属站点读写。
- [x] 站点用户列表改为展示所属站点名称，不再暴露站点 ID；浏览器已验证筛选和新增抽屉的纯名称选项。
- [x] 责任声明改为站点级系统配置：标题和多行正文可在管理端保存，用户端登录后按令牌所属站点读取（`202608180007`）。
- [x] 责任声明正文改为受限 Markdown：后台保存原始 Markdown，用户端支持标题、加粗、列表、引用和表格，HTML 不解析。
- [x] 修复用户端协议状态刷新丢失：未点击同意时刷新仍停留协议页，已同意状态按当前登录令牌记录。
- [x] 责任声明改为 Vue 3 富文本编辑器：支持选中文字设置颜色、加粗、字号、对齐、列表等，保存 HTML 并在用户端安全还原样式。
- [x] 修复用户端 CSS 覆盖富文本颜色：`strong` 改为继承外层选区颜色，不再强制覆盖为黑色。
- [x] 增加站点用户余额模型和展示：余额、信用余额、已用余额持久化，总余额与可用余额由后端计算，管理端可编辑，用户端顶部显示。
- [x] 系统配置按功能拆分为“责任声明 / 首页公告” tabs，切换后只显示当前功能对应的表单和保存按钮；页面固定在可视高度内，正文输入区域内部滚动。
- [x] 修复用户端公告请求未携带登录令牌导致接口 401 并回退旧固定文案；现在按当前用户站点读取管理端保存的公告。
- [x] 用户端请求层按 `utils/request`、`api/auth`、`api/user` 分层，增加全局请求拦截器自动注入令牌、全局响应拦截器统一处理业务错误和 401 退出。
- [x] 新增 React 代理端首版框架：按线上实测绿色主题复刻登录页、海豚 SVG、真实算术验证码、代理主页面布局、独立请求拦截器与 agent API/token scope；数据库升级 `202608180009` 已执行。
- [x] 修复本地代理站点进入与登录：允许 `localhost`/本地 IP，自动使用本地 HTTP 和 5176 端口；站点编辑同步 `site_admins`，代理登录兼容站点管理员账号及平台自有站点。
- [x] 按线上代理端实测重排代理首页：顶部公告、Logo/单一代理账号框、12 项绿色导航图标、选中态、彩种状态条分区显示；左侧不再显示会员数量、额度或汇总信息。
- [x] 代理账号区按线上最终模型显示单个 130×40 浮动标签框：“账号”压在上边框，框内显示“代理：真实账号名”。
- [x] 代理账号框改为按用户名自适应宽度，并增加超长账号省略保护。
- [x] 按线上截图实现代理端“总货概览”：位置面包屑、五个查询页签、账号/来源筛选、日期区间、明细表头和暂无数据状态；页签、下拉框和筛选输入均可操作。
- [x] 系统配置规则说明四个页签改为 WangEditor 富文本编辑器；旧纯文本自动转段落 HTML，用户端清洗并渲染富文本格式。
- [x] 代理端总货概览查询区按线上结构调整为三个带边框字段组，提交按钮改为外层框加内层红色描边按钮。
- [x] 管理端新增按站点隔离的“代理端协议”和“代理端公告”配置；代理端登录后读取服务协议，确认后进入主页并读取公告；修复进入站点路由被列表路由截获的问题。
- [x] 代理端服务协议改为独立深绿色主题，同意按钮同步为绿色，不再沿用用户端蓝色背景。
- [x] 修复首页公告和代理端公告正文覆盖保存按钮：取消 textarea 固定视口高度，正文区域弹性填充并内部滚动，底部保存栏固定可见。
- [x] 修复代理端协议回退到用户责任声明：后端和前端均使用独立代理协议默认值，无效代理 token 返回 401；浏览器验证标题和正文来自代理端配置。
- [x] 拆分站点域名用途：`domains.domain_type=agent/user`；平台进入站点只取代理端域名，代理登录只接受代理端域名，用户登录只接受用户端域名；代理中心表单分别配置两类域名。
- [x] 新增代理端规则说明页面：代理端通过 `/agent/rules` 读取当前站点配置，支持基础玩法、特殊打法、总金额、文本规范四个页签；富文本正文安全清洗并保留颜色/加粗/字号，正文区域独立滚动；已在右侧浏览器打开并验证页签切换。
- [x] 新增平台彩票目录和站点分配：迁移 `202608190005` 创建 `lotteries`、`site_lotteries`，管理端新增“彩票列表”页面，可新增、编辑、停用、删除和按站点分配；用户端/代理端只读取当前站点已分配的彩票，默认彩票已为现有站点初始化分配。
- [x] 新增开奖接口配置和开奖历史：系统配置增加开奖 Base URL；彩票列表增加“开奖历史”页面；后端按彩票编码映射第三方 `type`，首次打开历史页最多同步 10 页、每页最多 1000 条，已存在期号跳过并支持分页查询；迁移 `202608190006` 创建 `lottery_histories`。
- [x] 拆分管理端“系统配置”和“站点配置”：`/settings` 只维护全平台开奖接口 Base URL；`/site-settings` 保留按站点选择的责任声明、公告、协议和规则配置；侧边栏新增站点配置入口并已在右侧浏览器验证。
- [x] 系统配置开奖接口增加连通性测试：请求 `pls` 最小数据页，展示接口可用状态、HTTP 状态和毫秒响应时间；右侧浏览器实测返回 HTTP 200、响应约 450ms。
- [x] 站点配置增加参考站点填充：选择其他站点后可一次性填入责任声明、公告、代理协议、代理公告和规则内容；仅修改当前表单，不自动保存，已增加提示并完成构建验证。
- [x] 重构开奖历史同步：历史查询接口只读取数据库；新增 `lottery:sync` 后台命令，每分钟为每个启用彩票仅拉取最新 1 条并按期号去重；历史页每分钟静默刷新数据库结果，新期号直接出现在第一页首行，不显示加载动画。
- [x] 执行排列三一次性历史回填：`lottery:backfill 2 10 1000` 请求 10 页且无失败页；第三方将请求的 1000 强制限制为每页 500，实际接收 5000 条，新增 4999、更新 1，数据库总数 5000；日常同步策略保持每分钟 1 条不变。
- [x] 执行福彩3D第1至10页、每页10条的一次性回填：首次请求遇第三方短期限流，增加逐页间隔和最多3次重试后重新执行；100条全部处理成功、失败页0，因期号均已存在所以新增0、更新100，数据库总数保持500。
- [x] 按限流要求重新执行福彩3D回填：第1至10页、每页10条，每页间隔10秒；100条全部成功，失败页0，新增0、更新100。
- [x] 全量回填福彩3D和排列三：按接口实际总页数16页、每页500条、页间等待10秒串行执行；福彩3D收到7727条（新增7227、更新500），排列三收到7695条（新增2695、更新5000），两者失败页均为0；完成后恢复每分钟仅同步最新1条。
- [x] 彩票列表新增赔率详情：迁移 `202608190007/008` 创建并扩展彩票赔率表，支持类别、赔率项、最小下注、赔率上限、单注上限、单项上限、赔率、离线赚水、启停和排序；两个彩种初始化10个参考赔率项，新增/编辑/删除接口和抽屉已在浏览器验证。
- [x] 按参考代理站补充三位数彩票默认赔率：福彩3D和排列三各写入26项独立配置，包含定位、独胆、双飞、对子、直选、组三两码至全包、组六四码至九码等；浏览器验证两边均为26项且关键赔率项存在。
- [x] 赔率详情改为后端分页查询：默认每页20条，支持10/20/50/100切换；新增后回第一页，编辑和删除保持当前页，删除当前页最后一条时自动回上一页；浏览器验证总数26、第一页20条、第二页6条。

## 本轮执行步骤

1. 创建工程规范与问题记录文件。
2. 在外接盘 `.runtime` 中安装独立 PHP 和 Composer。
3. 创建 Vue 管理端与 React 用户端。
4. 创建 ThinkPHP API 和 `.env` 配置。
5. 创建并执行第一版 MySQL 迁移。
6. 安装依赖、启动服务并验证健康检查与前端页面。

## 验证记录

- Node.js：v22.22.1
- npm：11.12.1
- 系统：macOS ARM64
- PHP：8.3.33，外接盘 `.runtime/php/php`，已验证 mysqli/PDO/MySQL/mbstring/openssl/zip
- MySQL 端口：已连通
- Redis 端口：已连通
- 数据库迁移：`202608180001 CreateSaasTables` 已执行
- API 健康检查：`GET /api/v1/health` 返回 `database=ok`
- 前端构建：管理端、用户端、代理端均通过 `npm run build`
- 用户端 Toast：空账号提交显示右上角错误 Toast，验证码弹窗通过独立组件渲染。
- 浏览器验证：Playwright 已验证管理端登录、用户端验证码登录和快速录入主界面
- 二级代理接口：创建返回 `code=0`，列表返回 `total=1`
- PHP 语法检查：`server/app`、`server/config`、`server/route` 全部通过

## 下一步

### 本轮：代理端四个明细页面与功能（进行中）

- [in_progress] 盘点参考站“总货明细 / 中奖明细 / 投注明细 / 查看退码”四页的筛选字段、表格列、分页与交互。
- [ ] 审计并补齐代理端真实查询 API，严格按当前代理站点隔离。
- [ ] 实现四个 React 页面、共享查询组件、导航切换、加载/空状态和错误处理。
- [ ] 完成 PHP 语法、代理端 lint/build、API 回归和内置浏览器逐页验证。
- [ ] 更新文件化计划、问题记录和交付说明。

### 本轮纠正：彩票状态条与下方导航存在 2px 横向缝隙（已完成）

- [x] 按用户截图清除概览页顶部 2px 内边距，使彩票状态条与下方绿色导航直接贴合。
- [x] 代理端 oxlint 通过（0 错误，仅保留 Agreement.tsx 既有 2 条 Fast Refresh 警告），Vite 生产构建成功并发布。
- [x] 内置浏览器实测彩票状态条 bottom=180、下方导航 top=180、间距=0；截图确认无横向白缝，控制台无报错且无横向溢出。
- [x] 将本次用户纠正追加到 PROBLEM_LOG.md。

### 本轮纠正：彩票项透明左边框形成白线（已完成）

- [x] 修正上一轮误判，移除未选中彩票项会透出白底的透明左边框，并用选中项 1px 绿色外阴影覆盖渲染接缝。
- [x] 代理端 oxlint 通过（0 错误，仅保留 Agreement.tsx 既有 2 条 Fast Refresh 警告），Vite 生产构建成功并发布。
- [x] 内置浏览器放大截图与像素复核：交界 `x=196-202` 保持绿色，白底从 `x=203` 才开始；控制台无报错且无横向溢出。
- [x] 将本次用户二次纠正追加到 PROBLEM_LOG.md。

### 本轮补充：彩票项交界白线清理（已完成）

- [x] 按用户截图将选中彩种右边缘改为 1px 绿色分隔线，清理与下一彩种交界处的竖向白线，同时保留底部白色虚线。
- [x] 代理端 oxlint 通过（0 错误，仅保留 Agreement.tsx 既有 2 条 Fast Refresh 警告），Vite 生产构建成功并发布。
- [x] 内置浏览器实测选中项 `border-right: 1px solid rgb(0,100,0)`、`border-bottom: 1px dashed rgb(255,255,255)`；控制台无报错且无横向溢出。
- [x] 将本次用户纠正追加到 PROBLEM_LOG.md。

### 本轮补充：彩票状态条选中项底部虚线（已完成）

- [x] 对比参考站彩票状态条的真实边框样式，补齐选中项底部 1px 白色虚线与状态条四周 2px 绿色边框。
- [x] 代理端 oxlint 通过（0 错误，仅保留 Agreement.tsx 既有 2 条 Fast Refresh 警告），Vite 生产构建成功并发布。
- [x] 内置浏览器 1567px 视口实测：状态条 `x=0/y=130/1567×50px`，选中项 `x=2/y=132/200×46px`，底边为 `1px dashed rgb(255,255,255)`；控制台无报错且无横向溢出。
- [x] 将本次用户纠正追加到 PROBLEM_LOG.md，记录防复发检查。

### 本轮补充：总货概览筛选栏内部控件精确对齐（已完成）

- [x] 按用户浏览器标注复核筛选栏内部结构，改用参考站的浮动 label 容器并对齐输入框、下拉框和提交按钮。
- [x] 代理端 oxlint 通过（0 错误，仅保留 Agreement.tsx 既有 2 条 Fast Refresh 警告），Vite 生产构建成功并发布。
- [x] 内置浏览器 1567px 视口实测：字段框 x=578.5/758.5/878.5、输入框 x=588.5/y=263.5/150×24、来源框 x=768.5/y=265.5/70×20、提交按钮 x=913.5/y=261.5/50×28；输入字号 14px，控制台无报错且无横向溢出。
- [x] 将本次用户纠正追加到 PROBLEM_LOG.md，记录防复发检查。

### 本轮：代理端总货概览精确复刻参考站（已完成）

- [x] 对比参考站与当前站的 DOM、计算样式、坐标和可见交互，修正顶部信息区、导航、彩票栏与概览筛选表格。
- [x] 将来源选项、15 日动态日期范围和可展开温馨提示结构对齐参考站，保留当前动态账号、彩票与公告数据。
- [x] 代理端 oxlint 通过（仅保留 Agreement.tsx 既有 Fast Refresh 警告）；Vite 生产构建通过并发布，内置浏览器复核控制台无错误、无横向溢出。
- [x] 1567px 视口实测：Logo x=28、账号框 x=195、导航 x=400、筛选区 x=10/y=233、表格标题 x=25/y=325，与参考站关键坐标一致；温馨提示展开/收起高度为 200/40px。

### 本轮：代理端按站点动态获取彩票（已完成）

- [x] 收紧代理端彩票接口，只返回当前登录站点已分配且启用的彩票，并附带最新期号信息。
- [x] 代理端彩票栏改为首次加载、窗口聚焦和每分钟静默刷新，移除固定期号与倒计时数据。
- [x] PHP 语法和代理端构建通过；数据库分配、真实下一期号、浏览器动态展示及控制台无错误均已验证。

### 本轮补充：代理端彩种选中态（已完成）

- [x] 彩种栏每个动态彩种改为可点击选项，仅当前选中项显示绿色背景。
- [x] 刷新分配列表时保留有效选中项，被取消分配后自动选择剩余彩种。
- [x] 代理端构建通过；浏览器点击与刷新验证选中态正确切换并保留，控制台无错误。

### 本轮：彩票权限调整为站点配置（已完成）

- [x] 将彩票可见范围从“彩票列表分配站点”移动到“代理中心站点配置”。
- [x] 站点抽屉支持选择该站点可见的彩票，保存时在事务中覆盖站点分配，空选择代表全部不可见。
- [x] 彩票列表已移除分配控件；PHP 语法和管理端构建通过，浏览器验证站点配置回显、保存及代理端动态隐藏福彩3D。

### 本轮：代理端真实开奖倒计时布局（已完成）

- [x] 彩种块调整为上排“彩种名称 / 下期期号”、下排“开盘状态 / 真实倒计时”。
- [x] 按 `next_open_time` 判断开奖日凌晨至开奖前为“开盘中”，开奖日前为“未开盘”，开奖后为“已封盘”。
- [x] 代理端构建通过；浏览器核对字段位置、两秒连续递减、选中样式及控制台无错误。

### 本轮：代理端彩票请求失败静默处理（已完成）

- [x] 移除“彩票列表加载失败”弹窗；首次加载失败仅显示空状态，后台静默刷新失败保留当前彩种，不清空页面。
- [x] 代理端 TypeScript 构建通过。

### 本轮：代理端下级管理（已完成）

- [x] 新增代理范围的下级会员列表、新增和编辑接口，严格按当前登录站点隔离。
- [x] 按参考页实现信用额度汇总、筛选、新增会员、列表、分页和操作列。
- [x] 保留代理下与代理账号同名的真实会员，列表和信用额度汇总按站点会员表完整统计。
- [x] 按参考站实测尺寸重写筛选栏：字段框、输入框、状态选择、搜索和新增按钮位置及视觉参数一致。
- [x] 按参考站重做下级管理面包屑：双右箭头、路径文字、右侧页签及虚线分隔均使用实测尺寸。
- [x] PHP 语法和代理端构建通过；浏览器验证筛选、空状态、新增页面、分页及操作列布局正常。
- [x] 参考线上“新增下级”实现独立新增/编辑会员页面，包含账号等级、账号密码和动态彩种权限矩阵。
- [x] 新增会员彩种权限数据库升级 `202608190012`；创建、编辑、详情回显均按当前代理站点隔离。
- [x] 用户端彩票可见范围和快速下注均执行服务端权限校验；关闭彩种权限会同步关闭下注权限。
- [x] 浏览器验证新增页面、权限开关联动、编辑入口和返回导航；未创建测试会员、未修改现有会员权限。
- [x] 按参考站拆分独立“修改账号”页面：账号状态、密码、代号/备注、信用额度、拦货占成、逐彩种权限、离线赚水及会员赔率覆盖。
- [x] 新增会员账号配置迁移 `202608190013`，后端详情/更新接口支持上述字段和赔率覆盖，数据库升级状态为 `up`。
- [x] 浏览器实测 `zhang10867779` 修改页：真实回显两种彩票权限和 52 条赔率行；修复代理主题溢出隐藏，底部提交/返回按钮可滚动到；未提交表单。
- [x] 按参考站截图继续校准修改页：内容宽度调整为 1045px，基础资料/密码/权限/额度/拦货/平均赚水分区重排；赔率表改为除离线赚水外全部纯文本只读，表头和数字格式与参考站一致。
- [x] 根据最新截图修正主内容和信用额度尺寸：恢复参考站 `1200px` 主宽度，信用额度内框调整为 `1062px`，输入框 `86px`、归零按钮 `46×28px`，文字保持单行；浏览器实测通过。
- [x] 按最新参考截图调整拦货占成和操作区：拦货内框、黄色提示条、下拉框按参考站重排；平均离线赚水、提交、返回合并为 30px 同行区域，按钮位置和字号已在浏览器复核。
- [x] 根据最新参考截图纠正容器尺寸：修改页主容器恢复为 `1200px`，信用额度/拦货内框为 `1062px`，账号区 88px、密码权限区约 131px、拦货内框 97px；浏览器实测与参考图一致。
- [x] 使用内置浏览器同时接管参考页和本地页读取 computed style，按真实结构重做：摘要 44px、下间距 20px、资料区 88px、密码权限区 131px、额度外层 78px、拦货外层 125px、平均赚水 30px、表头 40px；同步修正内部虚线分隔、密码文案、额度数值和按钮。
- [x] 按参考站重做修改页下半部赔率表：8 列改为 7 列精确列宽，合并两个同玩法彩种为一套 26 项配置，增加 6 个类别分组行、定位玩法简称、同值同步开关和 600px 内部滚动；基础赔率保持只读。

### 本轮：站点多访问域名（已完成）

- [x] 代理端域名、用户端域名改为动态列表，支持新增、删除和设置唯一主域名。
- [x] 后端在事务中校验域名全局唯一并批量保存，兼容旧单域名字段。
- [x] PHP 语法检查和管理端构建通过；浏览器已验证当前配置保存、重新加载、多行动态新增及每组唯一主域名。

### 本轮：快速录入完整功能与接口（进行中）

- [x] 对照线上快速录入页面盘点文本替换、新标签、生成、下注、偏好开关、默认彩种、规则和金额预警功能。
- [x] 新增用户端快速录入 API 类型和请求方法，服务端实现解析预览与二次确认下注接口。
- [x] 为投注解析结果建立成功/失败行、失败原因、总码数和总金额展示。
- [x] 将标签和录入偏好按站点用户持久化，补齐标签列表/创建/删除与偏好接口。
- [x] 通过本地浏览器验证空文本、解析失败、解析成功和接口错误状态；重新登录后已验证真实预览接口与逐行结果展示。重复下注保护由独立事项继续跟踪。
- [x] 修复首页切换“投注记录”时顶部账号区因 flex shrink 发生宽度跳变，固定账户区 250px 并锁定鲸鱼标识宽度；同步压缩导航卡片间距以避免固定账号区造成头部横向溢出；本地浏览器切换前后实测尺寸一致。

下一阶段再继续实现其他页面和业务接口。

### 快速录入接口审计与补齐（2026-08-19，进行中）

- 已覆盖：生成预览、下注二次确认、用户偏好读取/保存、标签创建/删除后端、规则读取、前端金额/码数汇总与基础错误提示。
- 待补齐：线上真实解析/下注协议（`mb.tz` 的 `gtlcd`/`gbtdl` 与 `mb.bt`）、粘贴自动下注、立即识别、自动复制小票、标签删除/应用、预警持久化、期号/封盘/余额校验、事务与重复提交保护。
- [x] 扩展服务端解析器和统一码数/金额计算（中文数字、常见单位、玩法校验、长度限制）。
- [x] 实现前端粘贴自动识别/下注触发、立即识别、自动复制小票、预警设置持久化和标签应用/删除。
- [x] 增加下注余额校验、待分配期号标记、事务和短时间重复提交保护。
- [x] 浏览器已验证登录后的成功生成、失败提示、标签新增/删除、立即识别和预警持久化；下注确认已实际提交到本地 API，余额不足时返回提示并自动关闭确认框，未写入投注记录。
- [ ] 重复提交与系统剪贴板权限仍需在可控测试数据下补充验证。
- [x] 补齐投注记录 API 的分页、中奖状态/日期/原始文本筛选、加载状态、错误提示和 CSV 导出。
- [x] 新增停押降水数据表与 `/user/stop-drops` 查询 API，接入号码、类型、彩种、分类、日期、排序筛选及结果表格。
- [x] 浏览器验证“投注记录”和“停押降水”页签、筛选控件、空数据状态及查询加载流程。
- [x] 按浏览器标注将替换模块内标签列表下移 6px，并实测标签边界从 top 220px 调整到 226px。
- [x] 对照参考站补齐标签点击载入文本、红色圆形删除按钮和虚线标签卡片样式；构建已通过。
- [x] 按参考站实测重构修改密码页：150px 白色三栏、竖向虚线分隔、中栏规则说明、黄色禁用密码提示和居中保存按钮；保留真实修改密码接口。
- [ ] 修改密码页浏览器最终尺寸复核：本轮重新接管本地页被浏览器 URL 安全策略阻止，当前仅完成参考站实测与本地构建验证。
- [x] 实测并修正修改密码禁用密码提示条：14px 字体、36px 高、15px 圆角、无边框、`#ffffa1` 背景，红色“注：”使用 16px 粗体伪元素。
- [x] 为温馨提示收起/展开增加 280ms 高度、透明度和位移动画；改为保留内容节点过渡，不再条件卸载导致瞬间消失。
- [x] 移除温馨提示生硬的淡入淡出与位移叠加，修复固定 height 瞬间归零造成的卡顿，改为 240ms 纯高度滑动并同步平滑旋转箭头。
- [x] 按用户要求改用 `motion` 动画库：温馨提示高度与箭头旋转均使用 spring 动画，删除手写 transition；构建和 lint 已执行。
- [x] 快速录入结果改为逐行展示：每行原文对应一条成功或失败结果；成功行显示彩种、笔数和金额，失败行显示后端返回的具体原因，顶部汇总只统计成功行。
- [x] 修复中文“各/每/个”按字节读取导致单注金额未按笔数累计；`123 456 789 直选各1元` 已通过服务端回归和浏览器实测，结果为 3 笔、金额 3。
- [x] 将快速录入解析从控制器拆为 `QuickEntryParser` 服务，新增百十个定位集合笛卡尔积、福体双彩种计数和句末共/合计/总计校验；`福体百0234569十0234689个1235679各10合计6860` 已通过自动回归与真实页面验证，结果为 686 笔、6860 元。
- [x] 按参考站计算样式重构快速录入操作栏：70px 行高、26px 按钮、60px 虚线分隔、14px 汇总标签/16px 红色数值、97px 预警标签和 80×32px 浅蓝输入框；预警无配置或清空时显示并持久化为 0。
- [x] 按参考站重构快速录入文本区：textarea 与清空/计数栏合并在 212px 外框内，底栏固定 34px 并使用深蓝虚线；福彩3D为淡紫输入背景/紫色选中态，排列三为淡绿输入背景/绿色选中态。
- [x] 快速录入结果行补齐参考站交互：成功/失败行均支持新增和删除；成功识别每行只显示一个“详”，弹窗滚动展示全部号码，福体会展开为福/体两组；行增删后顶部码数和金额实时重算。
- [x] 按参考站重做号码详情弹窗：1000px 白色弹窗、详情总计行、体/福色标、玩法标题、8 组“号码/金额”表格列、浅蓝数据单元格、滚动区域和底部“关 闭”按钮。
- [x] 修正号码详情表格配对结构：每个号码与金额作为一对，每行固定 8 对并提供完整单元格边框；最后一行不足 8 对时，号码和金额均使用“—”补齐。
- [x] 强化号码/金额不可拆分结构：每一对使用独立两列容器和完整外边框，号码与金额之间不画分隔线，相邻对折叠边框连续排列，视觉上每行严格为 8 格。
- [x] 浏览器复核详情弹窗：弹窗距顶部 50px、无右上角关闭图标、每行固定 8 对；修复全局 `.empty` 样式污染导致空占位格被拉高，数据格与空格现均稳定为 32px。
- [x] 对照参考站修正详情汇总标题与表格：标题改为 16px/500 字重并使用信息图标，表格边框改为参考站的深色 `#5f5f5f`，表头高度同步为 33px。
- [x] 按参考站测量将详情“福/体”彩种色标下移 3px，补足色标与详情区域上边缘的间距且不改变表格布局。
- [x] 按参考站重构识别结果主行：顺序调整为减号、50px 编号、加号、120px 详情区、40px 福体标识和可编辑原文；主行及汇总行统一为 23px 高、14px 字体。
- [x] 新增并通过 `福体百2345678十2345689个0134579各7合计4802` 解析回归；页面实测为福体 686 笔、4802 元，详情体/福各 343 码并分别使用绿色和紫色标识。
- [x] 按参考站修正结果行“新增”：加号现在在当前行下方插入独立空白行，显示递增编号和蓝色“新”标识，不复制原投注文本或汇总数据。
- [x] 打通结果行与上方多行输入同步：编辑已有行会替换对应文本，新增行输入会追加一行；“立即识别”开启时编辑后自动解析，关闭时保留“新”状态直到点击“生成”。
- [x] 按参考站实测重构投注记录筛选栏：字段组固定为 88/370/398px、50px 高，控件和按钮尺寸、边框标签、颜色、日期秒级格式及 7px 表格间距均完成同步。
- [x] 统一所有业务空状态为 Ant Design 官方 `<Empty image={Empty.PRESENTED_IMAGE_SIMPLE} />`：用户端投注记录、停押降水、下注明细、开奖号码、通用页及代理端概览/占位页均已替换并通过浏览器检查。
- [x] 修复修改账号页信用额度内容溢出：内框改为外层左侧固定 15px，额度控件使用独立 28px 单行在 50px fieldset 内垂直居中；浏览器实测四向溢出均为 0。
- [x] 完成会员离线赚水两级联动：表头“同”同步全部 26 项，每个分类行新增独立“同”并只同步本组；浏览器验证全表、分类、单行三种状态且未提交测试数据。
- [x] 将会员离线赚水按“站点 + 代理 + 下级用户 + 玩法”持久化：迁移回填现有 104 条记录的代理归属，保存接口按当前代理隔离读写并校验 0-0.1；原值保存、刷新回显及数据库校验值回归通过。
- [x] 将管理端赔率配置改为树状模型：新增 `lottery_odds_categories` 类别表，玩法通过 `category_id` 挂载；赔率和限额字段允许留空并以 `NULL` 存储；管理端改为可展开类别/玩法树，支持类别 CRUD 和类别下新增玩法。
- [x] 赔率树默认收起并恢复分页：按类别分页，每页类别下完整保留其玩法；管理端默认不展开任何类别，分页器显示类别总数并支持切换每页数量。
- [x] 支持类别直接作为玩法：类别节点增加“类别本身就是玩法/类别下挂玩法”类型；玩法类别可直接保存六个可空赔率字段，不再强制创建子玩法，普通类别行为保持不变。
- [x] 按提供的赔率表补充福彩3D和排列三玩法：更新组六六码/组六七码为 1.2931/1.2605，新增对子全包、组六2胆拖（2码拖2至拖8）和单选全胆拖（1码拖2至拖8）；迁移可重复执行且不会重复插入。
- [x] 修复赔率树分页切换：后端按类别读取 `page/page_size`，每个类别携带完整玩法；浏览器验证切换第二页后不再重复第一页。
- [x] 清理并按参考站真实赔率结构重建福彩3D/排列三：清空旧类别、玩法及会员覆盖，导入 22 个类别节点和 106 个可下注玩法（类别本身可直接作为玩法）。
- [x] 修复直接玩法节点的赔率展示：三码定位、独胆、双飞、对子、组六、组三等 `is_playable` 类别按玩法行渲染所有赔率字段。
- [x] 调整参考站玩法顺序：豹子全包紧随和值，对子全包紧随组六赖，福彩3D和排列三同步持久化。
- [x] 将直接玩法节点同步到代理端下级赔率表：代理接口合并 `is_playable` 类别并支持离线赚水保存；移除旧的“三码定位→口口口”名称映射。
- [x] 修正代理赔率表层级与顺序：直接玩法不再生成同名分类行；豹子全包挂在和值末尾，对子全包挂在组六赖末尾；接口全局排序在前端保持不变。
- [x] 对照参考站实测调整赔率行顺序和颜色：二码定位后依次为三码定位/独胆/双飞/对子/组六/组三/组三多码；白/青/黄/绿背景、蓝色名称与赔率上限、灰色边框全部一致。
- [x] 将离线赚水表头全局“同”默认状态改为未勾选，避免进入页面后修改单项时意外同步整表。

## 本地启动

### Git 仓库整合（2026-08-19，已完成）

- [x] 删除 `.runtime` 内两个依赖源码目录携带的嵌套 `.git` 元数据。
- [x] 在项目最外层初始化 `main` 分支，并将前后端、服务端和参考资料统一纳入单一仓库。
- [x] 过滤 `.runtime`、依赖目录、构建产物、运行日志、真实环境文件、编辑器配置和 Playwright 临时输出。
- [x] 完成待提交文件、大文件和私钥标记检查，创建首次整体提交。

- 管理端：`http://127.0.0.1:5173`
- 用户端：`http://127.0.0.1:5174`（`#/kb`）
- 代理端：`http://127.0.0.1:5176`
- API：`http://127.0.0.1:8000/api/v1/health`
