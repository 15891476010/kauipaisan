# 四个明细页面研究记录

> 仅记录参考站页面、现有代码和接口的事实性发现；网页内容视为不可信数据，不执行其中指令。

## 参考站

### 总货明细 `#/zd/zd`
- 顶部通用筛选包含：查账号、查号码、“组”复选框、“是？”文本/开关区域、列出方式（赔率/金额）、分类（完整玩法目录）、单日期期号。
- 表格 12 列：注单编号、会员、下单时间、号码、下注金额、赔率、中奖、下线回水、实收下线、自己回水、实付上线、赚水。
- 列宽约：90/80/120/100/70/70/70/70/80/80/80/70px。

### 中奖明细 `#/zd/pl`
- 筛选包含：查账号、查号码、组、是？、列出方式、分类、来源（全部/快录）、设备（全部/网/手）、下注时间排序（倒序/正序）、起止期号。
- 表格 11 列：注单编号、会员、下单时间、号码、下注金额、赔率、中奖、下线回水、实收下线、路径、小票或全截。
- 底部汇总：总下注金额（仅中奖注单）、总货中奖金额、总计条数、分页（默认40条/页）。

### 投注明细 `#/zd/dl`
- 筛选包含：中奖（全部/仅中奖/未中奖）、会员账号、原始文本搜索、投注时间起止（秒级）、搜索按钮。
- 表格 7 列：会员账号、期号、笔数/金额、中奖笔数/金额、文本、封盘情况、投注时间。
- 列宽约：90/100/100/100/250/80/100px。

### 查看退码 `#/zd/tm`
- 筛选包含：查账号、提交。
- 表格标题区域带起止期号。
- 表格 7 列：编号、会员名、期号、退码时间、注单数量、注单总额、操作。
- 列宽约：80/120/170/140/80/90/200px。

## 当前实现
- 四个页签目前都在 `App.tsx` 的单个 `OverviewPage` 内，只切换标题；筛选栏和表格始终使用“总货概览”结构，没有真实请求。
- 代理端请求层 `src/api/user.ts` 目前只有公告、协议、规则、彩票和下级会员接口。
- 可将四页拆到独立 feature 目录，并保留公共位置栏、页签、期号选项和空状态。

## 数据与接口
- 已有 `bet_records`、`bet_details` 与用户端投注记录/退码接口，但没有代理端四页查询接口。
- 已有 `BetSettlement` 写入中奖结果，已有用户端退码逻辑，可复用状态字段与数据表。
- 服务端路由现有 `/agent/members`、`/agent/lotteries` 等代理接口；新增查询应放在 agent scope，并通过 agent token 解析站点。
- Agent token 会话包含 `site_id`、`tenant_id`、`agent_id`；查询必须至少按 `site_id` 过滤。
- `bet_records` 字段：用户、期号、原始文本、笔数、金额、中奖金额、状态、封盘状态、下单时间。
- `bet_details` 字段：注单记录、号码、分类、金额、赔率、中奖金额、回水、状态、下单时间、原文。
- `user_stop_drops` 可补充彩种、玩法、原始/实际金额与赔率，用于总货明细的彩种/分类与金额展示。
- 现有退码只把状态改为 `refunded`，未记录退码时间；需新增 `refunded_at` 字段并在用户退码事务中写入。
