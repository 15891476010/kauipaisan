# 问题与防复发记录

### 2026-08-31：代理端彩票列表不应出现在头部导航同一行

- 问题：总监、总代理、代理端头部把彩票列表与 Logo、账号和导航放在同一行，与参考站结构不一致；彩票状态栏应位于头部下方。
- 正确做法：从 `.site-header` 移除彩票列表，将其独立渲染为头部下方的 `.agent-lottery-strip`；保留彩种选择、期号、状态和倒计时逻辑。
- 防复发检查：登录任一代理层级后，Logo/账号/导航行不出现彩种列表；彩种状态栏紧随头部下方并可正常切换，页面主体高度按公告+头部+状态栏计算。

### 2026-08-30：主页左右滚动区域必须隔离

- 需求：左侧下注记录不能随右侧主内容无限增高，左侧最大高度不得超出视口；左侧列表在自身区域滚动，右侧继续使用页面整体滚动。
- 正确做法：主页根容器不再创建独立滚动上下文；主网格顶部对齐，左侧 aside 固定视口剩余高度并隐藏溢出，`side-record-list` 占据剩余高度独立滚动且启用 `overscroll-behavior: contain`；右侧 main 保持可见溢出交给页面滚动。
- 防复发检查：滚动左侧列表时右侧页面位置不变化；滚动右侧长页面时左侧高度不增长；切换左右侧面板后两种行为仍保持。

### 2026-08-30：一组合总额被误判为每注金额

- 问题：`福880 942 869 908 248一组合10米` 中的“组合”表示整组号码合计10元；旧解析没有识别该别名，可能按每注10元计算，最终金额校验失败。
- 正确做法：识别 `一组合` 为每个号码一组、整张合计金额；按号码形态拆分组三/组六，5个号码以基础2元计算为10元（880组三2元，942/869/908/248组六各2元）。
- 防复发检查：必须覆盖该原文及 `一组合计10` 变体；结果须为组三2.00、组六8.00，总额10.00，合计金额不得进入选号。

### 2026-08-30：福体彩六码共享玩法合计识别错误

- 问题：`福六码034689`、`体六码245678` 后接共享尾行 `组六30组三20` 与 `🈴100` 时，旧流程只按第一行福彩解析，或将尾行金额重复套用，导致整单金额失败。
- 正确做法：收集每个彩种六码选择，尾行分别生成组六30和组三20；两彩种各50元，整张合计100元，合计数字不得成为选号。
- 防复发检查：覆盖福彩/体彩不同六码、共享玩法尾行及独立合计行；必须返回4条成功明细，金额30/20/30/20，总额100.00。

### 2026-08-30：行内小计与末行总计被错误合并

- 问题：第一票带 `一直组🈴6`，第二票紧接着输入，末行再写 `🈴36` 时，分票器只按空行切分，导致第一票小计被吞入第二票，最终金额校验不一致。
- 正确做法：识别行尾 `合计/🈴金额` 作为当前票结束边界，即使下一票没有空行也要分开；末行合计仅校验所有票据总额。`一直组`按每个号码一组计算6元。
- 防复发检查：覆盖该三段输入，必须返回5条成功明细，金额6/10/5/10/5，总额36.00，不得把36当作第二票金额。

### 2026-08-30：值组口语别名导致一倍数字污染选号

- 问题：`福276一倍值组4米` 中的“值组”是直组口语写法；旧流程把“一倍”的1误当成001组选号码，导致金额校验错误。
- 正确做法：识别“一倍值组”并生成直选、组选各1倍，276为直2元＋组六2元，总额4元；一倍仅作为倍数元数据，不得进入号码。
- 防复发检查：覆盖无/有福彩前缀、空格及金额单位变体；结果必须为直2.00、组六2.00，总额4.00。

### 2026-08-30：不得猜测不完整输入的跨票总额

- 问题：仅输入 `福358.357.378复试直选一倍 / 合106` 时，曾猜测106属于未粘贴的前序票据并放行当前6元票，违反“用户写什么就是什么”。
- 正确做法：当前输入只能证明3个直选一倍为6元；106与当前内容不守恒必须失败。需要验证跨票总额时，用户必须粘贴完整前序票据。
- 防复发检查：独立金额不匹配必须失败；不得根据金额大小猜测遗漏票据、自动忽略合计或改写下注金额。

### 2026-08-30：复试直选一倍由复式10元加组选2元组成

- 问题：`福358.357.378复试直选一倍` 被当成普通直选三注×2元=6元，导致完整合计106被算成76。
- 正确做法：明确“复试直选”按每个三位不同数字的6个排列（复式10元）加对应组选三码2元，每个号码12元；三组总额36元。仅在号码三位且互异时启用，形态不明则失败。
- 防复发检查：该样本必须产生36元，并保留 `复式30元＋组选6元` 的金额分解；普通 `直1倍` 仍为每注2元。

### 2026-08-30：用户端异步生成结果不得被旧请求覆盖

- 问题：用户连续编辑或开启立即识别时，多个预览请求可能并发；较慢的旧响应会晚于最新响应写入页面，使后端已经算出106元但用户端仍显示旧金额。
- 正确做法：每次生成分配递增请求序号，只有最新请求可更新 generatedLines/generatedTotal/loading；旧响应只返回给其调用方，不覆盖当前页面。
- 防复发检查：连续触发两次预览并让第一次后返回，页面必须保留第二次结果；完整106样本的后端总额必须固定为106.00。

### 2026-08-30：角金额与注数后置格式未进入严格解析

- 问题：`号码直2角福54注合10.8` 同时包含小数角金额、后置彩种、注数和小计，旧分支无法完整提取，报“金额不一致”；不同历史格式因进入不同兼容分支，表现为有的正确、有的错误。
- 正确做法：新增通用“号码列表＋直/单＋金额单位＋N注＋合计”语法；`2角`转换为每注0.2元，54注严格校验，合计10.8保留两位小数。
- 防复发检查：覆盖 `元/米/块/角/毛`、后置福彩前缀、重复/长号码列表和小数合计；只要结构或金额不守恒就失败，不能回退默认单注金额。

### 2026-08-30：合计行出现在玩法行之前

- 问题：328个号码后写 `🈴328米`，下一行才写 `福直一米` 时，解析器只支持玩法尾行或把合计行当成结束，导致整单无法识别或回退到默认金额1/2元。
- 正确做法：新增“号码列表＋独立合计行＋玩法行”结构；合计只作总额校验，玩法 `直一米` 决定每注1元，328个号码严格得到328.00元。
- 防复发检查：覆盖合计/玩法互换顺序、单行或多行号码、328注以上长列表；合计数字不得成为号码，缺玩法或金额不守恒必须失败。

### 2026-08-30：9个号码直组2元与合计40元不守恒

- 问题：`506 515 272 191 536 513 316 516 503` 只有9个号码，`直组各2米`应为9×2×2=36元；却标注 `计40米`。旧兼容路径可能退化为单个2元结果。
- 正确做法：严格按原始号码和显式金额计算；36元版本成功，40元版本直接失败，不补号码、不调整单价、不猜测遗漏票。
- 防复发检查：同一号码列表分别测试合计36与40，前者金额总和36.00，后者状态必须failed。

### 2026-08-30：金额不一致必须支持人工确认而非静默改写

- 需求：当用户输入的合计与号码/单价计算不一致时，页面应在下方警示“检测到多条语句的总金额对不上，请进行人工确认”，并在对应行前显示确认勾；用户确认后才把合计改为解析建议值（本例40→36），同步更新顶部输入和结果。
- 正确做法：后端失败行返回 `suggested_amount/corrected_text`，前端只有点击确认勾才重新提交修正文本；未确认状态保持失败，不能下注或自动修改用户原文。
- 防复发检查：错误合计40必须保持失败并显示建议36；确认后重新解析总额36成功，原始号码和玩法不变。

### 2026-08-30：随机顺序的一直一组票据

- 问题：`872.852各一直一组一元`、`286组一元`、`福🈴5米` 将号码、玩法、金额和彩种拆在多行，旧解析容易只取一行或直接失败。
- 正确做法：新顺序无关分支独立提取每行字段并汇总：872/852各直组2元，286组1元，合计5元；合计行只校验总额。
- 防复发检查：交换三行顺序、插入空格和标点后，仍须得到2/2/1三条成功明细，总额5.00。

### 2026-08-30：99注组直算出217.8元

- 问题：长号码列表 `共99注组直各0.2米99*0.4=39.6` 被旧兼容解析成直选19.8元＋组六198元，合计217.8元；组选金额错误地把0.2米当成2元并把算式数字混入语义。
- 正确做法：新增严格算式语法，`组直`保持用户原文的通用组选标签；99个号码直、组各0.2元，分别19.8元，算式 `99*0.4=39.6` 只作金额校验。
- 防复发检查：覆盖99注长列表、重复号码、角/米小数和算式；结果总额必须39.60，任何不一致必须失败，禁止回退旧解析成功。

### 2026-08-30：长列表算式必须由V2唯一接管

- 结果：新增 `号码＋共99注＋组直＋各0.2米＋99*0.4=39.6` 严格语法，输出直19.80与组19.80，总额39.60。
- 正确做法：该结构命中V2后不再回退到旧解析器，避免旧分支生成19.80＋198.00=217.80。
- 防复发检查：同一文本预览和下注必须读取相同 parser_version=v2、amount=39.60；金额不一致时返回失败，不产生旧版成功行。

### 2026-08-30：解析器不得擅自推断用户未写的玩法

- 用户明确：所有格式都不能猜，原文是什么就保持什么。
- 正确做法：只允许已登记的文字别名做等价规范化；通用“组”不得在展示中改写为组三、组六或豹子。若内部赔率需要分类，只能作为不可见的赔率身份，不能改变用户玩法或金额。
- 防复发检查：结果中的展示玩法必须可追溯到原文；缺少玩法、金额或票据范围时返回失败，禁止兼容层以默认值生成成功单。

### 2026-08-30：独立彩种前缀提前拆散多行共享玩法

- 问题：单独一行 `福` 后接多行号码，只有末行带 `三单两组共180米` 时，顶层前缀继承逻辑逐行解析，前置号码失去共享玩法，末行3个号码又被错误拿去校验180元。
- 正确做法：遇到独立彩种前缀先尝试严格整票编译；可确定为“多行号码＋共享玩法尾缀”时将全部18个号码共同套用三单两组，再按号码形态拆分组三/组六显示。
- 防复发检查：该完整样本必须返回直108.00、组三36.00、组六36.00，总额180.00；不得生成前置号码“未识别到玩法”。

### 2026-08-30：玩法首行与多行号码、注数说明未合并

- 问题：`福直组各1米共40注` 位于号码之前，号码分布在10行，旧流程将首行的40误当成 `040` 号码，并逐行报告“未识别到玩法”，末尾 `🈴80` 也无法校验。
- 正确做法：识别首行玩法/注数说明，合并后续所有纯三位号码行；`共40注` 仅作为注数元数据，`直组各1米` 应生成直选40元，并按号码形态拆出组六29元、组三11元。
- 防复发检查：覆盖40个号码、多行换行、首行彩种前缀及独立合计行；必须返回三条成功明细，金额40/29/11，总额80.00。

### 2026-08-30：明确“各1米”不得套用默认2元

- 问题：`福直组各1米共40注` 明确指定每注1元，但部分路径可能使用彩种默认单注2元，导致组选明细看起来仍是2元。
- 正确做法：金额单位 `米/元/块` 直接使用用户输入金额；只有没有金额单位的“倍/注”才使用对应枚举或默认本金。该样本最终40个直选/组选总额必须80元。
- 防复发检查：同一输入须校验每个明细 `amount / stake_count = 1.00`，不得出现80/58/22等按2元计算的金额。

### 2026-08-30：省略号尾部总额导致直组金额误判

- 问题：`福彩514...120直组各1元……18` 中的省略号被忽略，尾部18未作为整张合计，旧流程只返回部分金额或校验失败。
- 正确做法：识别省略号后的数字为整张总额；9个号码各1元直＋组，分别9元，合计18元。重复号码可在展示层去重，但金额按原始9个号码计。
- 防复发检查：覆盖ASCII点号、中文省略号及重复号码；必须返回直选9.00、组六9.00，总额18.00，尾部18不得成为选号。

### 2026-08-30：算式合计最后一项合并多张票

- 问题：`116+30+30+40=216` 对应五张票，最后40是两张胆拖票的小计；旧算式分配要求每个操作数必须对应一张票，导致总额校验失败或把40套给单张票。
- 正确做法：当操作数少于票数时，前置操作数按票分配，最后操作数按剩余票据当前金额比例拆分；本例最后两张各20元，总额216元。
- 防复发检查：覆盖多票算式、最后合并小计及独立空行；不得把40变成单张20元之外的额外金额，也不得把算式数字当号码。

### 2026-08-30：长号码直组省略号总额未按原始注数计算

- 问题：长号码列表使用点号分隔，尾部写 `直组各1元……18` 时，重复号码和省略号总额没有统一解析，容易只返回直选或金额校验不一致。
- 正确做法：点号先归一化为空格；省略号后的18是整张合计，原始9个号码分别参与直选/组六各1元，输出9+9=18元。
- 防复发检查：覆盖重复号码、中文省略号及 `……18` 尾额；尾额数字不得进入选号。

### 2026-08-30：单挑玩法首行与多行号码未合并

- 问题：两行号码后写 `福彩单挑1米的`、末行 `合48米` 时，首行的“单挑”未被识别，48个号码逐行失败或合计校验不一致。
- 正确做法：将“单挑”归一为直选单玩法；合并前置所有号码行，每注1元，48注总额48元。单挑不附加组选金额。
- 防复发检查：覆盖多行号码、`的`尾缀、独立合计行和福彩前缀；必须只返回直选48.00元。

### 2026-08-30：组选五码一倍被当成单个号码

- 问题：`02349福组六一倍，合十` 中的02349是五位组选复式号码，旧流程将其连同“组六一倍”当成一条金额2元或虚构的六位号码。
- 正确做法：按组选五码目录的一倍固定总额10元展开10个组六组合；`合十`只作总额校验。
- 防复发检查：覆盖中文/阿拉伯数字倍数、中文合计“十”及福前缀；结果必须为组六五码、10.00元。

### 2026-08-30：基础定位玩法缺少赔率身份

- 问题：`福百123456 / 十012345 / 个123456 / 各100米` 虽然能算出21600元，但结果只标记为“定位”，结算文本丢失百/十/个位字段，赔率匹配返回“无法唯一匹配赔率”。
- 正确做法：三个位置统一输出 `三码定位`，号码字段保留 `百... 十... 个...`，settlement_text 同样保留位置选择，供赔率身份和结算匹配使用。
- 防复发检查：覆盖一码、二码、三码定位及不同位置顺序；结果必须同时满足正确注数/金额和可唯一映射的赔率身份。

### 2026-08-30：重复定位块与共享尾额无法分票

- 问题：多段 `福` 前缀分别包住百/十/个位选择，最后统一写 `各50合计7350` 时，旧流程把多个定位块合并或逐行失败，无法匹配基础定位赔率。
- 正确做法：以重复独立彩种前缀作为定位票边界；每个块按位置组合数计算，尾部“各50”作为共享单注金额，“合计7350”校验三票总额。
- 防复发检查：覆盖百/十/个位任意两位置组合、重复前缀、共享金额尾行；本例必须返回3条二码定位，各2450元。

### 2026-08-30：单数字定位不能按普通号码解析

- 问题：`福体 / 个位4 / 个位3 / 各300合计1200` 中，个位只选一个数字也属于合法的一码定位；旧流程把定位行拆散或报未识别到有效号码。
- 正确做法：每个 `个位4/个位3` 独立生成一码定位；福体下每笔300元按两彩种计600元，两笔合计1200元。
- 防复发检查：覆盖百/十/个位单数字、重复位置、多彩种及共享金额尾行；单数字不得被当成缺少三位号码而失败。

### 2026-08-30：组6别名后的合计金额无法识别

- 问题：`体1475829组6合计800` 中“组6”是组六别名，800是整张组选总额；旧规则不接受合计词，导致基础组选失败。
- 正确做法：显式支持 `组6/组六` 后接 `合计/共计/合/共/计` 的总额写法；选号1475829作为七码集合，展示为 `六1475829`，展开组合仅供内部结算。
- 防复发检查：覆盖组6与组六、排列三/福彩、合计词及金额单位；总额800必须保留为800，不按组合数量重复乘价。

### 2026-08-30：福体三位复式现金格式被误判选号数量

- 问题：`福体485。复试各100合计200` 中“复试”是复式别名，485是三码选号；旧规则不支持该顺序与合计组合，返回“所选数字数量与玩法不一致”。
- 正确做法：支持 `福体 + 三位号码 + 复式/复试 + 各/每金额 + 合计`；福体按两彩种各100元计算，输出复式三码、复485、总额200元。

### 2026-09-01：共355注与金额前置直玩法污染号码

- 问题：长号码列表后写 `共355注福一米直`（或 `福一米直共355注`）时，旧兼容解析把声明注数355和金额“一米”分别识别为额外号码，导致号码数量变为357并提示缺少金额；无空格粘连时同样失败。
- 正确做法：严格解析“号码列表＋可选共N注声明＋金额＋直/单”语法，支持注数省略、前后附着及无空格；声明注数只校验列表数量，金额按每注计算，中文金额“一米”不得转成001选号。
- 防复发检查：覆盖 `共355注福一米直`、`福一米直共355注`、`福一米直` 及无空格版本；355个号码必须返回直选355.00元，展示号码不得含355或001。

### 2026-09-01：单双组歧义、福体合计及包别名

- 问题：`945福打二单一组6` 中的“组6”可能是“总额6”而非组六玩法，旧归一化会静默判成组六并算错；带备注的 `福体下午茶，249-932-634十单五组，180` 会把180当三位号码，金额变为96/80等错误值；`福102注一倍直` 的102也会进入号码列表；“包”口语无法进入全包玩法。
- 正确做法：对“打N单M组＋尾额”返回组选类型不明确提示；支持带前置备注的单双组总额格式，福体按两彩种计算；支持跨行 `N注一倍直＋合计`，注数仅做校验；将 `组三/组六/豹子/对子＋包` 规范为对应“全包”。
- 防复发检查：覆盖上述四类原文及无空格变体；歧义输入必须失败并要求明确组三/组六，180应拆为120＋60，102注直选合计204，包别名结果玩法包含“全包”。

### 2026-09-01：紧凑组三/组六倍数及福体金额

- 问题：`23469两倍组三一倍组六` 未被严格识别，旧流程金额可能为空或走兼容分支；福体场景应按两彩种分别计价。
- 正确做法：新增紧凑多玩法倍数语法，五码组三两倍=20元、组六一倍=10元；福体类别乘2，输出40.00和20.00两条明细。
- 防复发检查：覆盖福、体、福体前缀及中英文数字倍数；禁止把倍数数字当成选号，金额必须按玩法和类别守恒。

### 2026-09-01：注数声明与号码列表粘连

- 问题：长号码列表后接 `体102注一倍直` 时，部分分段路径会把声明数字102先当作第103个三位号码，随后只识别出206元或注数不一致；有无空格时表现不一致。
- 正确做法：严格的跨行注数玩法解析会读取后置 `N注` 声明；若号码列表末尾多出一个等于声明值的三位 token，则将其视为粘连声明并剔除，再按剩余号码数量校验金额。
- 防复发检查：覆盖号码列表末尾无分隔、空格分隔、标点分隔及独立玩法行；102注一倍直必须只统计102个号码并得到204.00元，声明数字不得进入展示号码。

### 2026-09-01：打二单一组被少算

- 问题：`945福打二单一组` 旧解析只返回二单直选4元，遗漏“一组”应对应的2元；带尾额6时还可能把“组6”静默归类为组六。
- 正确做法：`打N单M组` 中“单”按直选注数计价；“组”就是通用组选，不能擅自改写成组六。`945福打二单一组` 应拆为直4元＋组2元；尾部 `6米` 是整张总额校验，不是每项3元。
- 防复发检查：覆盖 `945福打二单一组`、`945福打二单一组6米`；两者都应返回直4.00、组2.00，总额6.00，展示玩法保留“组”。

### 2026-09-01：福体算式批次后票未继承彩种范围

- 问题：首票带 `福体`、后票未重复写彩种时，算式 `180+60=240` 让后票按单一福彩计算，导致总额只有210；同时“下午茶”备注票的组选展示被误写成组六。
- 正确做法：算式批次检测到任一票明确为福体时，将福体范围继承到无前缀兄弟票；通用“组”保留为“组”，不根据号码形态改写。
- 防复发检查：覆盖首票福体、后票无前缀及 `180+60=240`，必须得到120/60/40/20四条成功明细，总额240。
- 防复发检查：覆盖中文句号、复试/复式、福体前后位置及合计金额；三位复式现金必须成功，选号数量不匹配仍失败。

### 2026-08-30：声明注数被误识别为号码

- 问题：长号码列表后写 `一共343注福体直各10合计6860` 时，旧解析把343拆成额外三位号码，导致注数/金额守恒失败。
- 正确做法：`compileFuTiDeclaredDirect` 先校验原始号码数量等于声明343，再按福体双彩种各10元计算6860元；声明数字只作为元数据。
- 防复发检查：覆盖不同声明注数、福体前缀、各金额和合计；结果不得把声明注数追加到 `number_text`，注数不一致必须失败。

### 2026-08-30：福体与直玩法顺序变化导致基础票失败

- 问题：`一共343注福体直各10合计6860` 可识别，但用户写成 `一共343注直福体各10合计6860` 时旧正则因顺序固定而失败。
- 正确做法：对明确的“直＋福体”声明允许两种相邻顺序，号码数量、单价和合计仍严格校验。
- 防复发检查：覆盖 `福体直`、`直福体` 两种顺序及不同空格；两者都必须得到6860元，声明343不得进入号码。

### 2026-08-30：多码各金额被重复解析为3000元

- 问题：两行独立输入 `福123456组三各1000米`、`福0123456组六各500米` 时，通用多码分支将每行拆成多条组合明细，金额合计变成3000元；把“各”改成“共”又因不支持该别名而失败。
- 正确做法：按物理行建立两张独立多码注单，每行金额只计一次；`各/共` 在该结构中都表示该行声明金额，结果为1000+500=1500元。
- 防复发检查：覆盖组三/组六不同码数、各/共别名和换行；每个物理行只能产生一条金额汇总，禁止按展开组合数量再次乘金额。

### 2026-08-30：多码玩法展示被拆成组合号码列表

- 问题：`123456组三`、`0123456组六` 为了结算展开成大量三位组合后，下注主表和详情也直接显示112、113、114…，用户无法看到自己输入的原始玩法与选号。
- 正确做法：解析时保留 `number_text/display_number_text/settlement_text` 为玩法级选号（如 `三123456`、`六0123456`），展开组合仅放入 `expanded_number_text` 内部字段，不参与用户展示；金额仍按该玩法原本金额计算。
- 防复发检查：覆盖组三/组六三至九码、各/共金额、多行输入及详情弹窗；界面不得把组合展开结果当成用户下注名称。

### 2026-08-30：倍数金额不能统一按彩票2元本金计算

- 问题：旧解析大量使用 `倍数×2` 或散落的 `倍数×10`，无法表达复式、单选组选复式、胆拖、全胆拖和沾边赖的不同一倍本金，预览与下注可能产生不同金额。
- 正确做法：使用 `BetMultiplierAmount` 枚举集中保存固定本金及全部码数表；只有输入单位明确为“倍”时查询枚举，元/米/角/毛的现金金额逻辑保持不变。组三沾边赖六码按用户纠正使用158元。
- 防复发检查：枚举表逐项断言所有码数；解析回归覆盖普通单注、多码、跨度、和值、全包、普通胆拖、组六2胆拖、单选全胆拖和两类沾边赖，禁止业务代码新增裸写 `倍×2/倍×10`。

### 2026-08-30：金额前置直组被算成48且漏掉第13个组号

- 问题：`13个号码一米直组` 未命中严格语法，旧兼容层把一米当成2元，并把888从组选中漏掉，错误生成直24＋组24＝48元。
- 正确做法：金额前置的直组语法由V2权威接管；13个原始号码每个一直1元、一组1元，总额26元。888必须保留为用户输入的“组”，不得擅自改写成豹子；内部赔率身份可按精确号码结算。
- 防复发检查：正确合计26必须成功；相同号码写合计48必须返回“整张总金额与识别金额不一致”，禁止回退旧解析成功。

### 2026-08-30：独胆连字符语义与金额误识别

- 问题：`福独胆2-8各50` 被按连续范围或普通号码解析，导致金额不是 2×50=100；带 `🈴100` 时也可能把合计数字混入选号。
- 正确做法：在明确的独胆语法中，`A-B` 表示两个端点号码 A、B（本例为2和8），`各`金额按端点数量计；合计词及其数字仅用于总额校验，不得成为选号。行内和独立合计行均须保持金额守恒。
- 防复发检查：覆盖 `福独胆2-8各50\n🈴100`、`福独胆2-8各50合计100`、`独胆1-3各500` 以及 `独胆1-9/3-9` 等端点写法；结果号码、注数、金额均须与端点数量一致。

### 2026-08-30：空行分单边界纠正

- 用户明确：单个空行仍属于同一张注单，连续两个空行才开始下一张注单。
- 正确做法：前端分单与后端解析均保留单个空行语义，只在连续两个空行时切分；合计/金额结束标记仍按原规则处理。


### 2026-08-30：一直一组末尾福体与独立合计无法识别

- 问题：号码列表后写“一直一组，福体”，下一行写“🈴40”时，整单被判定总额不一致。
- 正确做法：识别末尾彩种前缀和单双组倍数，按通用号码列表生成直选与组选两条明细；独立合计行仅参与整单总额校验。
- 防复发检查：覆盖号码重复导致组三/组六混合、福体倍增、逗号/点号分隔和独立合计行；示例总额必须守恒。

### 2026-08-30：注单结束不能只按空行判断

- 问题：用户多行粘贴内容可能没有空行，但已通过合计或单双组金额规则结束；仅按空行会把下一张注单合并提交。
- 正确做法：前端同时识别 `合计/合/🈴金额` 和 `一元单一元组/两单一组` 等终止语义；普通换行继续属于当前注单。
- 防复发检查：覆盖有/无空行、独立合计行和金额规则结尾，分别提交并确认每张注单。

### 2026-08-30：多行同注单的“几单几组”后缀无法识别

- 问题：连续号码行后独立写 `两单一组`、下一行写 `合36` 时，整单被判定金额不一致；空行语义未参与该后缀解析。
- 正确做法：在空行分隔前提下合并连续号码，按每号码倍数生成直选与组六明细；独立合计行仅作结束标记。示例6个号码得到24+12=36元。
- 防复发检查：覆盖一至十单/组、中文和阿拉伯数字、点号/空格号码分隔；无空行继续同一注单，有空行才开始下一张。

### 2026-08-30：清理后遗漏恢复赔率目录及空行分单

- 问题：全量清理后只恢复彩票和开奖记录，赔率表为空，所有下注报“无法唯一匹配赔率”；多段文本空行也被一次提交。
- 正确做法：从备份恢复赔率目录；用户端检测空行分隔的多张注单，逐张弹窗确认并分别提交。
- 防复发检查：恢复彩票后必须同时检查赔率目录；空行分单提交不得合并为一个主单。

### 2026-08-30：一元单双组与点号号码行无法识别

- 问题：点号分隔号码、独立“一元单，一元组12”和独立“合22”行混合粘贴时，解析器返回整单金额不一致；“一单”未明确按一直处理。
- 正确做法：合并连续号码行并拆成显式直选/组六两条，点号视为空格；独立合计行仅作总额标记不作为号码；保持通用数字匹配，不写死示例号码。
- 防复发检查：覆盖点号/空格分隔、多行号码、一元单双组和独立合计行；结果行数、金额和玩法均可核对。

### 2026-08-30：全量清理误包含SaaS平台管理员

- 问题：按“仅保留站点和开奖记录”的清理操作同时删除了 `admins` 平台管理员，导致SaaS后台无法登录。
- 正确做法：从清理前备份仅恢复平台管理员账号；不恢复会员、余额、注单或其他业务数据。
- 防复发检查：涉及“全部业务数据”清理前必须单独确认平台管理员是否保留；执行后检查 `admins` 至少存在可用平台账号。

### 2026-08-29：按确认执行全量业务数据清理

- 处理：用户明确选择“除站点和开奖记录外全部删除”。已备份数据库后清空会员、代理、账号、余额、注单、明细、账单、赔率、配置、权限、组织分账和全部业务流水，仅保留 `sites` 与 `lottery_histories` 数据。
- 防复发检查：执行前必须获得明确确认并创建备份；清理后核对保留表记录数及关键业务表为0；恢复只能从备份执行。

### 2026-08-30：会员明细号码补0和玩法标识展示不一致

- 问题：`独胆1` 在详情中显示为 `001` 且未显示“独胆”标识，其他玩法也缺少号码后的红色玩法标签。
- 正确做法：纯三位数字展示时去除前导补0（`001`→`1`），复式/组选等包含语义前缀的文本保持原选号；主页“详”弹窗和下注明细表统一在号码后显示红色玩法标识。
- 防复发检查：覆盖独胆、直选、飞/对子、组三/组六和定位；号码语义不丢失，玩法标识来自后端规范化名称。

### 2026-08-30：双飞重复号码的赔率与展示标签需分离

- 问题：重复数字“77双飞”虽然应取对子赔率，但若直接把玩法改成对子，详情会与用户原始注单不一致。
- 正确做法：保留 `play_type=双飞` 和原始“飞/双飞”展示，赔率身份层检测两位数字相同后映射对子赔率；不同数字仍映射双飞赔率。
- 防复发检查：`77双飞`显示双飞但赔率取对子，`37飞`显示双飞且赔率取双飞；结算文本必须保留原始玩法。

### 2026-08-30：胆码前置写法未参与中奖判断

- 问题：`福胆1各300米` 保存为 `001`，但结算匹配只识别“1胆”而未识别“胆1”，导致开奖号码123时错误判定未中奖。
- 正确做法：匹配器同时支持“胆1”和“1胆”；已将注单264明细316修正为中奖990元，并同步会员、账单、提交汇总和组织流水。
- 防复发检查：胆码前后顺序均须命中；号码显示为001不影响其作为独胆1的语义。

### 2026-08-29：组三/组六前缀胆拖格式未识别

- 问题：`福组三胆1拖23各1000米`、`福组六胆1拖23各1000米` 将玩法写在胆拖前面时无法识别。
- 正确做法：支持“玩法+胆1拖23”和“玩法+1胆拖23”两种顺序；未写组三/组六的通用胆拖仍必须报错。
- 防复发检查：两种玩法前缀均返回对应 `1码拖2`，不带玩法的 `1拖23` 继续提示必须注明组三或组六。

### 2026-08-29：跨度/胆拖别名无法识别

- 问题：`福跨2各10000米`、`福1胆拖23各1000米`、`福胆1拖23各1000米` 不能命中已有玩法规则。
- 正确做法：归一化“跨2”为“2跨”，新增两种胆拖标记顺序的解析，统一输出跨度2和1码拖2。
- 防复发检查：覆盖金额单位、福体前缀及胆/拖标记顺序；解析结果不得回退为普通直选或失败。

### 2026-08-29：关键错误提示不能使用自动消失 Toast

- 问题：生成失败、下注失败或无有效投注内容时使用 Toast，用户可能未看到完整错误便继续操作。
- 正确做法：改用必须点击“确认”才能关闭的错误/警告弹窗；成功提示仍可使用 Toast。
- 防复发检查：解析错误、服务端拒绝和空投注提交均显示确认弹窗，不使用自动消失提示。

### 2026-08-29：示例号码不得写死到业务正则

- 纠正：用户提供的 `34567`、`012345`、`895` 等仅是说明实例，不能据此编写只匹配固定号码的规则。
- 正确做法：业务正则只约束数字串长度、玩法标记、金额和结构；具体码数由运行时去重后的数字数量动态决定。
- 防复发检查：代码审查搜索示例号码只允许出现在测试、注释或一次性清洗脚本；解析器必须用通用 `[0-9]` 规则并覆盖不同长度输入。

### 2026-08-29：组三组六混合输入未拆成两条玩法

- 问题：`组三12345组六各50米`、`组六012345组三各1500米` 等顺序写法只识别其中一个玩法，或前端看起来像一笔注单。
- 正确做法：识别两种玩法及共同选号/金额，分别生成组三和组六明细；按选号数量映射对应码数赔率，详情弹窗按玩法分组展示。
- 防复发检查：玩法在号码前、后及两种玩法均在号码前的组合均拆分为两条明细；主单可包含多条明细但不得丢玩法。

本文件记录用户指出的问题、正确做法和后续检查。每次开发前必须阅读。

### 2026-08-23：组选集合的真实选号不能回退为 000

- 问题：旧组选复式记录在数据库中以 `000` 作为结算占位；规范化会把“五码”转换成“5码”，原匹配规则漏掉阿拉伯数字形式，导致页面又显示 `000` 或玩法名。
- 正确做法：仅在展示层从 `detail_source/record_source` 提取真实选号，并同时接受中文及阿拉伯数字的组选码数写法；数据库的 `000` 保持不变以兼容结算。
- 防复发检查：用 `15862福体组六各108` 的现有记录验证接口、下注明细表格和号码弹窗均显示 `15862`；运行 `php -l server/app/controller/UserBusiness.php`、`php server/tests/QuickEntryParserTest.php` 和服务器 `npm run build`，禁止通过真实下注验证。

## 2026-08-18：不要把构建产物误称为原始工程源码

- 问题：线上只能获得压缩后的 HTML、JavaScript 和内联资源，没有 sourcemap，不能声称已恢复原始源码。
- 正确做法：明确区分“线上构建产物”“可读提取文件”“重新实现的工程源码”。
- 防复发检查：交付时说明文件来源、是否反编译、是否重新实现以及缺失内容。

## 2026-08-18：用户要求的是样式和功能代码，不只是界面说明

- 问题：只描述责任声明页面，没有第一时间提取 CSS、DOM 和交互功能。
- 正确做法：遇到“拉取”要求时同步保存原始资源、DOM、样式、路由、接口和按钮行为。
- 防复发检查：每个页面至少记录结构、样式来源、功能入口、接口和资源。

## 2026-08-18：资源文件必须同步

- 问题：首次只下载 JavaScript 和 HTML，遗漏内联 SVG。
- 正确做法：每个新页面状态都检查图片、字体、SVG 和动态资源。
- 防复发检查：交付前生成资源清单并验证 SVG/XML 或图片格式。

## 2026-08-18：大任务必须持续更新计划

- 正确做法：每轮实现后更新 `PLAN.md`，记录完成项、验证结果、阻塞和下一步。
- 防复发检查：代码交付前确认 `PLAN.md` 的状态与实际实现一致。

## 追加模板

### 2026-08-18：平台站点与代理站点的权限边界

- 问题：把每个下拉选项标成“当前站点”，没有区分平台管理员可跨站点操作与代理仅能操作自身站点。
- 正确做法：平台本身是带扩展能力的自有站点，平台管理员可在所有有效站点中选择；站点/代理管理员不显示选择器，后端从认证会话取得 `site_id` 并强制作为列表、创建、编辑和删除的范围。
- 防复发检查：不能只在前端隐藏选择框；必须用不同站点的会话验证跨站点请求无法读写，并确保下拉选项仅显示站点名称。

### 2026-08-18：用户端文案必须站点化配置

- 问题：责任声明写死在用户端组件，任何内容调整都必须重新发布前端，且无法区分反代站点。
- 正确做法：标题和正文按站点保存于配置表，用户端凭登录令牌的站点范围读取；管理端使用专用多行配置页维护纯文本正文。
- 防复发检查：新增用户端可运营文案前，确认是否需按站点隔离、是否具备后台编辑入口，以及未保存时是否有安全的默认值。

### 2026-08-18：富文本声明使用 Markdown 而不是 HTML

- 问题：纯文本逐行渲染会丢失加粗、标题等原有强调样式。
- 正确做法：正文保存 Markdown，用户端使用受限 Markdown 渲染器和 GFM 扩展；不启用原始 HTML 渲染。
- 防复发检查：验证加粗、标题、列表、引用、表格的展示，并确认正文中的 HTML 标签不会执行或插入 DOM。

### 2026-08-18：协议同意状态不能只放在内存

- 问题：协议页是否显示只使用 React state，刷新后状态默认关闭，未同意也能直接进入主页。
- 正确做法：将当前用户令牌与协议同意状态绑定保存；刷新时令牌未被标记为已同意，就继续显示协议页。
- 防复发检查：覆盖首次登录、刷新未同意、点击同意后刷新、不同意、退出和自动进入站点六种流程。

### 2026-08-18：加粗不能强制映射为红色

- 问题：把 Markdown 的 `strong` 标签统一设为红色，导致用户无法区分“加粗”和“文字颜色”，也无法在后台直接选择颜色。
- 正确做法：改用 Vue 3 富文本编辑器，颜色由选区工具写入 HTML 样式；用户端通过 DOMPurify 过滤后渲染，保留合法颜色和排版。
- 防复发检查：加粗默认保持正文色，颜色必须由编辑器颜色工具设置；验证脚本、事件属性和危险标签不会进入用户端 DOM。

### 2026-08-18：用户端 CSS 不能覆盖富文本内联颜色

- 问题：`.agreement-card p strong { color: #000 }` 覆盖了富文本红色 `span` 中的加粗文字，导致管理端与用户端颜色不一致。
- 正确做法：加粗只设置字重，颜色使用 `inherit`，由富文本选区的内联样式决定。
- 防复发检查：同时验证普通黑色加粗和红色加粗两种内容，不能只看编辑器预览。

### 2026-08-18：余额必须由后端统一计算

- 问题：用户端顶部金额是写死的 0，管理端没有余额字段，无法表达用户实际信用和使用情况。
- 正确做法：站点用户保存余额、信用余额、已用余额；总余额和可用余额由 API 计算并返回，前端只负责展示。
- 防复发检查：校验非负金额、已用余额不得超过总余额，并验证管理端列表、编辑表单和用户端顶部使用同一组数据。

### 2026-08-18：依赖下载使用局域网代理

- 问题：公网 GitHub/Composer 下载速度不稳定，直连会超时或损坏续传文件。
- 正确做法：下载依赖时使用用户提供的 `http://192.168.2.28:7897`，续传前校验压缩包完整性。
- 防复发检查：归档依赖前运行 `gzip -t`、`tar -tzf` 或对应校验命令。

### 2026-08-18：Redis 扩展状态必须如实显示

- 问题：当前外接盘 PHP 编译的 redis 外部扩展未被 PHP 8.3 configure 识别，不能把它声称为已启用。
- 正确做法：`.env` 保留 Redis 参数，缓存默认使用文件驱动并在管理端显示“配置完成（文件降级）”。
- 防复发检查：运行 `php -m` 并验证 `extension_loaded('redis')` 后，才切换 `CACHE_DRIVER=redis`。

### 2026-08-18：ThinkPHP 动态路由会截断带连字符的资源名

- 问题：`admin/sub-agents` 使用通用 `:resource` 路由时参数被解析为 `sub`，导致创建接口返回 unknown resource。
- 正确做法：为 `sub-agents`、`audit-logs` 等带连字符资源注册显式路由，并在控制器内统一规范化下划线别名。
- 防复发检查：每新增带连字符资源，必须同时加入显式 GET/POST/PUT/DELETE 路由并用真实 HTTP 请求验证。

### 2026-08-18：管理端登录不能假设固定响应层级

- 问题：Axios 已解包一次时，登录状态又固定读取 `response.data.token`，代理返回已解包 payload 时会触发 `null.token`。
- 正确做法：登录状态兼容 envelope 与 payload 两种形态，并在 token 缺失时显示明确错误。
- 防复发检查：认证接口必须覆盖成功、账号错误、空响应三种前端解析场景。

### 2026-08-18：代理、站点、域名的归属关系必须明确

- 问题：早期导航把二级代理、站点、域名拆成并列菜单，无法表达“二级代理属于一级代理、站点属于代理、域名属于站点”。
- 正确做法：平台保留一级代理入口；二级代理归入代理中心；代理中心以站点为主记录并聚合其域名。
- 防复发检查：新增站点或域名字段必须包含代理/站点归属，二级代理必须校验一级代理父级。

### 2026-08-18：平台菜单不能重复表达代理层级

- 问题：同时显示一级代理、二级代理、代理中心，造成业务对象重复。
- 正确做法：平台只保留代理中心；每条记录是一个独立站点，站点管理员和反代域名都在该记录内维护。
- 防复发检查：新增导航前先确认是否已有同一业务对象的入口；站点管理员密码只能哈希存储，列表不得返回。

### 2026-08-18：站点管理员必须有认证范围

- 问题：仅保存站点管理员字段而不绑定用户站点，会允许跨站点登录。
- 正确做法：用户表增加 `site_id`，创建站点时建立站点用户，登录查询按 `site_id` 限制。
- 防复发检查：所有用户登录入口必须传递或从域名解析站点上下文，禁止只按全局用户名认证。

### 2026-08-18：线上 SVG 图标不能用文字占位替代

- 问题：用户端登录页使用 `◌` 和 `◇` 作为临时 logo，和线上页面视觉不一致。
- 正确做法：复用 `online-snapshot/login-page/assets/svg/login-logo.svg` 并纳入 Vite 资源构建。
- 防复发检查：页面交付前检查 logo、用户、锁、退出等 SVG 是否真实加载。

### 2026-08-18：登录错误不能以内联红字显示，交互需要组件化

- 问题：登录错误直接以内联 `.error` 文本显示，验证码弹窗和登录逻辑也集中在 `App.tsx`。
- 正确做法：错误信息统一使用接近 Vue/Element Plus `ElMessage` 的顶部居中 Toast；验证码交互抽为 `CaptchaModal`，可复用提示抽为 `Toast`，后续页面继续按功能目录拆分。
- 防复发检查：错误场景验证 Toast 的 `role="alert"`、顶部居中位置和移动端宽度；新增弹窗或通知不得重新内联堆入路由文件。

### 2026-08-18：登录提示和控件必须统一使用 Ant Design

- 问题：自定义 Toast 与原生输入框、按钮和弹窗混用，提示行为与参考站不一致，输入框还有占位符。
- 正确做法：用户端入口接入 Ant Design `App`，登录页统一使用 `Input`、`Modal`、`Button`、图标和 `message.error`；消息使用默认自动关闭。
- 防复发检查：新增用户端交互优先从 Ant Design 组件实现，禁止重新添加原生输入框、按钮或手写消息浮层。

### 2026-08-18：登录图标和验证码尺寸需对齐参考站

- 问题：Ant Design 默认前缀图标、电源图标和验证码按钮尺寸偏小，验证码弹窗底部按钮位置与参考站不一致。
- 正确做法：放大登录图标，密码使用无可见切换按钮的 `Input`，验证码数字按钮使用深蓝虚线样式，弹窗登录按钮底部居中。
- 防复发检查：登录页截图对照检查图标尺寸、输入框宽度、验证码按钮颜色和弹窗 footer 位置。

### 2026-08-18：验证码输入和按钮状态需稳定

- 问题：验证码缺少分割线，数字可继续追加，数字顺序和间距不符合参考站，Ant Design 按钮带默认 focus 光效。
- 正确做法：验证码最多两位，超过后静默忽略；数字固定 `0-9`，卡片使用小圆角和紧凑间距，hover/focus/active 只切换文字和背景色；电源入口使用原生按钮。
- 防复发检查：连续点击第三个数字时值不变且无 Toast，键盘焦点不出现蓝色光效。

### 2026-08-18：验证码数字必须随机且立即切换状态

- 问题：数字按钮曾固定为 `0-9` 顺序，Ant Design 默认 hover/focus 样式覆盖自定义颜色，导致光效延迟。
- 正确做法：打开弹窗和换题时使用 Fisher-Yates 洗牌；数字按钮使用专用类名和 `!important` 覆盖 hover/focus/active 状态。
- 防复发检查：连续打开两次验证码比较顺序不同，悬浮和按下时立即为深蓝底白字且无阴影。

### 2026-08-18：验证码内容区间距需对齐参考站

- 问题：题目与数字卡片之间的分割线不明显，答案框过宽，footer 登录按钮贴近底部分割线。
- 正确做法：数字区增加独立顶部边框，答案框缩至 78px，footer 增加顶部间距。
- 防复发检查：截图确认题目区、数字区、footer 三段边界清晰，按钮与底部分割线有留白。

### 2026-08-18：登录按钮尺寸需以参考站实测为准

- 问题：登录电源按钮尺寸偏大且边框颜色不符合用户要求。
- 正确做法：参考站实测按钮为 36×36px、24px 图标、圆形浅灰背景；本地按用户要求使用 1px 黑色边框。
- 防复发检查：浏览器计算样式确认尺寸、圆角、边框和图标比例。

### 2026-08-18：验证码错误需要独立对话框

- 问题：验证码确认失败时仍使用 Toast，且验证码弹窗没有先关闭。
- 正确做法：确认按钮无论成功或失败都先关闭验证码弹窗；失败使用 Ant Design `modal.error` 显示“验证码已失效”和 `OK`。
- 防复发检查：错误确认后页面只保留错误对话框，不同时显示验证码弹窗或 Toast。

### 2026-08-18：验证码必须使用服务端图片算术题

- 问题：前端验证码题目和答案曾经写死，无法防止直接绕过，也没有真实图片资源。
- 正确做法：服务端生成两数加法题，答案范围 0-18，操作数随机使用阿拉伯或中文数字，生成带干扰线/噪点/旋转的 SVG 图片；答案存入缓存并由验证码 ID 校验。
- 防复发检查：前端只展示接口返回的图片，登录请求必须携带 `captcha_id` 和答案，服务端重新校验并消费验证码。

### 2026-08-18：验证码字符缩放需按最终显示尺寸设计

- 问题：SVG 画布为 220×80，但前端显示为 102×47，28px 字体随图片缩放后过小。
- 正确做法：保持图片框尺寸不变，将 SVG 内部字符放大到 44px 并重新调整基线和横向位置。
- 防复发检查：使用浏览器截图确认算式在验证码框内清晰可辨且不溢出。

### 2026-08-18：登录不得忽略统一响应 code

- 问题：前端只判断 Axios 是否抛异常，忽略 API JSON 中的 `code`，导致账号密码错误或验证码错误时仍执行登录成功回调。
- 正确做法：验证码校验必须检查 `code=0` 和 `verified=true`；登录必须检查 `code=0` 和 token 存在，否则只显示服务端错误并保持登录页。
- 防复发检查：覆盖错误验证码、错误账号密码、成功登录三种流程，禁止仅凭 HTTP 200 进入系统。

### 2026-08-18：验证码错误与失效提示必须区分

- 问题：错误答案和过期验证码都返回“验证码已失效”，用户无法判断是输入错误还是需要换题。
- 正确做法：服务端区分缓存不存在（失效）和答案不匹配（错误），前端错误对话框直接使用服务端消息。
- 防复发检查：使用有效验证码输入错误答案应返回“验证码错误”，使用过期/伪造 ID 应返回“验证码已失效”。

### 2026-08-18：登录成功必须先经过责任声明

- 问题：登录成功后直接进入主界面，缺少线上快照中的责任声明确认流程。
- 正确做法：登录成功显示独立协议页；同意后进入主界面，不同意清理用户令牌和账号并返回登录页。
- 防复发检查：覆盖登录成功、同意、不同意三个状态转换，协议页不得被主界面直接跳过。

### 2026-08-18：协议页宽屏样式与线上不一致

- 问题：协议内容卡片错误限制为 1100px，正文和底部按钮间距也偏离线上页面，宽屏截图下两侧空白过大。
- 正确做法：恢复线上快照的 90% 卡片宽度、15px 外层内边距、正文 20px 水平内边距、提醒区和按钮布局参数。
- 防复发检查：使用宽屏截图确认卡片接近视口 90%，标题分割线、正文行距和底部按钮位置与参考页一致。

### 2026-08-18：首页导航图标不能使用字符占位

- 问题：首页曾使用 emoji 和字符模拟导航图标，和线上 SVG 图标的形状、尺寸及选中状态不一致；左侧鲸鱼也没有复用登录标识。
- 正确做法：直接接入 `online-snapshot` 中已拉取的 SVG 资源，登录页与首页统一使用 `login-logo.svg`，选中项使用深蓝背景和白色图标文字，线路与退出保留对应颜色。
- 防复发检查：导航项不得回退到 emoji/字符图标，宽屏截图确认选中项有完整色块且未选中项保持图标与文字颜色区分。

### 2026-08-18：首页公告必须动态读取并支持查看全文

- 问题：顶部提示曾是用户端写死的静态文本，不能由管理端按站点调整，也没有线上公告的跑马灯和点击查看行为。
- 正确做法：复用站点 `settings` 增加公告标题/正文读写接口；管理端系统配置支持编辑，用户端登录后按 token 获取公告，顶部滚动显示，悬浮下划线，点击使用 Ant Design 信息弹窗展示完整正文。
- 防复发检查：保存不同站点的公告后分别登录验证内容隔离；公告为空时仍显示默认提示，点击弹窗不影响首页操作。

### 2026-08-18：首页导航图标尺寸需按线上实测值

- 问题：本地导航直接把 SVG 放大到 38px，图标视觉过大，且没有复现线上 38px 圆形图标容器和选中项白色边框。
- 正确做法：通过线上登录后 DOM 测量确认 SVG 为 25×25px，外层容器为 38×38px、2px 圆形边框，导航项约 76×86px；本地按实测值实现。
- 防复发检查：选中快速录入时图标外圈为白色，未选中项为深蓝色，线路/退出分别保持绿/红色边框。

### 2026-08-18：首页头部账户信息需匹配线上 outlined 结构

- 问题：本地鲸鱼标识颜色不稳定，账户字段使用普通标签布局且字段数量多于线上，金额接口返回的 `0.00` 也直接展示。
- 正确做法：头部保留公告条、白色信息区和主题工作区的分层；鲸鱼应用登录 SVG 的蓝色滤镜；账户仅显示账号、信用、已用、可用，并用浮动 label + 只读 input 模拟 outlined 字段；金额去除无意义的小数零。
- 防复发检查：接口返回 `0.00` 时页面显示 `0`，字段标签压在边框上方且输入不可编辑。

### 2026-08-18：首页导航不能居中占满头部

- 问题：导航使用居中布局，菜单会漂浮在头部中间，和彩票列表之间的距离不符合线上页面。
- 正确做法：导航改为 `flex-start`，从彩票列表右侧开始按固定间距排列。
- 防复发检查：宽屏下快速录入紧邻彩票列表右侧，后续菜单依次向右排列。

### 2026-08-18：总金额工具栏位置与线上首页不一致

- 问题：总金额、更多和居右显示在快速录入内容区上方，跑马灯速度也过快。
- 正确做法：总金额工具栏移至左侧余额栏顶部，快速录入区上方只保留页签，公告滚动周期调整为 35 秒。
- 防复发检查：内容区不再出现重复总金额，左侧工具栏三个元素保持同一行。

### 2026-08-18：彩票列表高度和左侧按钮间距需对齐线上

- 问题：彩票列表高度超过白色头部，主题区紧贴头部；总金额后的按钮间距过小，居右使用了字符箭头。
- 正确做法：彩票列表固定为 104px 并收紧头部内边距，主题区增加 5px 顶部留白；按钮分开排列，居右改用已拉取的 `arrow-right.svg`。
- 防复发检查：彩票列表不超出头部边界，头部与主题区有 5px 间距，按钮图标不使用字符占位。

### 2026-08-18：彩票列表不能用普通网格近似

- 问题：本地彩票列表用 grid 拼接标题、期号和倒计时，内部排版与线上两行 flex 结构明显不同。
- 正确做法：通过线上 DOM 实测后改为 `ul > li` 两行结构；外框 300×102px、1px #3f3f3f，每行 298×50px，第二行顶部 1px dashed #afafaf；每行内部拆分彩种状态组和期号倒计时组。
- 防复发检查：浏览器计算样式同时核对外框、两行尺寸和虚线参数，不再只凭截图估算。

### 2026-08-18：总金额工具栏需使用右侧按钮组

- 问题：本地左栏宽度和按钮排列与线上不同，三个元素直接参与同一 flex 分配，导致按钮坐标偏移。
- 正确做法：线上实测左栏/工具栏为 250px/40px，左右 padding 6px；总金额独立在左侧，两个按钮包在右侧组内，按钮间距 6px，尺寸约 51×28px 与 73×28px。
- 防复发检查：计算样式确认工具栏按钮组右对齐，不能用三个同级元素的 space-between 代替。

### 2026-08-18：居右按钮需要交换左右面板

- 问题：居右按钮只有视觉占位，没有改变余额栏和主工作区的位置。
- 正确做法：增加面板方向状态，点击后使用 grid 列交换左右区域，按钮文字切换为“居左”，箭头旋转 180 度。
- 防复发检查：连续点击两次应恢复原布局，主区域和余额栏不能只移动按钮而不交换容器。

### 2026-08-18：快速录入选项不能使用静态文字模拟开关

- 问题：本地选项使用“是/否”文字块，无法切换，也没有线上问号提示的悬浮说明。
- 正确做法：按线上 DOM 使用 Ant Design Switch（44×22px、深蓝选中/灰色未选中），每个带提示的选项使用 20px 圆形问号和 Tooltip，提示文案来自线上组件。
- 防复发检查：五个开关可独立点击，文本/号码未选中显示“文”，悬浮问号出现对应说明。

### 2026-08-18：快速录入外框需要统一 outlined 组件样式

- 问题：开关区域外框虽然有边框，但标题仍在框内，和账号展示字段的浮动 label 不一致。
- 正确做法：选项标题改为绝对定位的浮动 label，覆盖边框上沿；默认彩种框也采用同样结构。
- 防复发检查：开关项和默认彩种项的标题都压在边框线上，边框不会穿过文字。

### 2026-08-18：免密进入站点不能绕过责任声明

- 问题：站点管理员从管理端进入用户端时通过 `auto_token`，旧逻辑位于已废弃的登录实现中，正式登录组件无法可靠接管。
- 正确做法：在 `App` 的正式状态流处理 `auto_token`，写入用户会话后统一展示责任声明；只有点击同意才进入主界面。
- 防复发检查：同时覆盖账号密码登录和管理端免密进入两条路径，均先显示协议页。

### 2026-08-18：代理中心表单应使用抽屉工作区

- 问题：站点新增/编辑使用居中的弹窗，不符合管理端固定工作区操作习惯。
- 正确做法：代理中心新增和编辑统一使用右侧 `el-drawer`，编辑时加载当前站点字段并调用更新接口。
- 防复发检查：代理中心验证新增、编辑、取消和保存四个操作状态。

### 2026-08-18：站点表单不能提交代理专属字段

- 问题：代理中心复用了旧表单，提交 `parent_id` 到 `sites` 表，保存时报字段不存在。
- 正确做法：站点创建和更新接口过滤 `parent_id`、`level`，并单独处理站点域名及管理员密码。
- 防复发检查：每个资源提交前核对字段与数据库表结构，至少跑一次真实新增和编辑请求。

### 2026-08-18：平台管理员、站点管理员、站点用户不能共用用户表

- 问题：所有账号共用 `users.username` 全局唯一索引，导致不同站点无法创建同名用户或管理员。
- 正确做法：拆分 `admins`、`site_admins`、`site_users`；站点账号唯一键使用 `(site_id, username)`，所有账号和站点使用 `deleted_at` 逻辑删除。
- 防复发检查：验证不同站点同名账号可创建；同一站点重复账号必须拒绝；删除后数据仍可审计查询。

### 2026-08-18：站点编码不是业务字段，进入站点必须有反馈

- 问题：代理中心展示无业务意义的站点编码；没有反代域名时点击进入站点无任何反应；用户管理入口缺失。
- 正确做法：移除站点编码展示，进入站点无域名时提示配置域名，增加站点用户管理和站点筛选。
- 防复发检查：操作按钮必须有成功、失败或缺少配置的可见反馈。

### 2026-08-18：平台进入站点不能只打开空白域名

- 问题：进入站点按钮没有域名时静默无反应，且有域名时没有平台管理员免密进入机制。
- 正确做法：无域名明确提示；有域名通过 API 生成一次性站点管理员 token，用户端消费 `auto_token` 后进入站点。
- 防复发检查：验证无域名、有域名、目标站点登录态三种路径。

### 日期：问题标题

- 问题：
- 正确做法：
- 防复发检查：

### 2026-08-18：系统配置不能把多个功能混在同一页

- 问题：责任声明和首页公告共用一张长表单，功能边界不清晰；内容加保存按钮超过可视高度时会带动整个页面滚动。
- 正确做法：使用 Element Plus tabs 分离“责任声明”和“首页公告”，保存按钮只保存当前 tab；外层页面和工作区固定高度并隐藏页面滚动，富文本编辑区和公告文本区各自负责内部滚动。
- 防复发检查：浏览器点击每个 tab，确认另一功能字段不可见；检查页面 `scrollHeight === clientHeight`，并确认正文控件 `overflow-y: auto`。

### 2026-08-18：管理端公告保存后用户端仍显示旧固定文案

- 问题：用户端调用公告接口时没有带 `Authorization`，接口返回未登录；异常被静默吞掉后使用旧的写死温馨提示。
- 正确做法：公告请求和用户资料请求一样携带当前用户令牌；请求失败只显示“暂无公告”，不再显示业务固定文案。
- 防复发检查：动态配置接口必须检查认证头；保存公告后使用对应站点用户端刷新页面，确认顶部跑马灯和公告弹窗显示新内容。

### 2026-08-18：用户端请求代码散落在页面组件中

- 问题：多个组件分别创建 axios 实例并手动拼接 `Authorization`，请求错误和登录失效无法统一处理。
- 正确做法：统一使用 `src/utils/request.ts`，请求拦截器自动注入令牌，响应拦截器统一处理业务错误和 401；具体接口放入 `src/api/auth.ts`、`src/api/user.ts`。
- 防复发检查：页面组件不得再调用 `axios.create` 或手动拼接认证头；新增接口必须先放入对应 api 模块。

### 2026-08-18：代理端必须使用独立身份域和主题

- 问题：代理端不能直接复用用户 token 或平台管理员账号，也不能只把用户端文案改成代理端。
- 正确做法：代理端使用独立 React 工程、`agent` token scope、`agent_admins` 表和 `/agent/*` API；视觉布局复用用户端结构，但按线上实测使用纯绿色和白色海豚线稿。
- 防复发检查：代理请求只读取 `agent_token`，账号按 `(agent_id, username)` 唯一；浏览器验证登录背景、Logo、输入框尺寸和验证码弹窗。

### 2026-08-18：localhost 进入站点和站点管理员登录失败

- 问题：进入站点前端要求域名必须包含点号，`localhost` 被判无效；本地地址被拼成 HTTPS；站点编辑只更新 `sites.manager_*`，没有同步 `site_admins`，代理端也只查询 `agent_admins`。
- 正确做法：允许 localhost/本地 IP 并在本地使用 HTTP；编辑站点时同步站点管理员账号；代理登录按域名隔离并兼容站点管理员与平台自有站点。
- 防复发检查：使用 `localhost:5176` 点击进入；重新保存站点管理员密码后，用同一账号完成验证码登录。当前数据库中的旧密码哈希与用户提供的密码不匹配，需要在站点编辑抽屉重新保存密码后才能登录。

### 2026-08-18：代理首页顶部布局与线上站不一致

- 问题：本地代理端把会员数量、额度和导航混在一行，导航名称和图标也没有按线上代理端排列。
- 正确做法：根据线上页面实测拆分公告、Logo/代理账号、导航和彩种状态条；导航使用“总货概览、分类账、报表、开奖号码、下级管理、拦货、日志、规则说明、设置、子账号、更换线路、退出”，账号区只保留代理账号。
- 防复发检查：浏览器截图确认导航项约 74×75、圆形图标 36px、选中项绿色底；账号区不得重新加入会员数量或额度字段。

### 2026-08-18：代理账号区最终只显示一个账号框

- 问题：上一版误将用户端的四项余额框套用到代理端。
- 正确做法：代理端只保留线上模型的单个 130×40 账号框，“账号”作为浮动 label 压在上边框，框内只显示“代理：真实账号名”。
- 防复发检查：浏览器确认代理端账号区只有一个框，label 不得作为框内第一行，也不出现信用、已用、可用字段。

### 2026-08-18：快速录入选项区域与独立模块层级混淆

- 问题：误把页面外层的浅蓝分隔色当成选项区域背景，弱化了每个 outlined 选项是独立模块的层级；开关和默认彩种按钮也没有统一居中，两个彩种紧贴。
- 正确做法：选项区域及各选项卡、label 都使用白色背景，通过独立 outlined 边框和间距区分模块；选项卡使用 flex 居中，彩种按钮增加固定间距。
- 防复发检查：截图确认选项区域为白色、各 outlined 模块互相分离；label 与各自卡片连为一体；内容居中且两个彩种按钮保留间距。

### 2026-08-18：快速录入选项白色区域超宽且缺少上下间距

- 问题：选项区域只有左右内边距，没有外边距，因此白色背景占满主栏并紧贴上下模块，看起来比输入区域更宽。
- 正确做法：选项区域宽度使用主栏宽度减去 24px，设置左右 12px、上下 10px 外边距；移除水平内边距以保持内部卡片原位置。
- 防复发检查：确认选项白色区域与上方文本框左右边缘对齐，上下均能看到主页面分隔底色，页面无横向溢出。

### 2026-08-18：快速录入卡片边缘和首项间距不足

- 问题：卡片边角过于生硬，选项区域上下留白不足，卡片内部内容贴边，首个卡片缺少左侧呼吸空间。
- 正确做法：卡片增加 3px 圆角和内部 padding，选项区域上下 padding 调整为 12px，首个卡片增加 4px 左间距。
- 防复发检查：确认圆角仅为轻微视觉修饰，卡片内容仍居中，首项不贴选项区域左边缘。

### 2026-08-18：首页余额与快速录入操作颜色不统一

- 问题：信用和可用余额没有业务状态色，“替换”和“生成”使用了两种接近但不同的黄色，默认彩种文字偏小。
- 正确做法：信用及金色操作按钮统一使用 `#b87613`，已用使用红色，可用使用绿色；默认彩种按钮字号提升到 15px。
- 防复发检查：确认四项账户信息中仅信用、已用、可用数值着色，替换和生成背景一致，彩种按钮文字清晰且未撑破卡片。

### 2026-08-18：默认彩种按钮被 flex 压缩导致文字换行

- 问题：默认彩种卡片继承 flex 布局后，两个按钮可被压缩，导致“排列三”换成上下两行，与线上横向按钮样式不一致。
- 正确做法：按钮设置 `flex: 0 0 auto` 和 `white-space: nowrap`，卡片减少水平 padding，保证福彩 3D 与排列三横向并排。
- 防复发检查：截图确认两个彩种按钮在同一行、文字完整显示、卡片边框不被撑破。

### 2026-08-18：新标签按钮受浮动布局影响位置下移

- 问题：新标签按钮使用 `float: right`，会根据前方行内内容参与排版，在不同宽度下出现下移或位置偏差。
- 正确做法：替换模块设为相对定位，新标签按钮绝对定位到右上角，与替换输入行对齐。
- 防复发检查：确认按钮始终位于替换模块右上角，不受前方输入框和替换按钮宽度影响。

### 2026-08-18：新标签按钮应位于替换模块第二行

- 问题：将“＋新标签”错误放在输入控件同一行右上角，线上实际布局是在输入行下方，且两行之间有虚线分隔。
- 正确做法：替换模块高度调整为 96px，使用中部虚线分隔；新标签按钮绝对定位到下方右侧。
- 防复发检查：截图确认输入框在第一行，虚线横贯模块，新标签按钮位于虚线下方，文本区域整体向下对齐线上位置。

### 2026-08-18：快速录入文本框缺少清空与字数底栏

- 问题：文本框下方没有线上页面的虚线分隔、禁用态清空按钮和 `0/10,000` 计数。
- 正确做法：增加独立底栏，使用 Ant Design 删除图标，清空按钮按内容启用，输入长度限制为 10,000。
- 防复发检查：空内容时按钮为禁用灰色，输入后计数实时更新且清空可用，底栏位于文本框与选项卡之间。

### 2026-08-19：温馨提示只有单行占位条

- 问题：右下角仅显示“温馨提示 ↑ ✕”文字，没有线上参考中的标题栏、正文面板和按钮交互。
- 正确做法：实现固定宽度消息面板，深蓝标题栏居中显示标题，右侧使用圆形 Ant Design 图标按钮，正文使用白底黑字；支持收起、展开和关闭。
- 防复发检查：确认展开时正文可见，收起按钮切换面板高度，关闭按钮移除面板，按钮形状和标题栏颜色与参考图一致。

### 2026-08-19：规则说明按钮没有打开规则弹窗

- 问题：快速录入底部“规则说明”按钮没有交互，无法查看线上规则内容。
- 正确做法：拆出 `RuleInstructionsModal` 组件，使用 Ant Design Modal/Tabs，按参考图提供四个规则页签、滚动内容、黄色重点提示和关闭按钮。
- 防复发检查：点击按钮能打开弹窗，页签可切换，正文区域可滚动，关闭按钮和右上角取消均能关闭。

### 2026-08-19：规则说明正文无法滚动

- 问题：页签内容区使用固定高度，但 Ant Design Modal 内部为 flex 布局，实际高度链路未建立，正文被 footer 截断且滚轮无效。
- 正确做法：Modal container/body/Tabs 建立完整 flex 高度链路，内容 holder 使用 `flex: 1; min-height: 0; overflow-y: scroll` 独立滚动。
- 防复发检查：弹窗高度不超过视口，鼠标位于正文时滚轮可用，滚动条始终显示，标题、页签和底部关闭按钮保持固定。

### 2026-08-19：规则滚动样式没有命中当前 Ant Design DOM

- 问题：当前 Ant Design 生成的页签正文容器是 `.ant-tabs-body-holder`，旧选择器 `.ant-tabs-content-holder` 不存在；同时规则正文高度不足以产生溢出。
- 正确做法：同时适配 `.ant-tabs-body-holder`，设置 `min-height: 0` 与 `overflow-y: scroll`，并补齐文本规范的后续规则内容。
- 防复发检查：浏览器读取正文容器应满足 `scrollHeight > clientHeight`，滚动前后 `scrollTop` 应发生变化。

### 2026-08-19：规则说明内容需要在管理端系统配置维护

- 问题：用户端规则弹窗内容写死在 React 组件，管理端“系统配置”无法按站点编辑。
- 正确做法：新增规则说明配置页签和四类文本字段；ThinkPHP 按站点保存 `rule_title/rule_basic/rule_special/rule_amount/rule_text`；用户端通过认证接口读取并渲染，“【重点】”行自动显示黄色提示。
- 防复发检查：管理端切换站点后内容隔离，保存后用户端刷新弹窗显示新内容；接口缺省时使用默认规则。

### 2026-08-19：更多按钮没有进入明细查询视图

- 问题：左侧“更多”按钮没有交互，无法进入线上参考中的日期和原始文本查询页面。
- 正确做法：增加 `MorePanel` 独立视图，点击后隐藏快速录入左右栏，展示日期选择、原始文本输入、搜索和返回控件；返回后恢复快速录入。
- 防复发检查：点击更多后头部保持不变，查询工具栏占满内容区，点击返回恢复原页面。

### 2026-08-19：投注记录页签只有通用暂无数据占位

- 问题：点击“投注记录”只显示通用空页面，与线上记录查询页的筛选栏、表头和空数据布局不一致。
- 正确做法：增加独立 `BettingRecords` 组件，提供中奖筛选、原始文本、投注时间范围、搜索、导出金额和记录表格空状态。
- 防复发检查：切换投注记录页签后确认查询栏和七列表头出现，空数据图标居中显示，快速录入页签内容不受影响。

### 2026-08-19：停押降水页签只有通用占位

- 问题：停押降水页签没有线上页面的筛选条件、排序选项和表格结构。
- 正确做法：增加 `StopDrop` 组件，覆盖查号码、类型、彩种、完整分类、投注日期、倒序/正序及停押降水表头。
- 防复发检查：切换页签后确认筛选栏和表头出现，空状态居中，快速录入和投注记录仍可切换。

### 2026-08-19：快速录入新标签和替换按钮没有交互

- 问题：线上点击“新标签”会打开添加弹窗，本地按钮无动作；替换输入也没有实际替换文本。
- 正确做法：使用 Ant Design Modal/Input 实现添加新标签，确认后在替换模块下方显示标签；替换按钮按输入内容替换文本框中的字符串。
- 防复发检查：点击新标签能打开/关闭弹窗，空标签不会添加，确认后标签可见；输入替换前后文本可验证替换结果。

### 2026-08-19：添加新标签弹窗视觉与线上不一致

- 问题：本地直接使用 Ant Design 默认 Modal，宽度、输入框背景、footer 分隔线和按钮样式与线上截图不一致。
- 正确做法：增加 `tag-modal` 主题覆盖，复刻线上 500px 弹窗、无圆角蓝色输入框、footer 分隔线、橙色关闭和深蓝确认按钮。
- 防复发检查：截图确认弹窗宽度约 500px，输入框位于内容区，按钮在右下 footer 内且无圆角。

### 2026-08-19：用户业务页面缺少真实后端数据基础

- 问题：后端只有认证、余额和站点配置，没有投注记录、投注明细、历史账单、开奖数据表及用户查询接口。
- 正确做法：新增迁移 `202608190001` 创建四张业务表；新增 `UserBusiness` 控制器和四个用户查询接口，所有查询按 token 中的 site_id/user_id 限制范围。
- 防复发检查：迁移执行成功；未登录请求被拒绝；列表接口统一返回 list/total，账单接口额外返回合计数据。

### 2026-08-19：业务查询接口创建后前端仍显示静态空状态

- 问题：投注记录虽然有页面结构，但没有调用后端查询，筛选也不会影响数据。
- 正确做法：新增用户端业务 API 类型和请求方法，投注记录接入 `bet-records`，搜索按钮传递状态、日期、原始文本参数并渲染返回行。
- 防复发检查：无数据时仍显示空状态；后端返回记录时表格行按真实字段显示；请求自动携带当前用户 token。

### 2026-08-19：导航页面仍由通用占位组件承载

- 问题：下注明细、历史账单、会员资料、开奖号码和修改密码路由都落到“暂无数据”通用页。
- 正确做法：拆出独立页面组件；下注明细、历史账单、开奖号码通过真实 API 拉取，会员资料和修改密码先提供对应表单/资料布局。
- 防复发检查：点击顶部导航后 URL 与页面组件对应，列表页空状态和真实数据行使用同一表格结构。

### 2026-08-19：修改密码页面没有后端保存能力

- 问题：修改密码只有静态表单，提交不会校验原密码或保存新密码。
- 正确做法：新增 `user/auth/password` 接口，校验当前用户会话、原密码、新旧密码一致性和数字字母组合规则；页面提交后显示后端消息。
- 防复发检查：错误原密码/弱密码/确认不一致均被后端拒绝，成功后更新 site_users 密码哈希。

### 2026-08-19：需要完整审查线上功能及后端接口边界

- 问题：此前只按单个页面局部实现，没有完成全部导航和后端数据能力盘点。
- 正确做法：已逐页检查线上下注明细、历史账单、会员资料、开奖号码、规则说明、修改密码、更多查询及停押降水的控件和表格结构；本地先修正新标签弹窗，后续按接口/数据表逐项接入。
- 防复发检查：每个页面实现前先记录线上字段、筛选项、表头、按钮行为和对应后端数据来源，避免继续用通用占位页。

### 2026-08-19：投注时间只能点击输入框图标

- 问题：原生 `datetime-local` 的选择器触发区域主要集中在右侧图标，点击 label 或边框空白处没有明显反应。
- 正确做法：起止时间外层区域统一绑定点击事件，调用 `showPicker()` 并聚焦对应输入框。
- 防复发检查：分别点击起始时间的标题/边框、结束时间框的任意位置，均能打开对应选择器。

### 2026-08-19：投注时间范围被拆成两个框且按钮使用字符图标

- 问题：起始和结束时间分别渲染，未形成线上完整 outlined 框；搜索、导出按钮使用字符模拟图标，形状不一致。
- 正确做法：使用单一时间范围容器包裹起止输入和“至”，按钮改用 Ant Design `SearchOutlined` 与 `ExportOutlined`。
- 防复发检查：截图确认“投注时间”label 位于同一外框顶部，两个时间值和“至”在框内，按钮图标为矢量图标。

### 2026-08-19：中奖筛选只有全部选项

- 问题：投注记录筛选中的中奖下拉框只能选择“全部”。
- 正确做法：增加“未中奖”和“已中奖”选项，默认仍为“全部”。
- 防复发检查：打开下拉框确认三个选项完整显示且 value 分别为 all/unwon/won。

### 2026-08-19：规则说明误用普通多行文本框

- 问题：规则说明四个页签只能输入纯文本，无法选中文字设置颜色、加粗、字号、对齐和列表。
- 正确做法：四个规则页签统一使用 WangEditor 富文本编辑器，保存 HTML；旧纯文本加载时自动转换为段落 HTML，用户端使用 DOMPurify 清洗后按富文本渲染。
- 防复发检查：每个页签都必须显示完整工具栏，保存后重新加载格式仍存在，用户端规则弹窗的颜色、字号和段落格式与管理端一致。

### 2026-08-19：代理端总货查询区与线上结构不一致

- 问题：提交按钮直接占满按钮区域，未复刻线上“外层字段框 + 内层提交按钮”的结构。
- 正确做法：查询区使用查账号、来源、提交三个并列字段组，提交组内使用 52×30 红色描边按钮，并保持整行白色容器和居中布局。
- 防复发检查：截图对比三个字段组的边框、间距、提交按钮内外层尺寸和垂直居中位置。

### 2026-08-19：用户端配置不能直接替代代理端配置

- 问题：用户端公告和责任声明已接入，但代理端仍显示写死公告，也没有独立服务协议；直接复用用户端字段会导致两端内容互相覆盖。
- 正确做法：新增 `agent_agreement_*` 和 `agent_announcement_*` 站点配置键及独立管理/API；代理端只使用 agent token 读取所属站点配置。
- 防复发检查：后台切换站点保存后，代理端首次登录显示对应协议；同意后公告跑马灯和弹窗显示对应公告，用户端原有配置不受影响。

### 2026-08-19：进入站点路由被资源列表路由截获

- 问题：`admin/agent-center/:id/enter` 位于 agent-center 列表路由之后，请求返回列表而不是免密 token；控制器参数名也与路由变量 `id` 不一致。
- 正确做法：将具体 enter 路由放在列表/动态路由之前，并由 Request 明确读取 `id`。
- 防复发检查：进入接口必须返回 `url/token/name`，该 token 能读取 `/agent/agreement` 和 `/agent/announcement`。

### 2026-08-19：代理端协议误用用户端蓝色主题

- 问题：代理端复用了用户端协议组件后，也继承了蓝色页面背景和蓝色主按钮，与线上代理端绿色视觉不一致。
- 正确做法：保留共享协议结构，在代理端外层增加独立主题作用域，将页面背景和同意按钮覆盖为代理绿色。
- 防复发检查：代理端协议背景必须为深绿色，用户端协议仍保持蓝色，两端样式互不覆盖。

### 2026-08-19：公告正文覆盖底部保存按钮

- 问题：公告 textarea 使用 `calc(100vh - 390px)` 固定高度，实际超出所属表单项并覆盖保存栏；用户端公告和代理端公告都会发生。
- 正确做法：公告正文表单项使用 flex 占据剩余空间，textarea 高度严格为父容器 100% 并内部滚动；保存栏固定 50px 高度、白色背景和独立层级。
- 防复发检查：浏览器测量 textarea 底部不得超过保存栏顶部，并分别检查“首页公告”和“代理端公告”两个页签。

### 2026-08-19：代理端显示用户端默认责任声明

- 问题：代理协议缺省时后端复用了 `DEFAULT_AGREEMENT_CONTENT`，前端请求失败时也回退到用户端 `defaultAgreement`；无效 agent token 又被误报为站点不可用。
- 正确做法：前后端分别定义代理服务协议默认内容；代理配置接口只使用 agent token，token 无效返回 401 并触发重新登录。
- 防复发检查：代理端协议标题必须为后台配置的“代理服务协议”，正文来自 `agent_agreement_*`；请求失败时不得出现用户端“致会员”声明。

### 2026-08-19：站点域名同时承担代理端和用户端入口

- 问题：`domains` 原来只有一个域名字段，平台进入站点、代理登录和用户登录没有明确入口边界，容易把用户域名当成代理域名使用。
- 正确做法：新增 `domain_type`，取值为 `agent`（代理端）或 `user`（用户端）；代理相关接口只匹配 agent 域名，用户登录必须匹配 user 域名；代理中心编辑页分别显示两类域名。
- 防复发检查：平台“进入站点”只能跳代理端域名；用户端域名不能登录代理端，代理端域名不能登录用户端；同一站点可以同时配置两类域名。

### 2026-08-19：代理端规则说明仍是占位页或复用用户端内容

- 问题：代理端菜单虽然有“规则说明”，实际路由仍显示占位文案，没有读取管理端按站点保存的规则内容。
- 正确做法：增加带代理 token 校验的 `/agent/rules` 接口，读取当前代理所属站点的 `rule_title/rule_basic/rule_special/rule_amount/rule_text`；代理端独立渲染四个页签，HTML 通过 DOMPurify 清洗后再显示，纯文本兼容换行和重点标记。
- 防复发检查：代理端打开 `#/rules` 必须显示管理端配置标题和正文；点击四个页签分别切换对应字段；修改管理端规则后刷新代理端，不能回退到用户端固定规则或占位文案。

### 2026-08-19：彩票名称写死在站点端，无法按站点控制可见范围

- 问题：用户端和代理端的“福彩3D/排列三”来自前端固定代码，平台无法新增彩票，也无法让不同站点看到不同彩票。
- 正确做法：使用 `lotteries` 维护平台目录，用 `site_lotteries` 保存站点分配关系；管理端通过“彩票列表”抽屉勾选站点；`/user/lotteries` 和 `/agent/lotteries` 必须按当前登录会话的 `site_id` 过滤，只返回已启用且已分配的彩票。
- 防复发检查：取消某站点的彩票分配后，用户端顶部、快速录入默认彩票选项和代理端顶部均不能继续显示该彩票；新增彩票必须先分配站点才可见。

### 2026-08-19：开奖历史接口地址和历史数据没有平台配置入口

- 问题：第三方开奖地址写死在代码中，彩票列表没有历史数据入口，也没有期号去重和分页同步。
- 正确做法：系统配置保存平台级 `lottery_api_base_url`；历史页按彩票编码映射第三方 `type`，每次同步最多请求 10 页、单页最多 1000 条；`lottery_histories` 以 `lottery_id + code` 唯一，已存在期号更新但不重复插入，页面使用后端分页。
- 防复发检查：更换 Base URL 后请求必须使用新地址；历史页不能一次加载全部记录；重复打开页面不能产生重复期号。

### 2026-08-19：全平台配置和站点配置混在同一页面

- 问题：开奖接口属于全平台唯一配置，却和按站点隔离的责任声明、公告、协议、规则放在同一个带站点选择的页面里，容易误以为开奖接口按站点生效。
- 正确做法：`/settings` 使用独立系统配置页面，不显示站点选择，只维护全平台开奖接口；`/site-settings` 使用站点配置页面，所有内容通过站点下拉框隔离保存。
- 防复发检查：系统配置页面不出现站点选择；站点配置页面不出现开奖接口；修改开奖接口后所有站点历史同步都使用同一 Base URL。

### 2026-08-19：站点配置重复录入，缺少参考站点填充

- 问题：为多个站点配置相同内容时只能逐项重复输入，容易漏填或复制错误。
- 正确做法：站点配置提供“参考站点”下拉框，排除当前站点；点击“填充配置”读取参考站点五组配置，填入当前编辑表单但不自动保存。
- 防复发检查：填充后必须显示“请检查后分别保存”；切换当前站点不会把参考站点内容误保存到数据库；参考站点列表不能包含当前站点。

### 2026-08-19：打开开奖历史重复执行大批量同步并出现加载动画

- 问题：历史查询接口同时承担远程同步，打开页面或每分钟刷新都会请求多页第三方数据，表格反复显示加载转圈。
- 正确做法：查询与同步分离；`GET lottery-history` 只查本地数据库，后台 `lottery:sync` 每分钟对每种彩票请求 `page=1&limit=1`；前端定时刷新使用静默模式，不改变表格 loading 状态。
- 防复发检查：打开历史页时网络请求不能等待第三方接口；后台单次同步请求的 limit 必须为 1；每分钟刷新期间表格不出现遮罩或转圈，新期号按倒序出现在第一页第一行。

### 2026-08-19：赔率详情接口被通用彩票列表路由截获

- 问题：`/admin/lotteries/:id/odds` 被较宽泛的资源路由错误处理，赔率页误显示彩票目录行。
- 正确做法：赔率接口使用独立前缀 `/admin/lottery-odds/:id`，避免与彩票目录资源路径重叠；赔率表独立保存限额、赔率和离线赚水字段。
- 防复发检查：进入福彩3D赔率详情必须显示“类别、最小下注、赔率上限、单注上限、单项上限、赔率、离线赚水”等列，不能出现 fc3d/pl3 彩票目录行。

### 2026-08-19：福彩3D与排列三默认赔率不完整

- 问题：初版只录入定位、独胆、双飞、对子等10项，缺少参考站中的直选、组三多码和组六多码默认配置。
- 正确做法：两者共用三位数玩法模板，但落库为各自独立赔率记录；使用幂等迁移按“彩票 + 类别 + 赔率项”去重写入，后续可分别调整。
- 防复发检查：福彩3D和排列三各应至少包含26项默认赔率；必须能检索到组三七码、组三九码、组六七码和组六九码，编辑一个彩种不能改变另一个彩种。

### 2026-08-19：首页切换投注记录后顶部账号区域突然变宽

- 问题：顶部 header 使用可收缩的 flex 子项，账号区声明 340px 但实际会被挤压到 184px；不同页面内容变化时宽度可能跳变，四个账号字段也随之改变。
- 正确做法：按线上账号容器约 250px 的宽度固定 `.account`，设置 `flex: 0 0 250px`，并锁定鲸鱼图标的 `flex-basis`，避免页面页签切换触发 flex 重新分配。
- 防复发检查：在 `#/kb` 与“投注记录”页签之间切换，分别测量 `.account` 和 `.account-field` 的 `getBoundingClientRect()`，宽度必须保持 250px 和 122.5px；同时确认 document scrollWidth 不超过 viewport，导航最后一项不被挤出。

### 2026-08-19：快速录入功能被误认为已经完整复刻

- 问题：原实现只有简单逐行解析和本地插入，开关、复制、标签删除/应用、预警持久化、余额与重复提交保护没有真正生效。
- 正确做法：增加中文数字和常见金额单位归一化、统一码数统计、粘贴自动生成、立即识别、复制小票、预警设置保存、标签应用/删除、余额校验、事务和 15 秒重复提交保护。
- 防复发检查：构建后逐项验证开关行为；登录状态下验证有效文本、无效文本、余额不足和重复提交都返回明确服务端消息，不能只看页面是否显示按钮。

### 2026-08-19：登录后页面仍显示但快速录入接口返回未登录

- 问题：页面保留旧的主界面状态，但 API token 已失效；直接点击生成时接口返回“未登录或登录已过期”。
- 正确做法：重新登录后在同一个本地页签验证；本次重新登录后已验证生成成功、失败提示、立即识别、标签新增/删除和预警配置刷新后保留。
- 防复发检查：测试前必须点击生成确认 API 返回成功，不能仅依据页面仍停留在 `#/kb` 判断会话有效。

### 2026-08-19：下注余额不足时确认弹窗未关闭

- 问题：`modal.confirm` 的 `onOk` 捕获后继续抛出异常，Ant Design 将弹窗保持在打开状态。
- 正确做法：捕获后端错误并显示消息，不再重新抛出，让确认弹窗正常关闭。
- 防复发检查：余额不足或接口 422 时，确认框必须关闭，页面保留生成结果并显示“可用余额不足，无法下注”。

### 2026-08-19：替换模块标签列表位置偏上

- 问题：标签列表使用 `bottom: 10px`，在替换模块内与用户参考位置相比偏上。
- 正确做法：将标签列表下移 6px，改为 `bottom: 4px`，不改变替换输入行和右侧新标签按钮。
- 防复发检查：测量 `.replace` 与 `.tag-list` 边界，标签列表应比原位置向下 6px 且仍在替换模块内部。

### 2026-08-19：投注记录和停押降水只有基础占位查询

- 问题：投注记录缺少分页、导出和加载错误反馈；停押降水筛选控件没有状态、查询请求或后端数据表。
- 正确做法：投注记录接入分页查询和 CSV 导出；新增 `user_stop_drops` 表及 `/user/stop-drops` 接口，停押降水全部筛选条件使用受控状态并渲染真实返回行。
- 防复发检查：分别打开两个页签，确认 API 请求后显示总数/空状态；改变筛选条件点击搜索必须重新请求，投注记录导出按钮无数据时给出提示。

### 2026-08-19：快速录入标签缺少参考站交互和样式

- 问题：标签被渲染成蓝色普通块，点击只填入替换输入框，删除按钮不是参考站右上角红色圆标。
- 正确做法：标签卡片改为白底/透明背景虚线边框，标签文字点击载入录入文本，删除按钮绝对定位在右上角并使用红色圆形 X。
- 防复发检查：标签卡片应有 `1px dashed #bfbfbf` 边框，删除按钮 18px 圆形且偏移到右上角；点击文字应改变文本域内容，点击 X 只删除标签。

### 2026-08-19：修改密码页误做成左侧小表单

- 问题：本地使用最大宽度 620px 的纵向表单，与参考站横向三栏页面完全不一致，也缺少禁止密码提示和第二条新密码规则。
- 正确做法：使用全宽 150px 白色三栏，三栏水平居中并以深蓝虚线分隔；新密码栏展示两条规则，下方显示黄色禁用密码提示，保存按钮独立居中。
- 防复发检查：参考站实测三栏容器高 150px，左右输入框 171×32px，中间输入框约 248×32px，保存按钮约 80×28px；浏览器可用时再复核本地实际尺寸。

### 2026-08-19：禁用密码提示条字体和边框判断错误

- 问题：提示条使用 13px 字体、偏亮黄色和单独的粗大“注：”节点；把审查元素的蓝色选中框误认为实际边框会导致错误复刻。
- 正确做法：读取计算样式确认参考站无实际边框，使用 14px 正常字重、36px 高、15px 圆角、`#ffffa1` 背景；“注：”由 16px 红色粗体伪元素生成。
- 防复发检查：区分浏览器评论/审查高亮与页面真实 CSS，必须通过 `getComputedStyle` 核对 border、字体和背景。

### 2026-08-19：温馨提示收起没有动画

- 问题：内容使用条件渲染，点击收起时 DOM 直接卸载，面板瞬间跳变。
- 正确做法：始终保留内容节点，通过 `max-height`、内边距、透明度和位移的 280ms transition 实现展开/收回。
- 防复发检查：点击收起和展开时内容节点仍存在，面板高度应在过渡期间连续变化。

### 2026-08-19：温馨提示淡入淡出不平滑

- 问题：同时切换固定 `height`、`max-height`、透明度和位移，`height: 0` 没有参与 transition，导致面板先跳变再淡出。
- 正确做法：移除固定 height、透明度和位移动画，只过渡 `max-height` 与上下内边距，使用统一的 240ms material easing。
- 防复发检查：收起过程中面板底边应连续向上移动，文字随裁切区域自然隐藏，不出现闪烁或透明残影。

### 2026-08-19：温馨提示不应继续手写动画

- 问题：反复调整 CSS transition 仍难以获得自然的展开/收起手感。
- 正确做法：引入 `motion`，使用 spring 动画驱动面板高度和箭头旋转，CSS 只负责静态布局。
- 防复发检查：业务组件直接使用 `motion/react`，温馨提示 CSS 中不再存在手写 transition 或收起状态类。

### 2026-08-19：快速录入结果把整段文本显示成单条失败

- 问题：生成结果区只显示简单状态，无法对应粘贴文本中的每一行，也没有按参考站分别展示成功行的笔数/金额和失败行的具体原因。
- 正确做法：将接口返回的每条 `QuickEntryLine` 渲染成独立结果块；成功行显示编号、彩种、原文、笔数和金额，失败行显示编号、原文及服务端原因；总码数和总金额只汇总成功行。
- 防复发检查：输入 `123 456 789 直选各1元` 和 `测试` 两行，页面必须显示两条结果；第一条为福、3 笔、金额 3，第二条为失败并显示“未识别到有效号码”，顶部汇总为 3 码、¥3。

### 2026-08-19：中文“各”导致单注金额没有按笔数累计

- 问题：解析器使用 `$m[0][0]` 读取正则匹配的首字符，UTF-8 中文字符被按字节截断，`perUnit` 始终为 false，导致“各1元”的三笔号码只计算 1 元。
- 正确做法：使用 `mb_substr($m[0], 0, 1)` 读取“各/每/个”，再按识别笔数乘以单注金额。
- 防复发检查：`123 456 789 直选各1元` 必须返回 `count=3, amount=3.00`；`壹贰叁 贰叁肆 组六各2角` 必须返回 `count=2, amount=0.40`。

### 2026-08-19：百十个定位集合无法识别

- 问题：旧解析器只提取独立的 1 到 3 位号码，不认识“百/十/个”后的数字集合；其中“个1235679”还会与“个10”的金额写法产生歧义。
- 正确做法：定位玩法分别提取百位、十位、个位集合并生成笛卡尔积；福体同时下注时将组合数乘以 2。金额表达式存在多个候选时取句中最后一个，从而把定位的“个1235679”保留为号码集合，把“各10”识别为单注金额；句末“共/合计/总计”必须与计算金额一致。
- 防复发检查：`福体百0234569十0234689个1235679各10合计6860` 必须返回 `category=福体, count=686, amount=6860.00`；本地快速录入页面顶部和结果行都必须显示 686 与 6860。

### 2026-08-19：快速录入操作栏与参考站尺寸和文字样式不一致

- 问题：本地操作栏依赖统一 `gap`，按钮高 33px 且为粗体，汇总和预警共用普通 label 结构，缺少参考站的两条整高虚线；预警无配置时还会显示空白。
- 正确做法：根据参考站计算样式重建子元素结构，使用 70px 操作栏、26px 按钮、按钮左右 10px margin、60px 虚线分隔、14px 标签/16px 红色数值，以及独立的问号标签和 80×32px 浅蓝输入框；缺省、旧空配置和清空操作统一回填 `0`。
- 防复发检查：分别测量参考站与本地按钮、分隔线、汇总、预警标签和输入框尺寸；清空预警框后 value 必须立即为 `0`，刷新后仍为 `0`。

### 2026-08-19：文本输入区未随默认彩种切换且底栏脱离外框

- 问题：本地 textarea 固定为紫色且自身占满 212px，清空/计数栏另起一块；默认彩种两个选项也共用紫色选中样式，排列三没有参考站的绿色状态。
- 正确做法：文本区使用一个 212px、1px 深灰边框的 flex 容器，内部为 176px textarea 和 34px 底栏；底栏顶部使用深蓝虚线。福彩3D textarea 使用 `#f3e8fc`，排列三使用 `#e4fff6`；福彩选项使用 `#7a2bc3`，排列三使用 `#00724d`，各自控制选中和未选中状态。
- 防复发检查：切换两个默认彩种时 textarea 背景和对应选项颜色必须同步变化；外框始终高 212px，textarea 176px，底栏 34px，清空/计数栏不得脱离外框或引起高度跳动。

### 2026-08-19：快速录入结果行缺少新增、删除和号码详情

- 问题：结果区只能静态显示识别摘要，无法按行新增/删除；成功行没有统一的“详”入口，福体的双彩种号码也无法查看完整明细。
- 正确做法：结果行左侧增加红色删除、绿色新增按钮；新增复制当前识别行并重新编号，删除移除当前行；成功行仅保留一个“详”按钮，使用可滚动弹窗展示号码，福体将每个组合展开为“福 号码”和“体 号码”。
- 防复发检查：成功行 `123 456 789 直选各1元` 点击“详”必须看到 123/456/789；点击新增后顶部汇总从 3 码、¥3 变为 6 码、¥6，连续删除后恢复 0；福体 686 笔详情数量必须为 686。

### 2026-08-19：号码详情弹窗样式与参考站不一致

- 问题：本地详情使用简单白色号码卡片，没有参考站的彩种色标、玩法标题、重复“号码/金额”列、蓝色表格单元格和底部关闭按钮。
- 正确做法：详情弹窗改为 1000px 宽，正文显示总笔数/总金额；每个彩种独立渲染色标与玩法标题，表格固定 8 组号码/金额列，数据区限制高度并独立滚动，底部使用蓝色“关 闭”按钮。
- 防复发检查：普通直选详情必须按“号码 金额”重复 8 组显示；福体详情必须分别显示体、福两段且总详情条数与总笔数一致；滚动条只能出现在详情表格区域。

### 2026-08-19：号码详情中的号码和金额没有组成一对

- 问题：号码和金额被当成 16 个独立网格项连续排列，视觉上无法确认对应关系；最后一行不足 8 组时没有补齐空单元格，表格边框也不完整。
- 正确做法：每个号码/金额使用一个两列 pair 容器，每行固定渲染 8 个 pair；所有格子都有右边框和下边框，最后一行缺失的 pair 同时填充“— / —”。
- 防复发检查：3 个号码的详情必须显示一行 8 对，其中前三对为号码/金额，后五对均为“— / —”；测量每对两个单元格应同高 32px，并具有 `1px` 右、下边框。
- 补充验证：每行 DOM 必须只有 8 个 pair，每个 pair 恰好包含号码、金额两个文本区域，并拥有独立 `1px solid #8f8f8f` 外边框；号码与金额之间不得存在任何边框，避免视觉上仍呈现 16 格。

### 2026-08-19：号码详情空占位格被全局样式拉高

- 问题：空号码对复用了通用 `.empty` 类，被页面已有的 `.empty, .generic` 规则附加 `margin: 12px` 和 `padding: 40px`，导致 32px 数据行里的空格实际高达 82px。
- 正确做法：详情表格使用作用域明确的 `.is-empty` 状态类，仅切换空格背景，不继承页面级空状态布局。
- 防复发检查：3 个号码的详情行必须高 32px，8 个 pair 都高 32px；前三对为浅蓝背景，后五对为浅灰背景且显示 `-- / --`。

### 2026-08-19：号码详情汇总标题和表格线视觉偏弱

- 问题：汇总标题使用了自绘字母 `i` 和 14px 普通字重，表格边框颜色偏浅，与参考站不一致。
- 正确做法：使用 Ant Design 信息图标，标题同步为 16px、500 字重、22.4px 行高；表格使用参考站实测的 `#5f5f5f` 1px 边框，表头高度 33px。
- 防复发检查：详情标题应显示蓝色信息图标；标题 computed style 为 `16px/500`，表格外框线清晰且不拆分号码金额组合。

### 2026-08-19：快速识别结果行结构与参考站不一致

- 问题：删除和新增按钮被放在同一容器，编号没有位于两者之间；详情按钮在行尾，彩种没有黑底白字标识，主行和汇总行高度也偏大。
- 正确做法：结果主行固定为 `22px 50px 22px 120px 40px 1fr` 六列，依次渲染删除、编号、新增、详情、彩种和原文；原文使用 23px 高可编辑 textarea，汇总行同步为 23px。
- 防复发检查：输入 `福体百2345678十2345689个0134579各7合计4802` 后，DOM 顺序必须为减号、1、加号、详、福体、原文；解析结果为 686 笔、4802 元，详情体/福各 343 个号码。

### 2026-08-19：新增行错误复制上一条投注

- 问题：点击加号时直接复制当前 `QuickEntryLine`，导致新行携带原文、彩种、笔数和金额。
- 正确做法：新增专用 `status: "new"` 空行，插入当前行下方；使用空原文、空号码、0 金额/笔数，并渲染蓝底“新”标识，隐藏汇总详情。
- 防复发检查：点击第 1 行加号后，结果区应为原成功行 + 一条 23px 高的蓝色空白“新”行，原行内容和顶部汇总不变。

### 2026-08-19：结果行编辑未同步多行输入

- 问题：结果区 textarea 只是局部展示，编辑已有行不会回写上方多行文本；新增行输入也不会进入生成请求，立即识别开关无法覆盖结果行编辑。
- 正确做法：结果行变化统一回写 `text`，结构变化只同步文本不触发识别；文字变化由“立即识别”开关控制延迟解析。手动生成时整段多行文本一次性提交解析。
- 防复发检查：已有行改写后上方对应行同步变化；新增行输入后上方多行文本追加一行；开启立即识别自动得到成功/失败结果，关闭时保持“新”直到点击生成。

### 2026-08-19：投注记录筛选栏与参考站尺寸不一致

- 问题：字段组被 flex 压缩，日期使用浏览器本地化格式，按钮高度和颜色偏差，中奖选项文案也与参考站不同。
- 正确做法：锁定字段组 88/370/398px 和 50px 高度；使用日期时间选择器显示 `YYYY-MM-DD HH:mm:ss`；按钮固定为 68×28px 与 96×28px；选项统一为全部、仅中奖、未中奖。
- 防复发检查：1470px 视口下所有控件 computed size 应与参考站一致，点击日期框任意位置能打开选择器，筛选栏与表格间距为 7px。

### 2026-08-19：业务空状态使用自绘 Inbox 图标

- 问题：多个页面通过 `InboxOutlined` 和自定义“暂无数据”容器绘制空状态，未使用 Ant Design 官方空状态组件。
- 正确做法：所有业务空数据分支统一渲染 `<Empty image={Empty.PRESENTED_IMAGE_SIMPLE} />`；请求加载中仍保留“加载中...”文本。
- 防复发检查：投注记录、停押降水、下注明细、开奖号码页面均应存在 `.ant-empty .ant-empty-image`，且页面不再存在 `.anticon-inbox` 空状态图标。

### 2026-08-19：站点域名被错误建模为单值

- 问题：代理中心把代理端域名和用户端域名各自写成一个输入框，API 也只读取和更新每类第一条，无法支持同一站点的多个访问域名。
- 正确做法：两类域名都使用动态列表，支持新增、删除和设置唯一主域名；后端统一归一化、校验全局唯一并在事务中批量保存，同时保留旧单域名字段兼容。
- 防复发检查：编辑任一站点时两组域名必须完整回显；每组只能选中一个主域名；保存后重新打开数量和值必须一致，列表仅显示主域名并以 `+N` 标记其余域名。

### 2026-08-19：代理端彩票展示没有完整动态化

- 问题：代理端虽然首次请求了站点彩票接口，但只请求一次，顶部期号和倒计时仍是写死数据，平台修改分配后必须重新进入页面才会更新。
- 正确做法：服务端严格按代理会话中的 `tenant_id + site_id + site_lotteries` 返回已启用彩票，并附带最新期号；代理端首次加载、窗口聚焦和每分钟静默刷新，期号与倒计时全部使用接口数据。
- 防复发检查：站点只分配排列三时，`/agent/lotteries` 和代理端顶部都只能出现排列三；取消分配后重新聚焦页面或等待一分钟必须自动消失，前端不得存在固定彩种列表和固定期号。

### 2026-08-19：代理端彩种栏没有当前选中态

- 问题：动态彩种都铺在统一绿色背景上，用户无法判断当前正在查看哪个彩种。
- 正确做法：每个动态彩种使用独立可点击按钮；未选中为白底绿色字，只有当前选中项为绿色底白字，并在会话内记住选择。
- 防复发检查：任意时刻只能有一个 `.agent-lottery-item.selected`；点击另一彩种时绿色背景必须转移，刷新后保留选择，当前彩种被取消分配时自动选择剩余第一项。

### 2026-08-19：彩票权限配置放错业务入口

- 问题：把“分配到站点”放在彩票编辑抽屉中，操作方向是彩票选择站点，不符合站点管理员配置本站资源的业务习惯；原复选框组取消后还出现视觉状态与提交数组不同步。
- 正确做法：彩票列表只维护全平台彩票资料；在代理中心新增/编辑站点时选择该站点可见彩票，站点保存事务同时覆盖 `site_lotteries`，空数组明确表示该站点不显示任何彩票。
- 防复发检查：彩票编辑页不得再出现站点分配；站点编辑页必须完整回显可见彩票。取消福彩3D、保留排列三后重新打开仍保持该状态，代理端和用户端接口只能返回排列三。

### 2026-08-19：代理端彩种块字段顺序和状态不符合参考站

- 问题：彩种块上排显示“彩种 / 即将开盘”，下排显示“期号 / 倒计时”，状态还是固定文案，无法准确表达当前开奖阶段。
- 正确做法：上排固定为“彩种名称 / 下期期号”，下排固定为“开盘状态 / 距 `next_open_time` 的实时倒计时”；开奖日 00:00 至开奖前显示“开盘中”，开奖日前显示“未开盘”，开奖时刻后显示“已封盘”。
- 防复发检查：每个彩种按钮的子节点顺序必须是 `b / strong / span / em`；连续观察两秒倒计时应减少两秒，选中彩种仍保持绿色背景且未选中项保持白底。

### 2026-08-19：代理端彩票请求失败不应打扰用户

- 问题：代理端彩票接口短暂失败时弹出“彩票列表加载失败”，给用户造成系统异常感知；后台静默刷新失败还不应覆盖已有数据显示。
- 正确做法：首次加载失败只显示统一空状态，静默刷新失败保持当前彩票列表，不弹 Toast 或错误对话框；401 仍交由全局登录拦截器处理。
- 防复发检查：代理端源码不得出现“彩票列表加载失败”提示；断网/接口超时期间已显示的彩种不能被清空，恢复后自动刷新。

### 2026-08-19：不能按用户名排除代理下的同名会员

- 问题：代理账号和站点会员允许同名；仅因 `site_users.username` 与当前代理用户名一致就将其排除，会误删真实下级的列表展示和信用额度统计。
- 正确做法：代理身份来自独立的管理员表，会员列表按当前 `site_id` 查询 `site_users` 的全部有效会员；不同身份不能通过用户名相同推断为重复数据。
- 防复发检查：代理 `zhang10867779` 下存在同名会员 `zhang10867779` 时，会员仍必须显示并计入总数；站点隔离只依据 `site_id`。

### 2026-08-19：下级管理操作按钮容易挤压

- 问题：操作链接直接平铺在固定表格单元格中，并继承自动断词样式，窄内容区会出现按钮文字换行和相互挤压。
- 正确做法：操作列使用独立的横向 `flex` 按钮组、固定间距和禁止换行；表格设置合理最小宽度，由外层容器承担横向滚动。
- 防复发检查：操作列“修改 / 查看月报 / 查看”必须保持单行且互不重叠，按钮组 `scrollWidth` 不得大于自身可见宽度。

### 2026-08-19：下级管理筛选栏不能凭截图估算尺寸

- 问题：首版筛选栏使用 `fieldset` 和浏览器默认表单尺寸，输入框、按钮高度、按钮宽度及字段间距均大于参考站。
- 正确做法：读取参考站实际 computed style 后实现独立浮动标签字段，字段框固定为 `170x50 / 170x50 / 110x50`，输入框 `150x24`，搜索按钮 `80x26`，新增按钮 `86x26`。
- 防复发检查：1470px 视口下字段框横坐标应为 `402 / 582 / 762`，搜索和新增按钮横坐标应为 `887 / 977`；筛选容器宽 `1450px`、高 `70px`。

### 2026-08-19：面包屑不能用文本符号代替参考站图标

- 问题：首版使用文本 `»`、粗体下划线和满高按钮，造成路径间距、字重、图标宽度及右侧页签尺寸都与参考站不同。
- 正确做法：使用 Ant Design `DoubleRightOutlined`，容器固定高 `36px`、水平内边距 `25px`；路径文字高 `26px`，右侧页签固定 `72x26px` 并使用白色虚线分隔。
- 防复发检查：1470px 视口下面包屑左侧起点为 `25px`，双右箭头为 `14x14px` 且左右各 `6px`；右侧两个页签从 `1284px` 开始并在 `1445px` 结束。

### 2026-08-19：新增下级不能只做会员基础弹窗

- 问题：参考站的“新增下级”是独立页面，并为当前站点每个彩种分别配置“权限”和“下注”；只保存账号基础资料会缺失关键授权边界。
- 正确做法：使用独立新增/编辑路由，动态读取站点彩票并保存 `user_lottery_permissions`；创建和权限写入同一事务，详情页完整回显。
- 防复发检查：每个站点可见彩种必须对应两个开关；关闭“权限”必须自动关闭并禁用“下注”。用户端彩票接口过滤 `can_view`，下注接口必须再次校验 `can_bet`，不能只依赖前端隐藏。

### 2026-08-19：权限详情查询不要使用 `column('*', key)`

- 问题：ThinkPHP 对 `column('*','lottery_id')` 的行为不可靠，可能导致会员详情接口失败并让前端误用默认权限。
- 正确做法：明确查询 `lottery_id,can_view,can_bet`，再在 PHP 中按 `lottery_id` 建立映射；接口失败时前端显示错误，不提交默认值。
- 防复发检查：打开已有会员编辑页时必须收到真实会员详情；旧会员无权限记录时兼容为全部开启，保存后必须以数据库权限记录为准。

### 2026-08-19：创建账号页面不能复用修改账号页面

- 问题：修改账号需要展示会员级状态、财务额度、拦货、离线赚水和赔率覆盖，继续复用新增页面会丢失字段并错误显示创建态。
- 正确做法：创建使用 `/subordinates/new` 的 `SubordinateFormPage`；修改使用 `/subordinates/:id/edit` 的独立 `SubordinateEditPage`，账号名只读并从详情接口回显。
- 防复发检查：进入真实会员修改页必须看到“修改账号”面包屑、账号状态、信用额度、会员赔率设置；新增页不得出现修改账号的历史值。

### 2026-08-19：代理端长表单不能隐藏纵向溢出

- 问题：代理主题 `.agent-app` 使用 `overflow:hidden`，会员赔率表虽然渲染完整但页面底部提交/返回按钮不可达。
- 正确做法：代理主容器使用纵向自动滚动，赔率表自身继续承担横向滚动；长修改页不改变页面内容宽度。
- 防复发检查：修改页 `agent-app.scrollHeight > clientHeight` 时可滚动到操作区，提交/返回按钮必须在视口内可见且无横向溢出。

### 2026-08-19：修改账号赔率表误把基础赔率做成可编辑输入框

- 问题：参考站只有“离线赚水”允许会员级调整，最小下注、赔率上限、单注上限、单项上限和赔率均为只读展示；本地首版把全部字段渲染成数字输入框，且内容区过宽。
- 正确做法：修改页内容宽度固定为参考站的 1045px；基础赔率使用纯文本，只有离线赚水使用下拉框；数值显示去掉无意义的尾随零，表头采用参考站棕色样式。
- 防复发检查：赔率表 `input` 数量必须为 0；每一行仅有一个离线赚水 `select`，参考页 52 行则应为 52 个下拉框。

### 2026-08-19：信用额度区域宽度和字号与参考站不一致

- 问题：修改页主内容被收窄为 1045px，信用额度区域铺满内容且归零按钮使用带空格文案，导致输入框、统计文字和按钮位置/字号偏差，按钮文字换行。
- 正确做法：主内容恢复 1200px；信用额度内框固定 1062px 居中，额度输入框 86px，归零按钮 46×28px，使用 `归零` 且禁止换行，统计文字使用 14px Arial。
- 防复发检查：信用额度内框宽度为主内容减 138px，按钮文字必须保持一行，截图对比时不得用全宽 fieldset 代替参考站内框。

### 2026-08-19：拦货占成和提交操作区高度/位置偏差

- 问题：拦货区域提示文字没有参考站的黄色胶囊样式，平均离线赚水区域被 fieldset 默认高度撑大，提交/返回按钮下沉且不在参考站同行位置。
- 正确做法：拦货提示固定 780×24px 黄色圆角条，内框按内容宽度留白；平均离线赚水改为 30px 单行区域，提交/返回按钮同一行并使用 72×28px。
- 防复发检查：平均区域高度必须为 30px，两个操作按钮 top 相同且无第二组底部按钮；提示条不得横向溢出。

### 2026-08-19：修改账号主容器宽度未按参考站固定

- 问题：主容器在不同轮次被调整为 1045/1200px，导致账号资料、拦货、平均赚水和赔率表整体左右边界与参考截图不一致。
- 正确做法：以最新参考截图为准，修改页主容器固定为 1200px；信用额度和拦货内框使用主容器减 138px，约 1062px；平均赚水和赔率表使用主容器全宽。
- 防复发检查：在 1500px 视口主容器左右边界应约为 x=140 / 1340，页面不得横向溢出。

### 2026-08-19：只调整外层宽度不足以复刻修改账号页

- 问题：虽然主容器宽度相同，本地仍使用带边框的网格和居中 fieldset，参考站则是无边框白色分区、独立全宽外层与左对齐内框，导致视觉明显不同。
- 正确做法：同时对比两页 computed style 和子节点坐标，按参考站分区结构实现；摘要、资料、密码、额度、拦货、操作行和表头分别固定高度和间距，不能只调整 `.subordinate-edit-shell`。
- 防复发检查：1500px 视口下各分区 top/bottom 应依次为 `236-280 / 300-388 / 398-529 / 539-617 / 624-749 / 769-799 / 819-859`，且无控制台错误或横向溢出。

### 2026-08-19：会员赔率表错误重复彩种并多出赔率项列

- 问题：本地将排列三和福彩3D各渲染一份相同玩法，并使用“类别 + 赔率项”的8列表格；参考站只有一套共享玩法，类别使用独立分组行，表头为7列。
- 正确做法：以第一套同玩法赔率作为展示行，修改离线赚水时通过“同”开关同步到其他彩种对应玩法；表头列宽按 `185/165/165/165/165/175/175px`，表体固定600px内部滚动。
- 防复发检查：表格必须是7个表头、6个类别分组、26个玩法、26个下拉框和0个数值输入框；定位名称显示为 `口XX / X口X / XX口` 等参考简称。

### 2026-08-19：信用额度内容被 fieldset 的 legend 向下挤出

- 问题：额度输入框、三组信用额度文字和归零按钮直接跟在 `legend` 后按普通行流排版，浏览器对 fieldset 的特殊布局会额外下推内容，导致 28px 内容行超过 50px 内框底部；父级 `place-items:center` 还使内框横向偏离参考站。
- 正确做法：将所有额度控件包进独立的 `.credit-line`，以 28px 单行在 fieldset 内精确定位；财务外层改为左对齐，1062px 内框相对 1200px 外层固定左偏移 15px。
- 防复发检查：额度内框 top/bottom 为 `553/603px` 时内容行必须为 `568/596px`，四向 overflow 均为 0；内框 left 必须等于外层 left + 15px，页面不得产生横向滚动。

### 2026-08-19：离线赚水“同”缺少全表和分类两级联动

- 问题：原表头“同”只同步两个彩种中名称相同的隐藏赔率项，用户看到的 26 个玩法并不会一起变化；各分类行也没有独立的同步控制。
- 正确做法：联动优先级固定为“全表同 > 分类同 > 当前玩法”。表头勾选时更新全部赔率；表头取消后，分类勾选只更新相同 `category` 的赔率；均未勾选时只更新当前玩法及另一个同玩法彩种的数据。
- 防复发检查：全表同步后 26 个可见下拉框值唯一；首个 3 行分类同步时前三项相同且第四项不变；关闭分类同步后修改第一项不得影响第二、第三项。页面应存在 6 个分类同步复选框。

### 2026-08-19：会员离线赚水缺少明确的代理关系归属

- 问题：`user_lottery_odds` 原来只保存站点、用户和玩法，虽然能回显数值，但没有明确记录该赚水属于哪个业务代理，后续代理分成和报表无法可靠归属。
- 正确做法：增加 `agent_id` 并从站点关系回填旧数据；详情读取和事务保存同时使用 `site_id + agent_id + user_id` 隔离，记录仍按具体 `lottery_odds_id` 保存。平台自有站点的 `agent_id=0` 是合法归属，不可当成缺失数据。
- 防复发检查：所有 `user_lottery_odds.agent_id` 必须等于所属站点的 `sites.agent_id`；保存前后玩法 ID/赚水值校验值一致，重新打开页面 26 个可见值完整回显；服务端拒绝小于 0 或大于 0.1 的赚水值。

### 2026-08-19：管理端赔率按文本类别平铺，无法树状维护且数值被迫填 0

- 问题：`lottery_odds` 每行重复保存 `category` 文本，管理端使用分页平铺表；新增/编辑接口把所有限额和赔率强制当数字处理，空值无法表达“未设置”。
- 正确做法：新增 `lottery_odds_categories` 父表，玩法通过 `category_id` 归属；迁移按原类别自动建父节点并回填 53 条玩法；管理端使用默认展开树表，类别可新增/编辑/删除，类别行可直接新增玩法；六个赔率字段均支持空字符串并持久化为 `NULL`。
- 防复发检查：赔率接口返回 `categories[].children[]` 且无 `category_id` 为空的玩法；管理端页面类别行显示“新增玩法”，玩法编辑表单所有数值 placeholder 为“可留空”；服务端接受 `NULL`/空字符串，拒绝负数和超过 0.1 的离线赚水。

### 2026-08-19：赔率树默认展开且缺少分页

- 问题：树表使用 `default-expand-all`，进入页面会一次性展开所有玩法；树形接口没有分页，类别较多时页面过长。
- 正确做法：接口按类别分页，单个类别的全部玩法作为同一树节点返回；前端取消默认展开并保留 Element Plus 分页器，分页总数使用类别总数。
- 防复发检查：首次打开每个类别节点均为 `Expand this row`，玩法行不可见；分页器显示 `Total <类别数>`，切换页码后只替换类别节点，不拆分类别内部玩法。

### 2026-08-19：赔率树强制类别下必须存在玩法子节点

- 问题：部分彩种的“类别”本身就是可下注玩法，原模型只能创建一个无意义的子玩法才能保存赔率。
- 正确做法：类别表增加 `is_playable` 和六个可空赔率字段；玩法类别作为树叶节点保存，普通类别继续通过 `children` 管理玩法；玩法统计将玩法类别计为 1 项。
- 防复发检查：新增/编辑类别表单可切换“类别本身就是玩法”；开启后显示可空赔率字段且树节点不显示展开按钮；关闭后仍可在类别行新增玩法。

### 2026-08-19：三位彩种缺少组六胆拖及全包玩法

- 问题：福彩3D和排列三的赔率目录缺少用户提供的“组六2胆拖”“单选全胆拖”玩法，且已有组六六码/组六七码赔率与新配置不一致。
- 正确做法：迁移 `202608190017` 对 `fc3d`、`pl3` 两个彩种按类别和玩法名称幂等 upsert；“组六赖六码/组六赖七码”按现有目录规范归一为“组六六码/组六七码”，避免产生错别字重复玩法。
- 防复发检查：两个彩种均应存在 16 条本次配置记录，组六六码赔率为 1.2931、组六七码为 1.2605；2码拖2至拖8 和 1码拖2至拖8 均完整存在。

### 2026-08-19：赔率树分页切换仍返回第一页

- 问题：管理端虽然传递了 `page` 和 `page_size`，赔率接口却始终查询全部类别，分页器切换后页面数据不变。
- 正确做法：服务端按类别分页查询，类别下的玩法作为完整 `children` 一并返回，并返回 `category_total/page/page_size` 供分页器使用。
- 防复发检查：将每页数量切换为 5 后，第二页应显示后续类别（包括“组六2胆拖”“单选全胆拖”），不得重复第一页类别。

### 2026-08-19：赔率目录与参考站玩法不一致

- 问题：此前只写入用户粘贴的少量玩法，遗漏参考站中的定位、复式、跨度、和值、组三/组六赖及多个胆拖分组。
- 正确做法：先清空 `lottery_odds_categories`、`lottery_odds` 及会员赔率覆盖，再按参考站实际页面采集结果重建；分组节点和“类别本身就是玩法”的节点分别保存。
- 防复发检查：福彩3D、排列三均应有 22 个类别节点和 106 个可下注玩法；管理端赔率页应显示定位、组三多码、组六多码、复式多码、跨度、和值、赖、胆拖等真实类别。

### 2026-08-19：直接玩法类别只显示名称不显示赔率

- 问题：参考站的“三码定位、独胆、双飞、对子、组六、组三”等是类别本身就是玩法；管理端模板只对 `node_type=play` 渲染数值，导致这些行的赔率列为空。
- 正确做法：`is_playable` 类别节点与普通玩法节点使用同一组赔率字段展示逻辑，空值继续显示“未设置”。
- 防复发检查：管理端切换到包含这些节点的分页后，应分别看到 `0.1/900/600/1500/900`、`0.1/3.3/40000/200000/3.3` 等参考站数值。

### 2026-08-19：参考站两个全包玩法位置不对

- 问题：导入时直接玩法统一追加到列表末尾，导致“豹子全包”没有紧跟和值，“对子全包”没有紧跟组六赖。
- 正确做法：持久化排序设置为和值 `63`、豹子全包 `64`、组六赖 `86`、对子全包 `87`。
- 防复发检查：两个彩种的类别顺序均应为“和值 → 豹子全包”和“组六赖 → 对子全包”。

### 2026-08-19：直接玩法未出现在代理端会员赔率表

- 问题：管理端直接玩法保存在 `lottery_odds_categories`，代理端接口只读取 `lottery_odds` 子玩法，因此三码定位、独胆、双飞、对子、组六、组三缺失。
- 正确做法：代理接口将 `is_playable=1` 类别转成稳定虚拟 ID 的赔率行，纳入展示、离线赚水同步和保存校验；移除过时的“三码定位”显示别名。
- 防复发检查：代理端修改账号页应出现六行并显示 `0.1/900/600/1500/900`、`0.1/3.3/40000/200000/3.3`、`0.1/16/10000/40000/16`、`30`、`0.1/150/10000/15000/150`、`300`。

### 2026-08-19：直接玩法被渲染成同名分类且全包玩法层级错误

- 问题：代理端按 `category` 无条件生成分类标题，导致三码定位等直接玩法出现同名分类行；豹子全包、对子全包只调整了同级排序，没有真正挂到父分类。
- 正确做法：直接玩法行携带 `direct_category` 并跳过分类标题；豹子全包迁入和值，排在和值子玩法末尾；对子全包迁入组六赖，排在组六赖子玩法末尾；前端按接口全局顺序构建分组。
- 防复发检查：代理表中不得出现“三码定位 同”等标题；索引顺序应为和值标题 < 和值0-27 < 豹子全包，组六赖标题 < 组六赖一码 < 组六赖七码 < 对子全包。

### 2026-08-19：赔率表直接玩法顺序和配色未对齐参考站

- 问题：直接玩法被统一追加到表格末尾，并且所有玩法行均为白底黑字，缺少参考站的业务分组配色。
- 正确做法：重排全局 sort，使二码定位后为三码定位、独胆、双飞、对子、组六、组三、组三多码；直接玩法名称和赔率上限使用蓝色，双飞/对子为 `rgb(212,246,246)`，组六/组三为 `rgb(255,248,191)`，分类行为 `rgb(212,246,228)`，边框使用 `rgb(143,143,143)`。
- 防复发检查：浏览器 computed style 必须与上述色值一致，位置索引严格满足 `X口口 < 三码定位 < 独胆 < 双飞 < 对子 < 组六 < 组三 < 组三多码 < 组三两码`。

### 2026-08-19：离线赚水全局“同”默认被勾选

- 问题：页面初始化将全局同步状态设为 `true`，用户修改任一离线赚水时会默认影响全部玩法。
- 正确做法：全局“同”和分类“同”均默认未勾选，只有用户主动开启后才执行批量同步。
- 防复发检查：刷新修改账号页后，“全部离线赚水同步”复选框的 `checked` 必须为 `false`。

### 2026-08-20：筛选栏外框坐标一致不代表内部控件已复刻

- 问题：总货概览筛选栏外层宽高与参考站一致，但内部使用原生 `fieldset/legend`，导致标签压在边框上的方式、控件纵向位置、输入框圆角、下拉框高度和提交按钮文字仍有明显差异。
- 正确做法：同时测量参考站每个子节点及其父容器；使用普通相对定位容器和绝对定位浮动 label，按参考站设置子容器 `170/110/110 × 50px`、顶部外边距 8px、输入框 `150×24px`、下拉框 `70×20px` 和按钮 `50×28px`。
- 防复发检查：1567px 视口下三个子容器必须分别位于 `x=578.5/758.5/878.5, y=247`；输入框位于 `588.5/263.5`，下拉框位于 `768.5/265.5`，按钮位于 `913.5/261.5`；文案必须为带间隔的“提 交”。

### 2026-08-20：彩票状态条只对齐内容，遗漏选中项底部虚线

- 问题：彩票名称、期号、状态和倒计时的位置已接近参考站，但状态条外框仍少 2px 绿色边框，选中项底部也缺少参考站的 1px 白色虚线。
- 正确做法：状态条使用 `50px` 高度和四周 `2px solid #006400`；彩票项在内层保持 `200×46px`，默认使用透明虚线占位，选中项将底部虚线切换为白色。
- 防复发检查：1567px 视口下状态条必须为 `x=0/y=130/1567×50px`；选中项为 `x=2/y=132/200×46px`，计算样式 `border-bottom: 1px dashed rgb(255, 255, 255)`。

### 2026-08-20：补底部虚线时遗漏彩种交界处的白线

- 问题：选中彩种改为绿色背景后，其右边缘直接衔接下一项白色背景，截图中形成一条明显的竖向白线。
- 正确做法：在选中彩种右侧保留 `1px solid #006400` 绿色边界，只让底部使用白色虚线。
- 防复发检查：选中项计算样式必须同时满足 `border-right: 1px solid rgb(0, 100, 0)` 与 `border-bottom: 1px dashed rgb(255, 255, 255)`，交界处不得出现白色竖线。

### 2026-08-20：仅修改选中项右边框仍无法消除交界白线

- 问题：上一轮将白线误判为选中项右边缘，但真正原因是下一彩票项的 `1px dashed transparent` 左边框；透明边框仍占宽度，并透出状态条的白色背景。
- 正确做法：未选中彩票项不设置左边框，使自身背景从盒模型左边缘开始绘制；选中项保留绿色右边框，并以 1px 绿色外阴影覆盖浏览器绘制接缝。
- 防复发检查：第二个彩票项的计算样式必须为 `border-left: 0px none`，选中项必须为 `box-shadow: 1px 0 0 rgb(0, 100, 0)`；截图交界像素在进入白底前必须保持绿色，不能只检查 CSS 声明。

### 2026-08-20：将横向白缝误判为彩种之间的竖向接缝

- 问题：用户指出的白线实际位于彩票状态条与下方绿色导航之间；`.overview-page` 的 `padding-top: 2px` 让两个区域之间露出灰白背景。
- 正确做法：概览页顶部内边距设为 0，使彩票状态条底部 `y=180` 与位置导航顶部 `y=180` 直接贴合。
- 防复发检查：同时测量上下两个区域，必须满足 `location.top - lotteryStrip.bottom = 0`；不能只检查彩票项左右边框。

### 2026-08-23：快速录入物理换行被误判为独立订单

- 问题：物理换行不代表独立订单；同一合并订单的前置行错误显示了“详”、笔数和金额，导致用户误以为每一行都是独立订单。
- 正确做法：解析结果必须携带批次元数据并区分批次角色。同一合并订单只有末行显示“合/详”以及整批笔数、金额，前置物理行不得显示详、笔数或金额，并使用不同背景明确区分前置行与末行。
- 金额守恒：重复 `668` 的场景共有 429 个唯一码、430 次投注、总金额 25800。详情金额必须按号码聚合；`668` 应聚合两次投注金额，其他号码各聚合一次，详情总额必须等于 25800，唯一码数量不得替代实际投注次数。
- 拦货 occurrence：拦货分配不得先按唯一码去重；每次投注必须保留独立 occurrence。该场景普通号码的 occurrence 金额为 60，重复号码 `668` 的两次 occurrence 聚合后必须为 120。
- 防复发检查：覆盖单行与多行合并批次的角色和背景、前置行字段隐藏、末行整批汇总、唯一码数与投注次数分离、重复号码详情金额聚合、拦货 occurrence 的 60/120 关系及详情总额守恒；浏览器和自动化验证均不得执行真实下注。

### 2026-08-23：旧 success 高优先级样式将批次白线覆盖为绿色

- 问题：结果行旧有的 `success` 高优先级样式覆盖了批次行的白色实线，源码中虽然声明了白线，浏览器最终计算样式仍显示为绿色。
- 正确做法：批次角色样式必须以足够明确的选择器覆盖旧成功态，只调整批次分隔线，不破坏成功行的其他状态样式。
- 防复发检查：不能只检查 CSS 声明；必须在浏览器读取目标行 computed style，确认分隔边框为白色实线，并结合截图检查实际渲染像素未被旧 `success` 规则覆盖。

### 2026-08-23：为对齐参考站误将结果表改为代码等宽字体

- 问题：为了让数字列看起来整齐而改用代码等宽字体，虽然字符宽度一致，却偏离参考包的真实字体、字重和整体视觉。
- 正确做法：结果表保持参考包真实字体栈 `Microsoft YaHei, Tahoma, Helvetica, Arial` 和字重 400；数字对齐继续保留 `tnum`，不得用等宽代码字体替代参考字体。
- 防复发检查：浏览器 computed style 必须确认 `font-family` 命中参考字体栈、`font-weight: 400`，并保留 tabular numbers/`tnum`；不能仅凭截图或源码声明判断字体已对齐。

### 2026-08-23：下注事务闭包漏捕获总注数变量

- 问题：启用 `bet_submissions` 聚合提交路径时，事务闭包内部写入 `bet_count` 使用了外层 `$count`，但 `use` 列表遗漏该变量，运行时记录 `Undefined variable $count` 并导致提交失败。
- 正确做法：将 `$count` 明确捕获到闭包；保留事务内重复提交、余额和明细写入逻辑不变。
- 防复发检查：静态检查事务闭包 `use` 列表与内部变量引用；运行 `php -l server/app/controller/UserBusiness.php`，使用回归夹具验证聚合路径，禁止通过真实下注验证。

### 2026-08-23：批次尾缀和混合输入必须动态识别

- 问题：批次解析只尝试把整段输入当成一个批次，尾缀使用“直选”别名或同一粘贴包含多个批次/普通行时会回退通用解析并合并成一行；结果行本地编辑、新增或删除后还会保留旧 `batch_end`/汇总元数据。
- 正确做法：只以明确的 `N注 + 玩法 + 金额` 尾缀作为批次标记，扫描每个连续物理片段；尾缀先执行统一别名/标点归一化，支持尾缀独立一行和多个批次；没有标记时不虚构批次。结果行发生结构或文本变化时清除 `batch_*` 派生字段，等待下一次服务端识别。
- 防复发检查：覆盖逗号/空格/顿号、尾缀独立行、“直选”别名、多个批次与普通行混合、无尾缀独立失败行、旧注数 mismatch，以及结果行编辑/新增/删除后的无陈旧汇总；运行 `php server/tests/QuickEntryParserTest.php`、用户端 `npm run build`，不执行真实下注。

### 2026-08-23：组选多码号码明细不能由展示层临时恢复

- 问题：上一轮保留 `bet_details.number_text='000'`，再由下注明细接口从原始文本恢复为 `15862`；这只是选号集合，不是实际的 10 个组六组合，也让前端承担了本应由下注保存模型保证的数据语义。
- 正确做法：组选多码必须在解析/保存时将实际组合号码持久化；下注明细页只读取已保存号码。历史 `000` 记录只能通过版本化迁移按可验证规则回填，不可继续依赖页面临时拆分。原父记录金额、状态、中奖金额和盈亏总额必须守恒。
- 防复发检查：`15862福体组六各108` 对每个彩种应持久化并展示 `158/156/152/186/182/162/586/582/562/862`；每组金额合计 108，历史命中 `862` 时中奖合计仍为 1620；API 返回稳定明细组标识，前端不得包含从选号集合生成组合的业务逻辑；全程禁止用真实下注验证。

### 2026-08-24：赚水必须独立记账且三处口径一致

- 问题：赔率扣水已经影响实际赔率，但 `bet_details.offline_rebate`、用户账单和代理分类账仍返回 0，且没有独立赚水流水，导致用户端、代理端和结算记录无法对账。
- 正确做法：结算时按实际明细金额乘锁定的 `user_stop_drops.drop_odds` 写入 `organization_water_ledger`；该流水只用于赚水查询，不直接修改组织余额。用户端明细、账单和代理分类账都使用同一公式并保留独立字段，不把它混入普通回水或占成。
- 防复发检查：同一明细重复结算不得重复流水；`amount × drop_odds` 与流水、用户端合计、代理端分类账逐分一致；无扣水时不写流水；赚水写入失败必须回滚整次结算；组织、站点和会员数据范围必须隔离；禁止通过真实下注验证。

### 2026-08-30：代理拦货容量与暗水/明水口径调整

- 问题：拦货容量原按号码分别计池，同一彩种/玩法下多个号码可合计超过配置额度；代理占成金、暗水和明水也未按业务约定统计。
- 正确做法：容量按“彩种 + 玩法 + 期号 + 代理（或平台）”建立总池；用户下注超过额度时只截留剩余额度，剩余继续向上级分配。每个代理不论实际拦货是否满额，都按整笔投注金额计暗水；占成金按会员盈亏乘自身占成，明水按占成金乘站点明水比例。
- 防复发检查：同一玩法多号码合计拦货不得超过额度；10000 分额度对 6000+6000 两笔只拦 10000；站点暗水 0.085、明水 0.012 时，暗水=下注额×0.085、明水=占成金×0.012；跨代理、跨期号、退款释放容量均保持隔离。

### 2026-08-31：离线反水按线路总投计算

- 问题：部分代理查询仍按玩法 `drop_odds` 计算离线反水，且中奖时会出现按中奖金额反推的旧口径。
- 正确做法：离线反水统一为站点配置的离线反水（明水）比例 × 当前代理线路下所有用户总投；默认比例 0.085。该金额与实际拦货多少无关，按线路下注总额统计。
- 防复发检查：同一代理线路两名用户分别下注 6000、4000 时，站点比例 0.085 的离线反水必须为 850；修改玩法赔率扣水或拦货容量不得改变该值；代理报表、分类账、总货明细三处合计一致。

### 2026-08-24：登录地点不能只停留在会话临时字段

- 问题：`account_sessions.location` 虽然已有城市级尝试，但各账号表没有统一保存最后登录地点，代理子账号和部分会员列表也没有返回该字段；现有 IP 服务无法保证村级精度。
- 正确做法：登录成功时将 IP 和解析地点同时写入账号表与会话表；地理服务必须可配置，按国家、省、市、区县、镇/街道、村的顺序提取服务实际返回的最高精度，解析失败只显示公网地址/内网地址，不阻断认证。
- 防复发检查：覆盖平台管理员、站点管理员、组织账号、代理子账号和会员；检查登录后账号表与最新会话地点一致、跨站点列表不越权；明确说明 IP 定位不能承诺村级准确度，只有配置支持村级字段的服务时才展示村级结果。

### 2026-08-24：组织层级下钻不能只依赖前端传入节点

- 问题：下级管理原接口只支持当前账号的直属下级；若直接增加 `organization_id` 参数但不校验组织路径，用户可构造参数读取上级、兄弟分支或其他站点数据。
- 正确做法：后端以登录账号绑定组织为权限根，只允许目标节点位于该根的后代集合中；每次仅返回目标节点直属组织，代理节点仅返回直属会员。进入后代节点为只读，新增、修改、删除继续只允许登录账号自己的直属下级。
- 防复发检查：覆盖正常逐级进入、面包屑范围、非直属数据不混入、通过参数访问上级被拒绝、代理会员只属于当前代理；前端隐藏后代节点管理操作不能替代后端鉴权。

### 2026-08-24：站点后台管理员不能隐式成为根总监

- 问题：`site_admins` 既能登录站点后台，又会被代理端登录回退逻辑自动映射到站点根总监；当前数据中甚至存在与根总监组织账号完全同名的站点管理员，形成两套账号控制同一业务层级。
- 正确做法：站点后台管理员仅使用管理端入口；代理中心的正常根账号只认 `organization_accounts`。总平台需要进入站点时创建带操作人标记的临时代入会话，并绑定根总监组织账号，不复用站点管理员凭据。
- 防复发检查：纯站点后台账号登录代理端必须失败、登录站点后台必须成功；旧 `site_admins/sites` 代理会话必须失效；平台代入会话必须标记 `impersonation=1` 并绑定根总监；新建或改名的站点后台账号不得与同站点组织账号重名。
### 2026-08-25：数字+玩法订单不能按主单编辑

- 问题：旧批量替换页面按单个三位数字明细选择，无法处理“号码 + 玩法”结构，也无法一次修改同一提交批次下的多彩种主单。
- 正确做法：以主单为选择单位，详情抽屉内编辑结构化玩法明细；批量操作先预览，再由后端事务校验状态、更新明细并重新汇总主单金额/笔数。
- 防复发检查：普通编辑和批量编辑均拒绝已开奖、已封盘、已退码记录；同一 submission_id 的兄弟彩种记录必须同时纳入批量范围；修改后检查 bet_details、user_stop_drops 与 bet_records 汇总一致。

### 2026-08-25：主单批量修改页面为空

- 问题：管理端页面请求被 Nginx 反代到旧系统的 5095 服务；旧服务使用另一套数据库，没有当前项目的投注表，因此批量主单接口返回空/错误。
- 正确做法：当前项目 API 独立运行在 18082，并将管理端、用户端、代理端 API 反代统一指向 18082。
- 防复发检查：发布后同时检查 `/api/v1/health`、管理端 `/admin/bet-records/batch-options` 的认证响应和当前 `kuaipaisan.bet_records` 统计；禁止再把 `kps*` 站点 API 指向旧系统端口。

### 2026-08-25：复式被错误按组六规则判定

- 问题：用户下单 `福024567 复试各1米`，期号 `2026227` 开奖 `522` 后显示未中奖；解析已正确归一为复式，但紧凑表达式和历史 `000` 占位表达式都要求开奖号包含 3 个不同数字，导致复式被错误套用组六条件。
- 正确做法：复式只校验开奖号的全部数字均包含在选号集合中，允许组三或豹子形态；组六继续要求 3 个不同数字。紧凑表达式与历史占位分支必须保持同一口径。
- 防复发检查：`复024567` 对 `522` 必须命中，对未包含选号集合的 `789` 必须不命中；`000` 历史占位使用 `024567复式六码` 时也必须命中 `522`；运行结算匹配器回归，禁止通过真实下注验证。

### 2026-08-26：站点下注时间段必须同时控制展示与业务

- 问题：仅在服务端禁止下注会让用户端仍显示预加载的下一期，且蒙版、文案和允许下注状态无法按站点时间段独立变化。
- 正确做法：站点按彩种保存时间段规则；开奖列表和期号下拉框由服务端过滤未开奖期，用户端再按规则动态计算文案、蒙版和按钮状态，最终下注接口再次校验时间段。
- 防复发检查：覆盖普通时间段、跨午夜时间段、规则间隙和无规则兼容模式；分别验证“可下注/蒙版/显示下一期”三项组合，以及预加载期在开奖列表、首页期号和期号下拉框中的一致性；禁止通过真实下注验证。

### 2026-08-26：隐藏预加载期时不能误删当前最新期

- 问题：服务端为兼容旧字段，在隐藏下一期时将 `next_code` 返回为当前最新期；用户端下拉框无条件按 `next_code` 过滤，导致最新 2026227 被误删，只显示 2026226。
- 正确做法：只有当 `next_code` 与 `latest_code` 不同时，才把 `next_code` 视为待开奖预加载期并过滤；两者相同则保留当前最新期。
- 防复发检查：以 2026227 已开奖、2026228 预加载为样本，关闭显示下一期时下拉框首项必须为 2026227，开启时才允许出现 2026228；开奖列表和其他期号选择区域保持相同规则。

### 2026-08-26：顶部彩票期号与历史期号展示必须独立

- 问题：顶部彩票列表的当前/下一期期号和开奖历史、期号下拉框共用同一个开关，无法实现“历史隐藏下一期但顶部显示下一期”等站点策略。
- 正确做法：时间段规则拆分为“历史/下拉期号”与“顶部期号”两个选择；服务端分别返回，前端 Header 只使用顶部期号选择，开奖列表和下拉框只使用历史选择。
- 防复发检查：覆盖四种组合：历史当前/顶部当前、历史当前/顶部下一期、历史下一期/顶部当前、历史下一期/顶部下一期；确认预加载期数据过滤不影响顶部期号显示。

### 2026-08-26：百十个定位复式不能把所有组合都算中奖

- 问题：`福体百0234569十0234689个1235679各1米` 展开为 343 个直选组合时，结算器只校验每一位是否属于选号集合，没有要求组合等于开奖号，导致福彩 522 和体彩 431 各把 343 注按 900 赔率全额派奖。
- 正确做法：定位复式先验证组合位于对应选号集合，再要求实际三位组合与开奖号码完全相等；保留紧凑定位表达式和历史 `000` 占位的兼容逻辑。
- 数据处理：提交 13 的两笔订单各从 308700 元更正为 900 元，合计 1800 元；会员余额、日账单、组织分账同步回滚差额，并通过 `manual_adjustment` 流水留痕。
- 防复发检查：定位复式 343 个组合在 522/431 各只命中 1 注；全库已结算定位复式扫描 2 笔，金额不一致 0 笔；禁止用真实下注验证。

### 2026-08-29：系统分分彩停机追赶导致倒计时归零

- 问题：系统彩开奖任务停机后，待开奖期停留在过去；原先每次最多追赶 10 期，无法在合理时间内追到当前时间，用户端收到过期 `next_open_time` 后倒计时显示 `00:00:00`，默认蒙版同时锁住下注区。
- 正确做法：短暂积压继续自动追赶；积压超过 100 期时快速将旧待开奖期落地并生成当前未来期，避免用户请求超时。只有存在该期投注时才执行结算，空期跳过结算查询。
- 防复发检查：系统彩必须始终存在 `is_opened=0` 且 `open_time>当前时间` 的待开奖期；用户端倒计时不得因历史积压固定为零；站点未配置禁止下注规则时 `timing_can_bet=true` 且不显示蒙版。

### 2026-08-29：会员编辑页遗漏 can_bet 开关

- 问题：代理端会员编辑页只显示“权限（可查看）”开关，没有显示“下注（can_bet）”开关；管理员即使开启了查看权限，保存后 `user_lottery_permissions.can_bet` 仍可能为 0，用户端持续蒙版且不能下注。
- 正确做法：编辑页同时显示查看和下注两个独立开关；关闭查看时强制关闭下注，开启下注时自动开启查看；后端继续以 `can_bet` 做最终权限判断。
- 防复发检查：编辑现有会员时必须能看到每个已分配彩种的“权限/下注”两个开关；保存后复查对应站点、用户和彩种的 `can_bet=1`，并通过 `/user/lotteries` 确认返回 `can_bet=true`。

### 2026-08-29：定位复式的“个”被误识别为金额

- 问题：输入 `福体百1234567十7654321个567890` 时，金额提取规则把定位标记 `个567890` 当成“每个 567890 元”，移除了个位选号，错误生成金额 `55653220` 和 98 注。
- 正确做法：金额前缀 `个` 只有在前面不是数字时才参与金额匹配；定位表达式保留百、十、个位三组选号，`7×7×6` 组合在福体下应为 588 注。未填写金额时返回“未识别到有效金额”，不虚构金额。
- 防复发检查：覆盖 `百…十…个…各1米`、无金额定位表达式和多行定位输入；确认个位选号不丢失、组合数/金额正确，禁止通过真实下注验证。

### 2026-08-29：调整系统彩间隔后期号冲突导致时间待定

- 问题：分分彩从 60 秒改为 1800 秒后，按新时间槽生成的期号可能已存在于旧 60 秒历史（例如 `2608290046`）；`insertPending` 直接返回已开奖旧记录，导致没有 `is_opened=0` 待开奖期，用户端显示“时间待定”。
- 正确做法：生成待开奖期时若目标编码已开奖，持续递增到当前彩种未占用的期号；已有未开奖记录仍原样复用。
- 防复发检查：修改系统开奖间隔后，必须同时确认存在唯一的未来待开奖期；`/user/lotteries` 返回非空 `next_open_time`，用户端不显示“时间待定”。

### 2026-08-29：多独胆输入被识别为三位直选

- 问题：`体16胆各500` 被通用数字解析成 `016` 一注，而“16”实际表示数字 1 和 6 两个独胆。
- 正确做法：新增独立的多独胆解析，按去重后的单数字分别计注、计额，并让结算按每个数字匹配开奖号码。
- 防复发检查：`体16胆各500` 必须返回 `1 6`、2 注、1000 元；福体应按两个彩种倍增；胆码拖码格式仍走原胆拖规则。

### 2026-08-29：单双组混合倍数需按号码长度计算金额

- 问题：`体895一单五组12` 原先无法识别；混合语法中的“一单”应表示 1 注三码定位，“五组”表示 5 倍组六，合计 `2 + 5×2 = 12` 元。
- 正确做法：新增单双组混合倍数解析；前置选择恰好三个数字时每倍按彩票单注金额（默认 2 元），否则每倍按 10 元；输出直选和组六两条独立结果并校验末尾总额。
- 防复发检查：覆盖中文/阿拉伯倍数、三位与非三位选号、福体彩种倍增，确认 `体895一单五组12` 返回 2 元直选 + 10 元组六，禁止真实下注验证。

### 2026-08-29：多号码单双组混合语法误把总额当号码

- 问题：`福418 403 901 406各四单一组计40` 被旧规则拆成普通直选、额外的 `040` 号码和组选，显示金额 50 元。
- 正确做法：识别号码列表后，按每个号码 4 单直选和 1 组组六计算：4×4×2=32 元，4×1×2=8 元，总计 40 元；末尾“计40”只做总额校验，不能作为号码。
- 防复发检查：覆盖“各四单一组计40”的中文/阿拉伯倍数及福体种类；结果必须只生成直选、组六两行，号码列表不得出现 `040`，禁止真实下注验证。

### 2026-08-29：中文“四十单”被拆成普通号码

- 问题：`福618 158各四十单一组` 中的“四十”被通用归一化拆成 `4十`，混合玩法未命中，随后把尾部数字当成 `004`，总额错误显示 6 元。
- 正确做法：混合单双组解析优先读取原始文本并支持中文十位数；两个三位号码各四十单、各一组应为 160 元 + 4 元 = 164 元。
- 防复发检查：覆盖“四十/40”和有无“计总额”后缀；确认不生成 `004`，福体下按彩种数量正确倍增。

### 2026-08-29：中文“四百单”不得降级成错误注单

- 问题：`福618 158各四百单一组` 无法被安全识别时会回退到通用解析，错误生成 `004` 并显示 6 元。
- 正确做法：检测中文百位倍数并直接返回明确错误，提示使用 `400单1组` 等数字格式；禁止继续生成或提交任何注单。
- 防复发检查：该输入必须是 `failed`，错误信息包含“中文百位数”，结果号码为空且不出现 `004`。

### 2026-08-29：双飞玩法需兼容“飞”简称

- 问题：解析规则只接受“双飞”，输入 `09飞各1元` 会回退为普通号码，无法按双飞玩法结算。
- 正确做法：将“飞”和“双飞”统一识别为双飞，保留原有金额、赔率和结算口径。
- 防复发检查：`09飞`、`09双飞`、带“各/每/倍/元”后缀的写法均映射为 `play_type=双飞`。

### 2026-08-29：重复双飞号码必须按对子赔率

- 问题：`77双飞` 仍按双飞处理；但两个数字相同表示对子，应使用对子赔率，只有 `37飞` 等不同数字才是双飞。
- 正确做法：解析时检测两位数字是否相同，相同则输出对子并将号码置于玩法前的结算格式；不同则输出双飞。组三/组六多码继续依据选号码数映射到对应赔率行（如组三五码、组六六码）。
- 防复发检查：`77双飞` 命中对子赔率，`37飞` 命中双飞赔率；`12345组三` 命中组三五码，`123456组六` 命中组六六码；结算匹配必须能提取号码。

### 2026-08-29：历史双飞/对子错误赔率数据清洗

- 处理：注单 233（37/77 各6000）拆分为双飞16与对子30，派奖由192000更正为276000；注单240（77各60）派奖由960更正为1800；待开奖注单246同步拆分。历史未中奖注单125、126仅修正66的赔率为对子30。
- 账务：会员补回84840元，账单和提交汇总同步修正，组织分账按原分配比例追加手工调整流水；所有更新在事务内并带目标值校验。
- 防复发检查：清洗脚本必须校验注单/期号/原金额/原派奖后才执行，重复运行应因目标值已变化而停止，禁止真实下注验证。

### 2026-08-29：组三组六多码必须按选号数量动态取赔率

- 问题：`34567组三` 被按普通组三赔率300处理，实际应匹配组三五码赔率15；`3456组六` 被按组六标准赔率150处理，实际应匹配组六四码赔率37；10位选号未映射全包。
- 正确做法：解析和赔率身份按去重选号数量动态映射两码、三码、四码……九码及十位全包；已清洗注单238、243并修正注单245赔率。
- 防复发检查：`01/012/0123/…/0123456789` 分别映射对应组三赔率；组六同理；开奖结算、用户账单和组织分账必须使用同一锁定赔率。

### 2026-08-29：历史普通组三九码误派奖清洗

- 处理：注单215 `福012345689组三1000米` 原按普通组三赔率300派奖300000元，实际应为组三九码赔率4.166，修正派奖4166元，回收295834元。
- 账务：会员Aa005余额扣回295834元（因余额不足形成负余额，保留欠款事实），账单、提交汇总及组织分账同步修正并写入手工调整流水。
- 防复发检查：多码组三/组六必须按去重选号数匹配赔率；清洗脚本校验原注单、期号、原赔率和原派奖，重复运行不得重复扣款。

### 2026-08-29：待开奖组三五码仍锁定普通赔率

- 问题：待开奖注单249 `福34567组三100米` 已识别出五位选号，但下单时仍锁定普通组三赔率300。
- 正确做法：在未开奖状态下将明细及停押记录的锁定赔率和玩法统一修正为组三五码15，下注金额保持不变。
- 防复发检查：所有 pending 多码组三/组六明细必须按去重选号数重新校验赔率身份；修正待开奖记录不得产生余额或派奖流水。

### 2026-08-29：超限金额不得静默截断下单

- 问题：复式金额超过赔率表单注/单项上限时，旧逻辑直接把实际金额截小后提交，用户输入金额与注单金额不一致且没有明确提示。
- 正确做法：检测到超限立即返回具体玩法、允许上限和当前输入金额，整单拒绝提交；不自动校正、不写入注单、不扣余额。
- 防复发检查：覆盖单注上限和单项上限两类超限；接口必须返回 422 中文错误，数据库注单和会员余额保持不变。

### 2026-08-29：无“各”后缀的多码组三无法识别

- 问题：`福34组三100米` 未带“各/每”时无法命中目录规则，错误提示“未识别到有效玩法”。
- 正确做法：支持紧凑的“选号+组三/组六+金额”写法，按去重选号数量映射组三两码、组三三码、组六四码等赔率。
- 防复发检查：覆盖2至10位选号及有无“各/每”后缀；结果必须返回对应玩法名称和赔率，不得回退普通直选。
### 2026-08-30：快速录入必须按阶段解析

- 问题：预览与下注解析依赖大量整句正则，金额、倍数和号码会互相误识别，且内部系统语法泄漏到用户表格。
- 正确做法：严格按彩种→号码→玩法→金额顺序扫描；预览保留用户物理输入，下注保存时才生成系统结算语法。
- 防复发检查：阶段失败必须返回明确阶段原因；test.txt 全量审计需验证物理行数量、金额合计和保存语法分离。

### 2026-08-30：严格 V2 编译器分批接管

- 处理：新增 `QuickEntryCompiler` 与 `QuickEntryCompileResult`，先迁移明确的直选/组选及单双组混合语法；V2 命中后结果具有权威性，校验失败不得回退为旧版成功结果，未迁移语法才进入兼容层。
- 防复发检查：覆盖 `89一单五组60`、多号“各四单一组”、中文十位倍数、玩法跨行输入及无玩法多号歧义；必须同时运行 `server/tests/QuickEntryCompilerTest.php` 与旧版回归，不得以 V2 覆盖率短期变化冒充完成迁移。

### 2026-08-30：V2 第二批玩法迁移

- 处理：V2 新增定位、和值/跨度、独胆范围、胆拖、双飞/对子、豹子/组选全包及多码组三/组六组合语法；这些语法命中后严格校验金额与选号形态，失败不得回退旧 parser。
- 防复发检查：所有新语法必须带正向和负向测试；未迁移的复式、赖/连、组拖与复杂多票继续走兼容层，不能标记为 V2 完成。

### 2026-08-30：V2 第三批玩法与多票迁移

- 处理：新增复式/组三/组六目录组合、赖/连/沾边赖、单/双胆拖、多码多玩法共享金额；按连续合计和空行分别编译多张票，并支持玩法跨物理行。V2 命中后保留原始文字、正規化文字和 `parser_version`，验证失败禁止回退。
- 防复发检查：覆盖 `15862组六五码108元`、`123456 7890 1789组三组六复式各20米`、`福123粘边赖组六10倍\n组三5倍`、`1拖23456 2拖5678组三组六各10米` 与连续合计分票；运行 `server/tests/QuickEntryCompilerTest.php`、PHP lint 和用户端构建。

### 2026-08-30：V2 第三批验证边界

- 结果：V2 专项测试通过，覆盖复式目录、赖/连/沾边赖、組拖、多码多玩法、连续合计分票和跨行複合票据；全量历史审计仍有 173/667 片段失败。
- 正确做法：未建立明确语义契约的历史旧格式继续保留兼容层，不能把旧回归失败或 V2 专项通过混称为全部格式完成；正式下注切换前必须完成逐票差异审计。

### 2026-08-30：V2 目标玩法审计进展

- 结果：补齐中文金额、多码共享金额、`六码` 前缀、连续合计分票和复杂复式/组选组合后，`test.txt` 审计由 494 提升至 499 个通过片段（168 个仍失败）。`QuickEntryCompilerTest.php` 与 `QuickEntryParserTest.php` 均通过。
- 防复发检查：金额/玩法从选择文本中剥离时必须同时剥离合计与码数前缀，避免把 `10`、`六码` 等元数据当作选号；多票分段失败时不得回退成部分成功。

### 2026-08-30：多行合并的安全边界

- 处理：新增严格的三行以上“彩种前缀 + 纯号码行 + 明确玩法/金额尾行”合并器；普通多行混合玩法若无法证明同票，则保留物理行交给兼容层，禁止整段 flatten 后产生假成功。
- 防复发检查：跨行合并必须满足尾行含明确玩法、前置行全部是纯号码或定位段；连续多票须由合计或空行分隔，不能仅凭多个玩法标记推断同票。

### 2026-08-30：全托与复杂连续多票迁移

- 处理：V2 新增全托展开（单/双胆码、剩余数字集、组三/组六金额）、`托`/`托码`别名、全托合计守恒；支持彩种前缀、纯号码行、玩法尾行组成的三行票据，以及带合计的连续多票。
- 防复发检查：全托必须保留 `组三胆拖/组六胆拖` 赔率身份；连续多票每张票单独编译并校验合计，不得把下一张票的彩种或金额带入上一张。

### 2026-08-30：目标玩法迁移完成审计

- 结果：V2 专项测试与旧版解析回归均通过；全量 667 个片段中按目标玩法关键词筛选的失败项已清空。V2 已正式覆盖复式多码、赖/连、组拖/全托、复杂连续多票与跨行复合票据。
- 防复发检查：任何目标玩法新格式先加入 `QuickEntryCompilerTest.php`，再运行 `audit_test_formats.php` 并按目标关键词筛选；不得因其他历史格式失败而放宽目标玩法的严格校验，也不得移除未完成语法的 fallback。

### 2026-08-30：独立福前缀导致整张注单金额错乱

- 问题：输入以单独一行 `福` 开头时，旧流程将其当成空票；后续 `2 胆300`、`9胆200` 和多码组选行被分开解析，导致号码遗漏、尾部金额被当作选号，整张金额显示两千多。
- 正确做法：顶层识别独立彩种前缀并作用到同一票据的后续非空行；连续空行或显式新彩种才结束继承。新增单双组无分隔符、多码组选无单位金额解析，并验证整张总额 668 元守恒。
- 防复发检查：`QuickEntryParserTest.php` 的 standalone lottery prefix multi-line ticket 必须返回 8 条成功明细、总额 668.00；不能出现空 `福` 失败行，也不能丢失 296 或把 200/300 识别为号码。

### 2026-08-30：福体直组合计被重复乘彩种数量

- 问题：`965 956 775 福体直组1米🈴12` 的 `福体` 类别在合计校验时被重复乘以 2；同时组选按无序组合去重时误丢掉 956，导致识别金额与输入总额不一致。
- 正确做法：合计值只转换一次；组选展示可按无序组合去重，但下注笔数/金额保留每个原始号码出现次数。该输入现在生成直选 6 元、组六 4 元、组三 2 元，总额 12 元。
- 防复发检查：覆盖福体、重复无序组选号、`直组` 组合和 `🈴` 合计；`QuickEntryParserTest.php` 必须验证 3 行、总额 12.00，且直选行 6.00 元。

### 2026-08-30：用户纠正福体直组金额口径

- 问题：`965 956 775 福体直组1米🈴12` 被合计校验判定为识别金额不一致；原因是无序组选展示去重时同时错误减少了金额计数，且 `福体` 总额在一个路径被重复乘彩种数量。
- 正确做法：直选保留 3 个原始号码、福体彩种共 6 注 6 元；组选按号码形态拆为 965 组六两彩 4 元、775 组三两彩 2 元；合计只换算一次，最终 12 元。
- 防复发检查：重复无序号码只能影响展示 token，不能影响 stake_count/amount；福体 `直组` 带 `🈴12` 必须成功且金额严格守恒。

### 2026-08-30：单字“共”合计标记漏识别

- 问题：`965 956 775 福体直组1米共12元` 中的 `共` 未被 V2 合计提取规则识别，后续通用分支把尾部金额再次计入，显示 18 元。
- 正确做法：`共` 与 `共计`、`合计`、`合`、`计` 等价，只转换一次合计金额；该输入现在和无合计、`合12` 一样返回 12 元。
- 防复发检查：覆盖 `共12元`、`共计12元`、`合12`、`🈴12` 四种标记，结果必须完全一致。

### 2026-08-30：合计词必须按 TOTAL 语义统一

- 问题：仅补 `共` 后，`和12元`、`总合12元`、`结算：12元` 等同类合计词仍会落入玩法/金额解析，导致 `福体直组1米` 从 12 元变成 24 元或其他金额。
- 正确做法：V2 surface 层把 `和/共/计/合/结/总`（仅在后接数字时）及 `合计/共计/总计/总合/合共/共合/总共/小计/结算/🈴` 统一转换为合计标记；标点折叠后再次处理，且不转换 `和值/和大/和小/和单/和双`。
- 防复发检查：无标点和带冒号的所有合计别名必须与无合计版本产生相同金额；回归 `965 956 775 福体直组1米…12元` 必须为 12.00 元。

### 2026-08-30：福体合计在 API 彩种拆分时被重复累计

- 问题：parser 已将 `965 956 775 福体直组1米🈴12` 正确算成 12 元，但 `UserBusiness::lineForLottery()` 用不含彩种前缀的 `parse_text` 重算时，跨彩种合计无法匹配，回退路径把原始 12 元分别返回给福彩和体彩，quickPreview 最终显示 24 元。
- 正确做法：回退路径检测原行的 `福体` 范围后，按目标彩种拆分 `count/stake_count/code_count/amount`，每个彩种只拿 6 元明细对应的金额，避免重复累计。
- 防复发检查：Controller 回归必须覆盖带 `🈴`/`和`/`共` 合计的福体直组行；parser 总额和两个彩种 lineForLottery 金额之和必须一致。

### 2026-08-30：复式组选与跨票算式合计被误拆

- 问题：`015689`、`福组六组三各十快`、`合计52+20=72` 被逐行解析时，第一行可能退化成 `015直` 1378 元，第二行无号码失败；算式中的 52/20/72 也可能污染选号。
- 正确做法：识别“纯多位选号行 + 玩法金额行 + 合计算式行”为同一当前票；`各十快` 统一为各10元，组三/组六各生成10元；`52+20=72` 仅作为跨票算式备注，不参与当前票金额校验，当前票金额为20元。
- 防复发检查：该三行输入必须返回两条成功明细（组三六码、组六六码，各10.00），总额20.00，`raw_text` 保留完整三行，不能生成 `015直` 或 1378 元。

### 2026-08-30：完整两张票算式样本识别失败

- 问题：完整输入中第一张 `186-...-596- / 福三元单一元组共13*4=52` 与第二张 `015689 / 福组六组三各十快 / 合计52+20=72` 只用一个空行分隔，旧分段将两张票黏连；末尾算式也被逐行解析，导致第一张和第二张都失败。
- 正确做法：以显式 `共13*4=52` 结束第一张票；支持尾部连字符号码列表和 `N元单 M元组` 语法；第二张玩法金额各10元，`合计52+20=72` 只作为跨票总和。整段四条成功明细总额为72元。
- 防复发检查：完整样本回归必须返回金额 `39.00/13.00/10.00/10.00`，总额72.00；第一张不能退化成默认1元直选，第二张不能把算式数字当号码。

### 2026-08-30：小数单注金额被尾部算式污染并四舍五入

- 问题：`福169注各单0.2米共计169*0.2=33.8米` 未按注数语法处理，`169` 被当作额外三位选择，实际按170注计算后显示34.00元。
- 正确做法：新增“号码列表 + N注各单金额 + 算式合计”解析；严格校验号码数量=N、每注金额为0.2、算式结果为33.8，输出金额保留两位小数 `33.80`。
- 防复发检查：长号码列表、0.1/0.2/0.5 等小数金额均不得转整数；`count/stake_count` 必须等于声明注数，算式中的数字不能进入号码集合。

### 2026-08-30：点号分隔与多票算式合计无法识别

- 问题：`794..974`、`974..479` 的点号号码行和独立 `直1倍`/`组一米` 玩法行被拆散；尾部 `8+9+2=19米福` 只有双项算式清理，三项数字会污染选号。
- 正确做法：将三位号码之间的一个或多个 ASCII 点视为分隔符；支持多行号码块后跟多个独立玩法行；存在空行的多票按算式项分配金额，单票算式只作为备注清理。完整样本输出8、9、1、1元，总额19元。
- 防复发检查：点号号码、独立玩法行、三项以上加法算式必须有专项回归；算式行不能生成 `008/009/002` 等伪号码。

### 2026-08-30：多行“一单/一组”玩法与福体合计错误

- 问题：`一直一组` 后接独立 `🈴40` 时，福体行可能出现合计不一致；`福彩一单444 777` 与 `福彩一单一组309...303` 多行输入会被拆为无号码失败，或把 1/组计数当成金额。
- 正确做法：增加纯合计尾行处理、`一单/一单一组` 前置计数语法；单行号码保持三位列表，计数词限制为一至十或一至两位数字，避免抢占 `F123单100` 等紧凑直选语法。
- 防复发检查：前述两类样本分别必须返回总额40和32，且 `F123单100` 继续按紧凑直选语义通过。

### 2026-08-30：单号码“两单一组”只统计直选

- 问题：`福彩两单一组507 / 🈴6` 在旧统计/审计路径中只取直选4元，忽略一组2元，导致识别金额与输入6元不一致。
- 正确做法：前置计数语法支持单个三位号码，分别生成直选（2单×2元=4元）和组六（1组×2元=2元），合计6元。
- 防复发检查：该输入必须返回两条成功明细、金额 `4.00/2.00`，总额6.00。

### 2026-08-30：纯数字金额行与码数前置组选被误解析

- 问题：复合输入用独立 `12/24/30/30` 结束每张票；旧分段把数字当号码，跨行续码403/307/947丢失，并把“四码/五码”玩法前缀当选号，报组六选号数量不足。
- 正确做法：最终 `🈴96` 前，以纯数字金额行为票据终止标记；纯三位号码行继承上一行玩法；支持“五码组六三倍03479”等码数+玩法+倍数+选号顺序。四票分别校验12、24、30、30，总额96。
- 防复发检查：完整样本必须返回六条成功明细，金额 `8/4/12/12/30/30`，不得把12/24/30识别成号码或遗漏续行选号。

### 2026-08-30：复合票据“六码/五码”被误判组六选号不足

- 问题：`福彩四码组六3倍0479 / 30` 与 `福彩五码组六三倍03479 / 30` 在多行输入中被旧分支拆成号码不足；纯数字 `12/24/30` 终止行也会被当成号码。
- 正确做法：行尾金额分票器优先处理，码数+玩法+倍数+选号顺序统一由 V2 解析；四票金额分别校验后总额96元。
- 防复发检查：完整样本返回六条成功明细，所有状态 success，禁止生成 `024` 或“组六选号数量不足”。

### 2026-08-30：算式总额不能替代缺失票据

- 说明：`03578一倍组三一倍组六` 对应当前票 20 元；`🈴20+90+60+112=282` 是包含前面三张票的总和。仅贴最后两行时，系统只能安全显示20元，不能凭算式虚构90/60/112对应的下注内容。
- 正确做法：完整输入包含四张票时按算式项分配并显示282元；单独输入当前票则保留20元，算式作为摘要而不是可下注金额。
- 防复发检查：完整四票样本必须返回10条成功明细、金额 `80/32/20/20/10/10/60/30/10/10`，总额282；单独当前票必须是两条10元明细。

### 2026-08-30：直接替换主解析器导致识别覆盖率下降

- 问题：将新阶段解析器一次性替换主 `QuickEntryParser` 后，`test.txt` 失败片段由214增加到273，多个历史玩法无法识别。
- 正确做法：恢复已备份的稳定解析器，以独立阶段引擎逐类迁移；每一类通过审计后再切换主入口。
- 防复发检查：切换前必须比较全量审计失败数，若显著增加立即回滚；不得用未完成的新引擎覆盖线上主解析器。
### 2026-08-31：会员上分/下分必须纳入总监链路记账

- 问题：部分会员创建/编辑路径直接写 site_users.balance 或 credit_balance，虽然代理余额可能被扣减，但缺少统一的双方流水契约，无法按同一交易号核对“代理出账 ↔ 会员入账”，也容易出现管理端和代理端口径不一致。
- 正确做法：所有会员总分变更统一调用 ScoreTransfer::setUserBalances()。上分由会员直属代理可用分数扣减、会员总分增加；下分由会员可用分数减少、代理可用分数恢复。两边各写一条 organization_credit_ledger，使用同一 transaction_no，并关联对手账户、租户、站点和操作人。
- 防复发检查：创建会员、代理编辑会员、平台编辑会员均不得直接更新三项余额；必须通过统一服务并置于数据库事务内。会员有 used_balance 时禁止超额下分；未归属会员、停用代理、跨站点账户禁止变更；每笔上/下分金额的双方流水必须相等且方向相反。

### 2026-08-31：代理端顶部身份与彩种列表布局

- 问题：代理端顶部沿用用户端的四个账号/额度字段，无法明确当前处于总监、大股东、小股东、总代理还是代理层级；彩种信息与页眉导航混排，和参考站的横向彩种栏不一致。
- 正确做法：顶部只显示“账号”字段，内容为“层级：当前账号”；层级标签优先取组织资料的 `level_label`，没有时使用系统层级映射。导航独立保留在页眉，彩种列表放在页眉下方横向展示，每项固定 200×46px，选中项绿色底白字。
- 防复发检查：后续修改不得恢复信用/已用/可用字段，不得把彩种列表重新塞回导航同一行；新增层级必须同时补充显示映射并保持彩种栏期号、状态、倒计时逻辑不变。

### 2026-08-31：拦货盈亏按剩余逐级上返与跨级开户

- 问题：占成分配入口使用按原始会员盈亏分别计算的直算方式。代理占成 20% 后，上级仍按原始金额取占成，和“代理先取 20%，剩余 80% 继续上返”的业务口径不一致；组织创建也只允许相邻层级，无法建立总监直接直属代理等关系。
- 正确做法：结算统一调用 `SequentialProfitShare::allocate()`，每个非根节点按收到的剩余金额乘自身占成率，根总监收到最终剩余；占成流水元数据标记为 `sequential_remainder`。`CHILD_LEVELS` 明确列出每个层级可直接创建的全部下级，所选层级保存为真实直属 `parent_id`。
- 防复发检查：示例 10000、代理 20%、上级 20% 必须分配为代理 2000、上级 1600、再上级承接 6400；所有正负盈亏都必须金额守恒，且跨级创建不得自动改写为中间层级。

### 2026-08-31：代理端彩种列表被旧样式裁切

- 问题：彩种栏虽然已移动到页眉下方，但通用彩票列表样式的布局规则会与代理端覆盖样式叠加，造成彩种内容视觉上被裁切、内部对齐不稳定。
- 正确做法：代理端彩种栏明确设置 `box-sizing`、横向滚动和纵向隐藏；列表及彩种项强制 `display:flex`、`flex-direction:row`、固定 `200×46px`，内部行使用左右分栏对齐。
- 防复发检查：代理端彩种栏计算尺寸必须为容器高度 46px、列表横向排列，单项宽高 200×46px；不得重新复用通用 `.lottery` 的纵向/网格布局。

### 2026-08-31：代理端顶部账号文字挤压且未居中

- 问题：代理端头部复用了用户端 `.account` 的网格和字段样式，账号/层级文本在页眉中挤压，账号区域没有独立外层容器，无法按参考站水平、垂直居中。
- 修复：新增 `.agent-identity` 和 `.agent-account-box` 身份容器，账号字段固定 138×40px 并居中；保留“账号 / 层级：账号”语义，长文本省略；通过更具体选择器隔离旧版网格规则。
- 防复发检查：头部账号必须位于身份容器内部，容器和导航保持独立；不得恢复 `.account` 两列布局或让账号文本撑开导航区域。

### 2026-08-31：综合报表占成口径与结算流水不一致

- 问题：报表按会员 `interception_rate` 对每个层级分别计算占成，上级没有使用下级分配后的剩余盈亏；在总代理、股东存在占成时，总投和盈亏无法与逐级结算流水守恒。
- 修复：`AgentReport` 改为读取会员所属组织链，按 `SequentialProfitShare` 先分配最近层级、再把剩余金额传给上级；会员盈亏、离线反水、赚水和平台剩余均基于同一条明细计算。未归属组织的历史会员保留旧比例兼容。
- 防复发检查：报表不得把拦货金额直接当占成盈亏基数；同一明细的会员盈亏与各级分配必须使用统一正负号，后续应以“会员盈亏 + 各级主体盈亏 + 最终承接方 = 0”进行回归核对。

### 2026-08-31：会员栏错误显示总赚水

- 问题：综合报表前端把内部回水字段直接作为会员“总赚水”列显示，和实际会员账务结构不一致。
- 修复：会员分组只保留笔数、总投、总中、盈亏；回水仍可作为盈亏计算的内部项，但不作为会员独立展示列。
- 防复发检查：会员表头必须为 4 个字段，代理/总代理字段不得因删除会员列而错位。

### 2026-08-31：报表层级列写死导致层级错位

- 问题：报表前端固定渲染会员、代理、总代理三组列，当前账号切换到不同层级或存在大股东/小股东时，自己的层级、下级和上级列无法正确显示。
- 修复：接口根据当前组织节点的真实后代与祖先返回 `report_levels`；前端按 `downline/self/upline` 关系动态生成分组和字段，避免静态列错位。
- 防复发检查：每个动态分组的 colspan 必须与字段数量一致（下级 3、当前层级 5、上级 2），会员始终为 4 列；没有实际关联层级时才使用代理自身兼容兜底。
- 当前限制：该版动态列先复用现有报表指标，未改变每个下级账号的聚合粒度；若一个层级下存在多个独立组织，后续需要按组织节点进一步拆分数据行。上级关系已限定为直属父级，避免代理看到直属上级的上级层级。

### 2026-08-31：占成金额正负号及赚水列口径错误

- 问题：占成金额直接沿用带符号的会员盈亏，导致用户中奖时显示负的占成金额；明水、离线反水和总赚水字段之间重复或错位。
- 修复：占成金额使用绝对值；占成盈亏按“用户盈利由上级支付、用户亏损由上级获得”的方向计算，并严格按“占成金额 + 占成金额 × 明水”计算；有占成比例时离线反水单列显示 0，避免重复叠加；总赚水列显示 0。
- 明水比例按最终确认的 `0.085` 计算，不使用旧的 `bright_water_rate`（0.012）字段。
- 防复发检查：用户盈亏为正时占成盈亏必须为负，用户盈亏为负时必须为正；占成金额始终非负；总赚水不填充任何内部水项，但界面统一显示 0。

### 2026-08-31：占成金额 1328.2 被逐笔绝对值放大

- 问题：报表每条下注明细先取 `abs(盈亏)` 再乘 20% 后汇总，混合中奖和未中奖明细时得到 1328.20，而会员汇总净盈亏 -6003.80 按公式应为 1200.76。
- 修复：增加带符号 `share_base` 隐藏聚合字段，先汇总会员净盈亏对应的占成基数，再取绝对值作为占成金额；明水、离线反水和占成盈亏基于汇总后的占成金额重新计算。
- 防复发检查：同一会员报表的占成金额必须等于 `abs(会员净盈亏 × 占成比例)`，不得等于 `sum(abs(每笔盈亏) × 占成比例)`。


### 2026-08-31：修改密码页必须按参考站纵向表单实现

- 问题：用户端密码页误用三栏表格布局，标签与输入框在手机端错位；说明文案、按钮和额外“系统禁止不可用密码”提示也与参考站不一致。
- 正确做法：按参考站结构使用三个纵向字段，每个字段为居中的 outlined 输入框，标签绝对定位浮在边框上沿；说明文字直接位于字段下方，使用深青色粗体两段文案；按钮使用深蓝色“提 交”，移除参考站不存在的黄色禁用密码提示。保留真实 `changePassword` API 与服务端校验。
- 防复发检查：430px 手机视口下字段外框宽度约 96vw、输入框约 86vw、输入框高度约 8vw；标签不参与横向挤压；说明文案不被截断；页面无横向滚动；只做构建和只读页面验证，不提交真实密码修改。
### 2026-08-31：全平台错误区分离线反水、明水和暗水

- 问题：SaaS 站点配置同时保存 `dark_water_rate` 与 `bright_water_rate`，代理报表、分类账、拦货和用户端又分别读取，造成明水重复计算、离线反水列混入用户盈亏。
- 修复：站点统一保存单一 `water_rate`（默认 0.085）；旧字段仅作兼容读取，不再写入或参与计算。代理侧明水统一为 `abs(占成基数) × 0.085`，用户盈利时代理承担负的占成盈亏，用户亏损时代理获得正的占成盈亏；离线反水、暗水和额外占成明水均为 0/不展示。
- 防复发检查：同一站点任意报表、分类账和拦货明细只能出现一个明水口径；用户端盈亏公式为 `中奖 - 投注 + 回水`，不得再加玩法级 `offline_rebate`。
### 2026-08-31：用户 H5 查看更多与停押降水移动端信息层级

- 问题：查看更多沿用桌面六列表格，手机端文字拥挤；停押降水包含编号、下单时间等额外列，和参考站的四列核心信息不一致，底部按钮也未进入独立页面。
- 正确做法：查看更多改为日期/盘口/原始文本/仅退码筛选和浅粉色卡片列表；停押降水移动端只保留期号/号码、应打/实打/停押、原水/实水/降水、文本，并提供空状态、分页提示和返回；手机端停押/降水按钮跳转 `#/dbl`。
- 防复发检查：375/393/430px 视口不得横向溢出；表头和内容必须使用同一四列网格；不得在参考站验证时执行下注、退单或其他真实业务操作。
### 2026-08-31：手机端识别/下注按钮与清空统计同行

- 问题：识别、下注仍使用绝对定位操作栏，在真机上与清空、字数统计及其他控件重叠。
- 正确做法：手机端在 `.text-entry-footer` 内渲染独立识别/下注按钮，使用 flex 靠右排列；桌面绝对定位按钮仅桌面显示，业务处理函数不变。
- 防复发检查：手机端按钮必须与清空和 `0/10,000` 同一 footer 行，375/393/430px 无横向溢出，识别和下注不能出现第二组可见按钮。
### 2026-08-31：手机端开关状态、输入高度和结果合计错位

- 问题：选项开关被样式偏移且默认彩种始终显示打开；异步设置回写会覆盖刚切换的彩种；识别结果出现后输入区和结果表格占位不稳定，桌面操作栏还会与 footer 重叠。
- 正确做法：继续使用真实 Ant Design Switch，卡片内上下居中；默认彩种以当前彩种相对首选项控制 checked，并用 onChange 同步保存；设置只初始化一次；结果区采用可滚动容器和输入区最小高度；手机只显示 footer 操作按钮及一份合计。
- 防复发检查：点击默认彩种后 aria-label、checked、输入区色调一致；有结果时 footer 不被覆盖，结果行可见且 375/393/430px 无横向溢出；不能通过识别测试文本触发自动下注。
### 2026-08-31：默认彩种卡片尺寸不一致及长文本结果溢出

- 问题：默认彩种外框比其他开关卡片矮，开关受顶部内边距影响未垂直居中；识别结果文本使用固定单行高度，长号码超出后不可见。
- 正确做法：默认彩种卡片与其他选项统一 11vw 高度并清除顶部内边距，保留 Ant Design Switch；结果行根据文本长度设置 textarea 行数，启用换行和结果区域滚动。
- 防复发检查：所有选项卡外框高度相同、开关上下留白相等；长文本在结果行中完整换行显示，375/393/430px 无横向溢出；测试只点击识别，不触发下注。

### 2026-08-31：用户 H5 主页下拉刷新与识别结果高度

- 问题：移动端主页在页面顶端继续向下拖动时会触发浏览器下拉刷新；识别结果 textarea 仅按固定 `rows` 估算高度，真机窄屏下会比实际换行内容更高，底部出现大片空白，行内编号/按钮也未始终垂直居中。
- 正确做法：根页面和 `.app` 使用 `overscroll-behavior-y: none` 禁止 overscroll 链；结果 textarea 首次渲染及内容变化后按实际 `scrollHeight` 自适应，结果主行采用居中对齐，统计行同步垂直居中。
- 防复发检查：不得使用 `touch-action: none` 破坏输入和正常滚动；在 375/393/430px 视口验证长文本无多余空白、按钮和编号垂直居中，textarea 与结果区仍可滚动。

### 2026-08-31：H5 识别结果字号被响应式规则缩小

- 问题：移动端结果文本和笔数/金额统计使用 `3vw`，在窄屏上低于原有 14px，用户反馈字体过小。
- 正确做法：结果文本、统计信息使用 `max(14px, 3.6vw)`，行高使用 `max(20px, 5vw)`，在保证真机可读性的同时继续支持换行和高度自适应。
- 防复发检查：375/393/430px 视口下结果字号不得低于 14px；调整字号后需复核 textarea 的 `scrollHeight`、结果区高度和横向溢出。

### 2026-08-31：结果字号需固定 16px 并保留上下边距

- 问题：字号恢复为响应式最小 14px 后，用户仍反馈真机显示偏小，且结果区域需要更明确的上下留白。
- 正确做法：识别结果及统计统一使用 16px/22px，textarea 通过实际 `scrollHeight` 自适应，仅保留上下 4px 内边距。
- 防复发检查：不得再用低于 16px 的移动端覆盖规则；375/393/430px 下长文本应完整换行，结果行高度随内容增长且无多余固定空白。

### 2026-08-31：真机结果区仍显得过小且输入区域过高

- 问题：结果文字虽然恢复到响应式最小字号，但真机截图中仍不符合用户要求；有结果时输入区域固定公式保留了过多空白。
- 正确做法：结果文字和统计固定 16px、22px 行高，textarea 上下各保留 4px；有结果时输入区采用按结果高度折半的自适应公式并保留 40vw 最小触控高度。
- 防复发检查：不得将结果字号降回 vw 小字号；在 375/393/430px 下验证结果文本完整换行、上下边距一致、输入区不占满结果下方空间。

### 2026-08-31：用户要求完全按文字内容决定结果高度

- 问题：结果区域仍存在固定 `line-height` 和最小高度，文字较少时会留下额外空白。
- 正确做法：移除结果 textarea、主行和统计行的固定行高/最小高度，保留上下内边距，由实际换行内容和 `scrollHeight` 决定高度。
- 防复发检查：不得在移动端结果规则中重新设置固定 `line-height`；长文本必须完整换行，短文本不应被固定高度撑大。

### 2026-08-31：查看更多与停押降水页面无法滚动

- 问题：查看更多只设置了最小高度，停押降水表格超出手机可视区后没有稳定的滚动宿主，触摸滑动无法继续浏览内容。
- 正确做法：查看更多面板和 `.body.full-page` 在手机端设置固定可视高度并开启纵向触摸滚动；停押降水内容随 full-page body 滚动，内部不再截断。
- 防复发检查：375/393/430px 下长列表必须能上下滑动；主页根页面仍禁止下拉刷新，页面内部滚动使用 `overscroll-behavior-y: contain`，不得用 `touch-action: none`。

### 2026-08-31：长文本结果被 textarea rows 估算无限撑高

- 问题：结果 textarea 使用 `Math.ceil(text.length / 14)` 设置 rows，343 注长文本会预留上百行最小高度；后续 `scrollHeight` 自适应无法把它缩回实际换行高度，导致主页表格无法正常查看。
- 正确做法：textarea 固定 `rows={1}`，首次渲染、内容变化和容器宽度变化后统一以实际 `scrollHeight` 设置高度；超长结果由外层结果容器滚动。
- 防复发检查：不得重新根据字符数计算 rows；用至少 1300 字符的长文本验证 rows=1、line-height 为 normal、结果容器内部可滚动、输入区同步收缩，且测试不点击真实下注。

### 2026-08-31：真机浏览器工具栏遮挡与结果表滚动回弹

- 问题：Edge/Chrome 真机动态地址栏和底部工具栏变化时，H5 仍按旧的动态视口高度布局，长结果区会被遮挡；倒计时每秒更新还会重复渲染长结果表，触摸滚动松手后容易回到顶部并出现卡顿。
- 修复：viewport 增加 viewport-fit=cover；通过 visualViewport.height 同步 --h5-viewport-height，H5 根壳固定到实际可视高度；主页长结果使用固定剩余空间的唯一滚动容器，并设置 overscroll-behavior、flex-basis: 0 和 height: 0 防止触摸结束时重新按内容高度布局；QuickResultTable 使用基于输入/结果引用的 memo，避免倒计时更新重绘长表。
- 防复发检查：Android Edge/Chrome 430×932 视口下 .app 高度必须等于实际 visualViewport.height，文档无横向溢出；长结果 .quick-result 的 clientHeight < scrollHeight 且底部不超过可视高度；清空测试文本后不得留下结果或自动下注。

### 2026-09-01：手机主页下注记录被隐藏

- 问题：H5 移动端通用规则将 .body:not(.full-page) aside 设置为 display:none，导致主页下方的下注记录以及每条记录的“详、号、退”操作全部不可见。
- 正确做法：移动端主页使用纵向 flex，快录主区域在上、下注记录 aside 在下；aside 恢复显示，记录列表保留独立触摸滚动，详情/号码/退单继续调用原有业务处理函数。
- 防复发检查：移动端 DOM 必须存在 .side-record-item；有记录时 .side-record-action 按钮可见；退单只能通过用户主动点击并经过确认弹窗，测试不得自动提交退单。

### 2026-09-01：移动端下注记录颜色偏红

- 问题：移动端卡片沿用了旧的统一浅红背景，普通未退单记录也显示成红色，和 PC 端灰色记录卡片不一致。
- 正确做法：普通记录使用 PC 端灰色卡片、白色时间栏/金额操作栏；只有 status=refunded 的记录使用退单粉色背景；详、号、退按钮分别保持绿色、浅橙色、红色。
- 防复发检查：移动端普通记录背景必须为 #d9d9d9，退单记录为 #f5d3d7；按钮颜色必须与 PC 端一致，不得把普通记录统一染红。

### 2026-09-01：公告刷新后重复显示

- 问题：公告弹层初始固定为显示状态，每次刷新都会重新出现，且未区分账号。
- 正确做法：登录后成功读取公告才打开弹层；关闭时将公告标题和内容按当前账号写入 localStorage。刷新时若公告内容未变化则保持关闭，公告更新或切换账号时可重新显示；读取失败不显示空公告弹层。
- 防复发检查：无 token 时不得主动显示；首次登录加载完成后可见；用户关闭后刷新同账号不可见；不得自动点击关闭按钮或执行其他业务操作。

### 2026-09-02：沾边赖组三六码一倍金额调整

- 调整：沾边赖组三 6 码一倍金额由 158 元改为 156 元。
- 同步更新：金额枚举、解析测试和结算规则测试。

### 2026-09-01：组选玩法写在号码前的合计单误判

- 问题：`福组六8724561合计800` 的玩法/号码顺序与旧规则相反，未进入明确的组六七码解析。
- 修复：增加“组六/组三 + 多码 + 合计”语法，按实际选号数生成对应玩法；合计金额直接作为整单金额校验，不推导或拆分额外注单。

### 2026-09-01：定位玩法结算未识别“百位/十位/个位”标记

- 问题：定位明细保存为多个位置标记时，结算未按组合数分摊整张金额，导致命中后按整单金额乘赔率，历史测试账号出现异常高额中奖。
- 修复：结算前将同一定位表达式的位置标记合并为一个逻辑注，并按百/十/个位组合数计算单注金额；仅命中的组合按赔率派彩。
- 防复发检查：带“百位/十位/个位”与不带“位”两种写法都必须按组合数分摊，不能使用整单金额直接乘赔率。

### 2026-09-01：手机端粘贴后没有自动识别

- 问题：粘贴处理器只在“粘贴后自动下注”开启时调用识别；关闭自动下注时，粘贴只更新文本，用户必须再点击识别。
- 正确做法：粘贴始终立即调用预览识别；自动下注开关只决定识别完成后是否在允许下注的时段提交，关闭时只显示识别结果。
- 防复发检查：自动下注关闭时粘贴 111 112 113 应出现识别结果且不提交下注；自动下注开启时才允许进入原有提交逻辑；测试不得点击下注。

### 2026-09-01：手机端重复显示盘口标签

- 问题：手机端操作栏同时显示了“盘口：A盘 - A”和上方的“A - A”盘口框，造成重复、重叠。
- 正确做法：移动端隐藏 action-board-label 桌面标签，只保留快录区域的 A - A 盘口框；桌面端继续显示完整盘口标签。
- 防复发检查：430px 视口下 action-board-label 必须不可见，A - A 盘口框仍存在，页面无横向溢出。

### 2026-09-01：H5 桌面视口仍显示重复盘口标签

- 问题：此前只在 max-width:599px 隐藏 action-board-label，导致桌面宽度打开 H5 时仍显示“盘口：A盘 - A”。
- 正确做法：user-h5 所有视口统一隐藏重复操作栏标签，仅保留上方 A - A 盘口框；PC 独立应用不修改。
- 防复发检查：1181×881 和 430×932 下 action-board-label 均不可见，A - A 盘口框存在，页面无横向溢出。

### 2026-09-01：SaaS 批量修改无法按单条投注选择

- 问题：原页面按主单选择，选择用户后只能整单修改，无法针对单条投注明细。
- 修复：增加彩种选择并默认定位当前未开奖期；用户支持多选；投注明细独立复选框，默认全部不选，支持全选当前投注；提交时仅发送选中的明细。
- 防复发检查：切换彩种会重新加载未开奖期和用户；未选择用户时不显示投注明细；未选择投注时执行按钮禁用；服务端继续校验期号、未开奖和明细归属。

### 2026-09-01：SaaS 批量修改未开奖期号为空

- 问题：原 currentIssue 只从已有投注记录中寻找未开奖期，开奖后下一期尚未下注时会错误显示“当前没有可修改的未开奖期”。
- 修复：直接读取 lottery_histories 中该彩种最早的未开奖期；若调度器短暂尚未插入下一行，再使用最近已开奖记录的 next_code 或数字期号递增兜底。
- 防复发检查：即使下一期暂无用户投注也必须显示期号，并将用户/投注列表显示为空状态；修改接口仍校验期号当前有效且未开奖。

### 2026-09-01：SaaS 机器人管理基础

- 需求：机器人作为会员挂载到代理下，配置账号密码、分数、开始时间、随机间隔、金额范围、彩种和福/体权重，并支持启动/停止、转普通会员和查看下单历史。
- 实现：新增 `robot_accounts` 配置表及 AdminRobots 接口；机器人创建时同步创建站点会员并通过 ScoreTransfer 分配分数；管理端新增机器人列表和配置抽屉。
- 当前边界：随机玩法生成、常驻调度下注、开奖结算自动推进尚未接入，机器人默认停止，避免在调度链路完成前产生真实下注。

### 2026-09-01：站点彩种更新后用户端彩种列表为空

- 问题：站点启用彩种关联已切换到新的 lottery ID，但用户权限表仍保留历史停用 ID；用户端按当前站点彩种 ID 查询白名单时全部匹配失败。
- 修复：站点彩种保存事务新增权限同步，按彩种 ID 优先、名称/编码兼容迁移历史权限；新增启用彩种默认可见可下注，移除彩种清理权限，保留用户对仍存在彩种的显式开关。
- 数据修复：已批量修复现有站点用户，`user1`（及本站其他用户）权限已迁移到当前启用的福彩3D、排列三 ID。
- 防复发检查：站点彩种新增、移除、替换均在同一事务内同步 `user_lottery_permissions`，并通过 PHP 语法检查。

### 2026-09-01：H5 登录输入白屏及代理保存分数被重置

- H5 修复：视口增加 `user-scalable=no`、固定缩放范围，登录输入控件统一 16px，关闭 iOS 自动放大；键盘弹出时不再把缩小后的 visual viewport 高度写入整个应用壳，避免输入登录信息时出现大面积空白。
- 代理端修复：仅保存权限或赔率时不再提交/改写分数；实际修改额度时读取数据库最新余额再记账，避免旧页面数据覆盖期间产生的余额变化。
- 校验：负数额度仍被拒绝；正常的非负额度修改继续按代理可用分数和已用分数校验。已完成 user-h5、agent-web 构建发布及 PHP 语法检查。

### 2026-09-02：福多行五码/四码混合组选识别错误

- 问题：`福 12467\n2467\n23457\n组六组三10共60` 被旧的兼容解析器拼成一段数字，无法区分每行的五码/四码，出现号码截断或玩法识别错误。
- 修复：新增多行混合组选语法，逐行保留数字集合并按实际位数生成对应的 `组六五码/四码`、`组三五码/四码`；共享金额按每个玩法/号码集合计算，并严格校验总金额。
- 回归结果：示例生成 6 条明细，每条 10.00 元，合计 60.00 元；五码、四码和福前缀均正确保留。

### 2026-09-01：机器人配置页彩种无法选择

- 问题：彩种复选框被放在没有绑定值的 `el-checkbox-group` 中，Element Plus 组上下文会拦截/覆盖单项状态，导致点击后无法正常选中。
- 修复：移除无绑定的复选框组，改为独立复选框并保留现有响应式配置数组；保存时仅提交已勾选彩种。
- 防复发检查：新增机器人打开抽屉后，福彩3D和排列三可分别勾选/取消；不勾选彩种时保存仍给出提示。

### 2026-09-03：H5 退码列表原始文本搜索框与参考站不一致

- 问题：user-h5 `#/rtl` 页面搜索框额外使用居中的 label 包裹，字号、边框和间距被 rem 规则放大，和参考站的 `textarea.ant-input` 盒模型不一致。
- 修复：改为参考站同等的 textarea 结构；宽度 96%，固定 14px 字号、4px 11px 内边距、1px 边框和底部圆角；聚焦时同步蓝色边框、阴影与 6rem 高度。
- 防复发检查：构建发布后对当前站与参考站计算样式逐项比对，默认和聚焦状态均一致。

### 2026-09-03：H5 退码列表操作栏位置与文字比例不一致

- 问题：仅退码操作栏继承了 user-h5 的放大 rem 样式；搜索按钮图标/文字未按参考站拆分，金额数字仍嵌在标签内，造成位置、间距和视觉比例偏差。
- 修复：按参考站结构拆分搜索按钮文字和金额节点，恢复操作栏透明背景、参考站按钮高度/圆角/字号、图标间距、开关垂直位置及金额间距。
- 防复发检查：重新构建后对两站操作栏的计算样式进行线上比对，按钮、图标、搜索文字、总金额和开关位置已一致。

### 2026-09-03：H5 快录结果内容区与参考站比例不一致

- 问题：参考站快录结果采用“3rem 操作行 + 3rem 文本行 + 独立提示行”，user-h5 旧实现把操作控件和文本压在 23px 网格中，内容较多时字号、行高和按钮位置明显偏小。
- 修复：拆分结果行 DOM，按参考站 rem 尺寸重做操作行、文本行、状态色、删除/新增按钮、失败提示和金额汇总样式，并清除末尾高优先级旧网格规则。
- 防复发检查：使用参考站同样的 8 行输入内容生成 user-h5 结果，线上核对结果行 188.03px、操作行/文本行各 94.02px，横向控件位置一致。

### 2026-09-03：快录结果行放大后紫色输入区被压缩

- 问题：结果行从 23px 调整为参考站的 rem 尺寸后，旧的 `--quick-result-height` 视口扣减公式继续生效，将紫色输入区高度计算为 0px，工具栏被顶到输入区顶部。
- 修复：有结果状态下固定恢复约 24.6rem 的输入容器，文本区约 19.6rem，底部工具栏 5rem，并保留聚焦时白色输入背景。
- 防复发检查：用带结果的真实快录页面线上验证，输入区约 770.95px、文本区约 614.27px、工具栏约 156.69px，工具栏不再覆盖输入区。

### 2026-09-03：快录新标签按钮被旧绝对定位顶到输入区

- 问题：旧的 `position:absolute`、`left/top` 和 `17.1vw` 偏移规则仍作用于新标签、替换文本、规则和预警控件；输入区恢复高度后，按钮漂移并覆盖紫色编辑区。
- 修复：按参考站将新标签和替换文本恢复为同一 flex 行，清除旧偏移，补齐按钮顺序与分隔间距，并把规则/预警控件定位到同一工具栏基线。
- 防复发检查：线上核对新标签 x=35.06、替换文本 x=393.88、规则按钮 x=733.34，均与参考站对应位置一致；紫色输入区从工具栏之后开始。

### 2026-09-03：快录工具栏残留匿名节点导致规则和预警错位

- 问题：替换区的隐藏输入及文本节点仍参与 flex 排列，导致规则按钮和预警框多出额外间距。
- 修复：移除不应显示的替换提示节点，把规则按钮和预警输入框直接放入统一工具栏，并用高优先级样式清除旧偏移。
- 防复发检查：构建发布后线上位置为新标签 x=35.06、替换文本 x=393.88、规则 x=734.16、预警框 x=980.96，紫色输入区不再被覆盖。

### 2026-09-03：A - A 盘口框仍留在旧 actions 层

- 问题：盘口框仍由 actions 中的绝对定位样式控制，和新标签工具栏重叠，造成截图中蓝色盘口框横向覆盖规则按钮。
- 修复：将盘口框改为独立 DOM 行，按参考站使用独立 flex 行和 rem 尺寸；工具栏仅保留新标签、替换文本、规则和预警控件。
- 防复发检查：线上盘口框尺寸为 727.08×109.69px，工具栏从下一行开始，四个控件不再重叠。

### 2026-09-03：快录规则按钮文案过长

- 问题：工具栏按钮显示“规则说明”，比参考站的“规则”更长。
- 修复：改为“规则”，保留原有规则弹窗点击行为。
- 防复发检查：构建发布后线上按钮文本已核对为“规则”。

### 2026-09-03：快录工具栏虚线缺失或位置不一致

- 问题：当前工具栏没有参考站的三个独立虚线分隔节点，旧伪元素还会产生错误的分隔位置。
- 修复：新增三个仅用于视觉分隔的节点，使用绝对定位固定在 flex 工具栏内，不参与按钮排版；禁用旧按钮伪元素。
- 防复发检查：线上三条虚线高度均为 188.03px，横向位置与参考站一致。

### 2026-09-03：快录工具栏外围误出现蓝色边线

- 问题：旧规则给 replace 工具栏增加了蓝色 `border-top`，导致 A-A 下方出现额外蓝线；与参考站只有 A-A 盒子自身带蓝色边框不一致。
- 修复：移除 replace 外围边框、背景渐变和伪元素，仅保留 A-A 盒子自身的蓝色三边。
- 防复发检查：线上 replace 外围 `border-top/bottom` 均为 0，A-A 盒子边框保持 `3px solid rgb(48,83,207)`。

### 2026-09-03：A-A 外围行需显示顶部蓝线

- 问题：用户要求 A-A 所在外围行顶部显示蓝色边线，之前只保留了 A-A 盒子自身蓝色边框。
- 修复：给 `quick-board-indicator` 增加 2px 蓝色顶部边线，保持底部无边框，避免影响下方工具栏。
- 防复发检查：线上外围行 `border-top: 2px solid rgb(48,83,207)`，A-A 盒子仍保持 3px 蓝色边框。

### 2026-09-03：无结果状态下快录工具栏回落旧绝对定位

- 问题：工具栏规则仅写在 `.entry.has-results` 下，无结果时新标签、替换文本和规则按钮被旧定位规则挤到容器底部，预警框也塌陷。
- 修复：将 flex 工具栏和控件样式覆盖扩展到所有 `.entry`，并提高规则按钮选择器优先级。
- 防复发检查：无结果线上位置与参考站一致，新标签 x=35.06、替换文本 x=393.88、规则 x=733.89、预警框 x=979.11。

### 2026-09-03：工具栏下方出现大段空白

- 问题：桌面端紫色输入区继承旧 `margin-top: 17.1vw`，将输入区与工具栏拉开。
- 修复：桌面端输入区改为零上边距，仅保留参考站 `.8rem` 下边距。
- 防复发检查：线上输入区紧贴工具栏底部，页面不再出现异常空白。

### 2026-09-03：快录撤销按钮仍使用旧绝对定位

- 问题：撤销按钮的旧 `top/left` 定位在工具栏调整后造成浮动、压线和位置偏差。
- 修复：撤销按钮改为 flex 工具栏中的静态节点，使用参考站的宽高、字号和灰色样式，并同步调整后续控件。
- 防复发检查：构建发布后撤销节点的静态定位规则已生效，避免再次漂移。
- 线上真实触发撤销后核对：按钮 x=690.02、y=440.91、宽 180.52px，高 112.81px；规则和预警控件随之顺延，不再覆盖紫色输入区。

### 2026-09-03：查看更多未退码卡片被错误渲染成退码样式

- 问题：查看更多使用旧的简单卡片结构，未退码记录没有参考站的蓝色盘口标签、彩种文本底色、红色金额和操作按钮，长文本还被旧字号/间距放大。
- 修复：重构为参考站 `label → p → footer` 卡片结构；未退码和已退码分别应用样式，并补齐复制文本/查看号码按钮。
- 防复发检查：线上加载 9-2 有记录日期，未退码卡片为透明外壳+浅蓝文本+红色金额，已退码卡片为粉红底+深蓝金额，尺寸与参考站一致。

### 2026-09-03：查看更多按钮交互与参考站不一致

- 问题：绿色文件图标被误实现为复制按钮，蓝色文本区域没有复制行为。
- 修复：绿色文件图标和“号”按钮统一打开下注详情弹窗；未退码卡片的蓝色/绿色文本区点击复制原始文本，退码卡片禁用复制。
- 防复发检查：线上验证绿色按钮可打开“下注详情”，弹窗包含彩种和号码/金额/赔率/中奖明细，详情弹窗尺寸与参考站一致。
- 复制行为补强：蓝色文本区使用 execCommand 与 Clipboard API 双重兜底，适配当前 HTTP H5 页面环境。
- 弹窗样式补齐：容器零内边距、宽度 92vw、内容左右 1.5rem、彩种标签 10.75rem×3rem、明细 4.6rem 行高，关闭图标与底部按钮按参考站对齐。
- 响应式验证：393×852 手机端卡片和详情弹窗均通过计算样式核对，未退码浅蓝文本、退码粉红背景、按钮和弹窗宽度与参考站一致。
- 430×932 手机端修正仅退码开关旧 `margin-right: 5vw` 偏移，开关中心与外框中心误差小于 0.01px。
- 手机详情弹窗补充标题/玩法/表头层级和 4.6rem 明细行高，按 430px 视口完成尺寸核对。
- 表格防复发：移动端必须显式保留 flex 四列结构；表头灰底白字、列间白色虚线，数据列比例 35%/18%/22%/25%，金额深蓝、赔率橙色。
- 宽屏端复核：四列表格使用同样的 35%/18%/22%/25% 比例和 4.6rem 行高，数据行隔行浅蓝，表头灰底白字。

### 2026-09-03：详情弹窗长明细撑高外层高度

- 问题：长号码记录时，当前弹窗 body 使用 65vh，外层高度明显高于参考站。
- 修复：改为内部内容层滚动，使用参考站 `calc(100vh - 27rem)` 最大高度，固定头部和底部区域。
- 防复发检查：宽屏外层约 486.91px，body 273.16px，内容层 147.81px；手机端同样按视口限制内容层高度。

### 2026-09-03：桌面工具栏 DOM 调整后手机端仍命中旧选择器

- 问题：盘口已移到独立的 `.quick-board-indicator`，规则和大金额预警也已移入 `.replace`，但手机端仍显示旧 `.replace::before` 盘口并按 `.actions` 定位后两项；结果是真实盘口文字裸露、规则覆盖替换按钮、预警控件消失。
- 修复：手机端隐藏新的重复盘口节点并保留现有蓝色盘口框；按当前 `.replace` 子节点分别定位新标签、替换文本、规则和预警输入，排除规则按钮对替换按钮通用选择器的继承。
- 防复发检查：调整工具栏 DOM 后必须同时检查 430px 窄屏；可见控件应依次包含“＋新标签、替换文本、规则、大金额预警输入框”，真实盘口节点不可再次以裸文字出现，页面不得产生横向溢出。

### 2026-09-03：手机工具栏恢复后仍沿用合并网格

- 问题：上一轮为恢复缺失控件，将盘口和按钮放入同一个两行网格，但旧按钮伪元素分隔线仍生效，虚线穿过盘口；输入区同时保留旧的顶部外边距，按钮下方出现空白，新标签纵向位置也偏低。
- 修复：恢复为“独立盘口行 + 独立按钮行”；禁用按钮伪元素和旧容器伪元素，只让三个真实 `.replace-separator` 在按钮行显示；手机输入区顶部外边距归零，并按新的 15vw 工具栏高度调整标签列表。
- 防复发检查：430px 下盘口行 12vw、按钮行 15vw，按钮横向锚点为 4.14/28.82/50.66vw，三条虚线为 25.37/48.41/64.69vw；输入区必须紧接工具栏，虚线不得进入盘口框。

### 2026-09-03：录入区有内容时仍显示下方历史记录

- 问题：手机快速录入已有文本或识别结果时，首页下方 `.side-record-list` 仍继续展示历史注单，与参考站的聚焦录入逻辑不一致。
- 修复：快速录入非空文本状态增加 `.has-entry-content`，手机端据此暂时隐藏历史记录列表；“查看更多 / 停押降水”导航仍保留。
- 状态恢复：清空文本或下注成功后输入被清空，类名自动移除，历史记录列表重新显示。
- 防复发检查：生产 JS 与 CSS 必须同时包含 `has-entry-content`，隐藏规则只能作用于 599px 以下的 `.side-record-list`，不得影响桌面端或历史记录导航操作。

### 2026-09-03：手机按钮分隔虚线长度仍过长

- 问题：三条真实分隔线虽然已限制在按钮行，但高度仍为 13vw，在 430px 下约 56px，明显长于约 31px 高的按钮。
- 修复：保持 25.37/48.41/64.69vw 三个水平锚点不变，将分隔线高度缩短为 9vw，并把顶部位置从 1vw 调整为 2vw，使其围绕按钮垂直居中。
- 防复发检查：修改手机工具栏时分别核对横向锚点和线段长度；虚线不得进入盘口，也不得延伸到紫色输入区边缘。
# 2026-09-03：手机工具栏虚线使用固定 vw，长度反复偏长或偏短

- 现象：虚线先设为 `13vw` 时过长，改为 `9vw` 后又短于参考站。
- 根因：参考站并未用绝对定位和固定 `vw`。其根字号是 `font-size: calc(100vw / 50)`，工具栏为 flex 行，预警控件高 `6.6rem`，分隔线 `<b>` 使用 `height: calc(100% - .6rem)`、`margin: 0 1rem`，高度随整行内容计算。
- 修复：手机端工具栏恢复参考站的 flex/rem 布局和按钮顺序，分隔线改回父级百分比减 rem 的公式；标签列表也作为可换行的完整 flex 行参与自然高度。

### 2026-09-03：快录有结果后仍显示记录操作且结果区节奏不一致

- 问题：当前页面在解码结果出现后只隐藏手机端历史卡片，仍保留“查看更多 / 停押降水”；结果按钮顺序、输入区高度和结果区留白也与参考站存在细微差异。
- 根因：旧实现用“输入框非空”作为隐藏条件，并混入手机专用 px/vw 覆盖。参考站实际按 `textList` 是否存在，在“解码结果”和“完整记录组件”之间二选一；全页面以 `calc(100vw / 50)` 为根字号，输入区和结果区使用 flex/rem。
- 修复：改用 `.has-results` 隐藏完整 `aside`；结果行顺序调整为“删除 / 序号 / 新增 / 彩种 / 详情”；恢复输入区 flex 剩余高度、结果区 `min-height: 30rem` 和 `3rem` 行高。
- 防复发检查：手机与宽屏必须共用根字号和结果区 rem 规则；原始文字未解码时不得隐藏记录组件，产生解码结果后不得保留记录卡片或其导航按钮。
# 2026-09-03：flex 工具栏改为 auto 高度后虚线消失

- 现象：按钮位置恢复后，三条竖向虚线完全不可见。
- 根因：虚线使用 `height: calc(100% - .6rem)`，但父级工具栏被设为 `height:auto`；百分比高度没有确定的包含块高度，最终计算为 0。
- 修复：父级恢复参考站自然计算出的确定高度 `8.6rem`（`6.6rem` 控件加上下各 `1rem` padding），虚线保留原百分比公式并以 `min-height:6rem` 防止再次归零。
- 防复发：凡是子元素依赖百分比高度，父级必须具有确定高度；若 DOM 结构与参考站不同，应同时保留参考最终计算值作为兜底。

### 2026-09-03：有结果时仅靠 CSS 隐藏记录区不够稳定

- 现象：页面已有解码结果时仍可能看到“查看更多 / 停押降水”行。
- 根因：记录组件仍存在于 React DOM，仅依赖父级 `:has()` 选择器隐藏；旧页面实例或选择器未命中时会继续显示。
- 修复：快录页把 `generatedLines.length > 0` 同步给主布局，有结果时主布局直接不渲染整个 `aside`。
- 防复发：结果区和记录组件必须在 React 结构层二选一；CSS 隐藏只作为兜底，不得作为唯一逻辑。
# 2026-09-03：手机选项栏与记录操作栏使用固定像素近似值

- 现象：五个设置开关整体接近参考站，但卡片上下位置、开关宽度和栏高仍有细微差异；“查看更多 / 停押降水”按钮略窄，外层高度也偏小。
- 根因：页面把参考站的 rem/flex 自适应布局换算成了 430px 下的固定宽高，且选项栏额外使用 `translateY(10px)`；换算和 Ant 组件 DOM 差异叠加后产生偏差。
- 修复：直接采用参考源码：选项栏 `width:96%`、`height:7rem`、`justify-content:space-between`；卡片以 `14rem` 为 flex 基准并自动收缩；开关使用参考的 3rem 高度与选中/未选中宽度；记录操作栏按 `4.8rem / 3.6rem / 1.5rem` 恢复。
- 防复发：响应式区域优先保留参考站 rem/flex 关系，不再把单一 430px 截图测量值固化为 px。
# 2026-09-03：Ant Design 6 开关内部文字沿用新版位移

- 现象：“文/号”以及“是/否”在开关内的位置与参考站不一致，部分状态靠近滑块或出现两层文字错位。
- 根因：参考站使用 Ant Design 3 单层 `.ant-switch-inner` margin 定位；当前 Ant Design 6 同时渲染 checked/unchecked 两个 span 并用 transform 切换。只修改外框和滑块无法复刻文字位置。
- 修复：手机选项栏内将两层文字改为绝对定位；checked 位于左侧 `.6rem`，unchecked 位于右侧 `.6rem`，并按开关状态隐藏另一层。
- 防复发：迁移不同主版本 UI 组件时同时核对内部 DOM，不应只复制旧版外层选择器。

### 2026-09-03：快录“详”弹窗沿用了历史注单详情结构

- 现象：点击快录结果的“详”后显示“号码 / 金额 / 赔率 / 中奖”四列，与参考站横向四组“号码 / 金额”明显不同，也没有重复号码次数。
- 根因：快录弹窗复用了历史注单详情的表格概念和样式，没有按参考站独立的结果明细组件实现。
- 修复：为快录弹窗建立独立结构和作用域样式，按彩种、玩法分组，每行四注；重复号码合并金额并显示红色重复次数，标题显示总笔数与总金额。
- 防复发：快录“详”和历史注单详情必须保持两套独立 DOM/CSS；响应式尺寸继续沿用根字号 `calc(100vw / 50)` 的 rem/flex 关系。

### 2026-09-03：下单反馈未沿用参考站消息样式与文案

- 现象：当前下单仅显示接口返回的“下注提交成功”，缺少请求开始时的“下注中...”提示，成功提示也没有“应打/实打”金额；消息卡外观与参考站不同。
- 根因：当前站使用 Ant Design 6，消息卡样式位于 `.ant-message-notice`；参考站使用 Ant Design 3，覆盖的是 `.ant-message-notice-content`，旧选择器不能直接复用。
- 修复：提交请求前显示信息提示，成功后根据预览金额和接口实投金额生成文案；把参考站 `top:30vh`、`2rem` 字号、`#ffffc7` 底色、2px 黑边、`.5rem` 圆角及图标间距映射到新版 DOM。
- 防复发：跨 Ant Design 主版本复刻组件时必须同时核对调用行为和实际 DOM，不能只复制旧版类名。

### 2026-09-03：手机历史注单底栏仍使用固定像素近似样式

- 现象：金额行偏矮、三个操作按钮偏小且间距不一致，绿色详情按钮显示“详”字，与参考站文件图标按钮不同。
- 根因：旧样式把参考站 rem 规则换成了 430px 下的固定 `25px/27px`，并用 flex `gap` 代替每个按钮的 `margin-right: 1rem`；按钮内容也没有沿用参考站图标。
- 修复：直接恢复参考源码的 `4rem` 底栏、`3.6rem` 按钮、`.8rem` 横向内边距和 `1rem` 间距；卡片恢复 `96%/.5rem/.6rem` 几何，详情按钮使用文件图标。
- 防复发：历史卡片继续使用根字号 `calc(100vw / 50)` 对应的 rem/flex 关系，禁止再次固化为单一机型 px 值。
- 验证：lint 0 errors（既有 15 条 warnings）；生产构建和线上产物检查通过，最终发布资源为 `index-BYDcwZRi.css` / `index-CqxjOJWG.js`。

### 2026-09-03：首页和“查看更多”的“号”进入两套号码界面

- 现象：首页记录卡点击“号”弹出 Ant Modal，“查看更多”记录卡点击“号”则进入带分页、下载和复制功能的完整号码页面。
- 根因：首页 `SideBetRecords` 自行维护 `detailMode="numbers"` 及独立号码弹窗，没有复用 `MorePanel.openNumbers` 的页面状态。
- 修复：首页将当前注单交给主布局，由主布局打开 `MorePanel` 并通过 `initialNumberRecord` 进入同一号码页面；移除首页旧号码弹窗及其复制、打印代码。
- 防复发：两处“号”必须只维护一套号码页和加载逻辑；首页不得再新增独立号码 Modal。
- 验证：lint 0 errors（既有 15 条 warnings），生产构建通过；线上浏览器点击首页“号”后确认统一号码页存在、旧号码 Modal 不存在，随后返回原首页。

### 2026-09-03：手机开奖时间用 grid/absolute 模拟参考站 flex

- 现象：430px 浏览器截图大致接近，但真机上倒计时的纵向位置会随字体行高产生偏移。
- 根因：当前站使用三列 CSS Grid，并把“开盘中”以 `left/top` 绝对定位；参考站实际是 `7rem` 高的横向 flex，右侧 `10rem` 容器再用纵向 flex 自然排列倒计时和状态。
- 修复：调整开奖行 DOM 顺序，将期号作为第二个 flex 项、倒计时和状态放入同一个纵向容器；直接采用参考站 `height:7rem`、`padding:0 2rem`、`width:7rem/10rem`、`font-size:1.8rem`，并清除旧 Grid 遗留的 `column-gap:6vw`。
- 防复发：手机头部不得再把单一 430px 截图换算成 grid 列宽和绝对 top/left；统一依赖根字号的 rem/flex 关系。
- 验证：430×932 下参考站与当前站的名称列 `x=146.1875 / width=60.1875`、期号 `x=232.09375`、时间列 `x=326.8125 / width=86` 完全一致，时间与状态纵坐标仅有约 `0.016px` 的边框舍入差；lint 0 errors（既有 15 条 warnings），生产构建发布 `index-xm4J6F-7.css` / `index-Dw6j_s_m.js`。

### 2026-09-03：相同 CSS 数值在 Ant Design 6 消息外壳下仍与参考站不同

- 现象：黄色底色、黑边和字号已经生效，但实际提示的整体排版仍不同于参考站。
- 根因：参考站的卡片位于 Ant Design 3 `.ant-message-notice-content` 内，外层 notice 自带 8px 留白；当前 Ant Design 6 把卡片、绝对定位和列表间距合并到 notice 节点，强行覆盖后结构关系仍不相同。
- 修复：为下注反馈建立 portal 提示层，复刻“全宽 30vh 容器 → 8px notice → inline-block content”结构；普通 message 不再被全局黄色样式影响。
- 防复发：要求结构级像素对齐时，跨 UI 库主版本应使用隔离的业务组件，不应继续叠加覆盖第三方内部 DOM。
- 验证：lint 0 errors（既有 15 条 warnings），生产构建和线上资源检查通过；发布 CSS/JS 均已切换到独立提示层实现。

### 2026-09-03：多行组三结果缺少“合”且成功行颜色错误

- 现象：相同的 8 行组三文本在参考站末行显示“合/详”，所有行均为薄荷绿成功色和紫色“福”；当前站末行没有“合”，前置行或末行还会出现蓝色“新”或紫灰色背景。
- 根因：当前解析器返回 8 条成功记录但不带 `batch_*` 元数据；前端拆分同源多行时把前置行强制改成 `new`，也没有生成参考站 `ij=1` 对应的合并状态，并把 `batch-end` 错当成大金额警示色。
- 修复：重建行时保留每条语义结果的成功状态，合成仅用于展示的批次与合并文本；末行显示“合/详”和独立总计，成功末行继续使用 `#a7e1ce`。
- 防复发：多行同源且有多条成功语义结果时必须生成合并查看状态；`batch-end` 只表示批次结束，不得改变成功背景色。
# 2026-09-03：手机记录标题的“A盘”低于时间文字

- 现象：盘名尺寸接近参考站，但文字基线比“时间”更低，看起来不在同一行。
- 根因：当前移动端覆盖给盘名设置了 `vertical-align: middle`；参考站使用普通行内元素的默认 `baseline`。
- 修复：仅将 `.side-record-board` 的移动端垂直对齐改为 `baseline`，保留参考站一致的 `1.6rem` 字号和 `2.6rem` 行高。
- 防复发：盘名与标题文本的对齐应比较文字基线，不要用元素外框的几何中线代替。
- 验证：线上计算样式已为 `vertical-align: baseline`；lint 0 errors（既有 15 条 warnings），生产构建发布 `index-CBNVQHWW.css` / `index-CXqQq4uk.js`。
# 2026-09-03：真机号码页底部被浏览器工具栏遮挡

- 现象：桌面手机尺寸模拟可滚动到“请核对”，真机滚动到底仍看不到最底部。
- 根因：全屏容器固定使用 `100vh` 且内外两层同时设置滚动；移动浏览器工具栏展开时，布局视口高于实际可见动态视口。
- 修复：以 `100vh` 作回退并优先使用 `100dvh`，外层改为隐藏溢出、只让内容区滚动；底部加入 `safe-area-inset-bottom`、滚动留白和惯性滚动。
- 防复发：真机全屏浮层必须使用动态视口和单一滚动所有者，不要用桌面等宽截图证明 `100vh` 安全。

# 2026-09-03：真机退出按钮白色电源符号缺失

- 现象：橙色退出格存在，但部分手机不显示内部白色电源符号。
- 根因：当前实现通过 `::before` 输出 Unicode `⏻`，实际字形依赖手机系统字体；参考站使用内联 SVG 图标。
- 修复：改用 `PoweroffOutlined` SVG，显式继承白色 `currentColor`，隐藏旧文字 Grid 节点以保证图标居中。
- 防复发：核心操作图标不得依赖系统 Unicode 字形；应使用项目内 SVG 或组件图标。
- 验证：430px 下按钮为 `39.4375×43px`，SVG 为 `23.21875×23.21875px`，坐标 `x=398.671875/y=9.890625`，在按钮内居中且 `fill=#fff`。
# 2026-09-04：管理端下单汇总/风险入口缺少后端路由

- 问题：点击管理端下单记录的“玩法汇总”或“号码风险”时，请求 `/admin/bet-records/aggregation` 未命中控制器，ThinkPHP 动态解析为不存在的 `app\\controller\\Api`，页面显示“控制器不存在”。汇总明细接口同样缺少显式路由。
- 正确做法：在 `server/route/app.php`（并同步路由副本）显式注册 `BetAggregation/index`、`BetAggregation/details`，且置于下单记录集合路由之前并使用 `completeMatch()`，避免嵌套路径被集合/动态控制器吞掉。
- 防复发检查：点击玩法汇总、号码风险及任一汇总行明细，分别验证 HTTP 200、统一 `code/message/data/request_id` 响应；不得回退为控制器不存在或 404。
