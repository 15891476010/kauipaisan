{
  "name": "kuaipaisan-saas",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -k -n api,admin,pc,h5,agent -c yellow,blue,cyan,green,magenta \"npm run dev:api\" \"npm run dev:admin\" \"npm run dev:user-pc\" \"npm run dev:user-h5\" \"npm run dev:agent\"",
    "dev:api": "./scripts/php think run --host 0.0.0.0 --port 8000",
    "dev:admin": "npm --prefix apps/admin-web run dev -- --host 0.0.0.0 --port 5173",
    "dev:user-pc": "npm --prefix apps/user-pc run dev -- --host 0.0.0.0 --port 5174",
    "dev:user-h5": "npm --prefix apps/user-h5 run dev -- --host 0.0.0.0 --port 5175",
    "dev:agent": "npm --prefix apps/agent-web run dev -- --host 0.0.0.0 --port 5176",
    "build": "npm --prefix apps/admin-web run build && npm --prefix apps/user-pc run build && npm --prefix apps/user-h5 run build && npm --prefix apps/agent-web run build",
    "db:status": "./scripts/php think migrate:status",
    "db:upgrade": "./scripts/php think migrate:run"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
